# Feature catalog and status

The complete scope, known before the first line of architecture code. This is
the **destination**, reached through the staged releases in
`docs/ARCHITECTURE.md` and `BUILD-PROMPT.md` Part G — beginning with a
deliberately smaller 1.0.

**Part H requires the row for a feature to be updated in the same commit that
finishes it.** A row that says `done` for a feature whose control renders but
does nothing is a defect in this file as much as in the code — see the
wired-tools rule.

## Legend

| Status | Meaning |
|---|---|
| `—` | Not started. |
| `wip` | In progress. Not reachable in the UI: the command registry's `when` predicate hides what does not exist yet. |
| `done` | End to end. Registered, contract entry validates, kernel proof **and** UI dispatch test both pass, strings are i18n keys, survives save and reopen. |

## Progress

| Stage | Scope | Status |
|---|---|---|
| 0 | Walking skeleton — the architecture, whole | **wip** |
| 1 | Viewer core (D1) | — |
| 2 | Page management (D2) | — |
| 3 | Annotation platform, then tools (D3) | — |
| 4 | Forms (D5) | — |
| **1.0** | **Minimum Shippable release — Stages 0–4** | — |
| 5 | Text editing (D4) | — |
| 6 | OCR (D6) | — |
| 7 | Security and signatures (D7) | — |
| 8 | Import/export/convert and non-AI review tools (D9, D10, D8) | — |
| 9 | AI and cloud (D11, E5, D8 AI items) | — |
| 10 | Ship (D12, E3, E4, a11y and visual QA, perf, Store assets) | — |

---

## D1 — Viewer and navigation · ribbon: Home (display controls in Tools › Display) · Stage 1

| Feature | Status |
|---|---|
| **Gate:** IPC payloads bounded per invariant 11 — no channel's payload scales with document size per *operation*; the one sanctioned byte crossing is a snapshot, once per **version**. Moved here from the Stage 0 gate rather than asserted there: at Stage 0 the contract declares a single channel carrying a version string, so a check would have passed without inspecting anything and stayed green while the channels that make L11 bite — page rasters, document bytes, save output — did not exist. A vacuously-green invariant check is worse than an honestly-deferred one. Assert it as the first document-carrying channel lands. | — |
| Continuous scroll with lazy per-page render (IntersectionObserver) | — |
| Zoom: fit-width / fit-page / presets / ± / Ctrl+scroll | — |
| Two-tier zoom: instant CSS stretch + 150 ms debounced true re-render | — |
| Thumbnail sidebar (lazy, drag-reorder) | — |
| Page navigation: go-to, PageUp/Down, Home/End, Alt+arrow history, click-to-jump | — |
| Recent files | — |
| Start screen (features grid, errors inline) | — |
| Multi-document tabs | — |
| Split view (two pages) | — |
| Side-by-side document compare | — |
| Loupe | — |
| Rulers and grid | — |
| Dark page mode | — |
| Named destinations panel | — |
| Links panel | — |
| Layers (OCG) panel with visibility toggle | — |
| Search: case / whole-word / regex, Unicode-normalized, CSS Custom Highlight API, cancellable background indexing | — |
| Status bar | — |
| Command palette | — |
| Themes: light / dark / high-contrast, accent color | — |
| HD render toggle (PDFium) | — |

## D2 — Page management · ribbon: Organize · Stage 2

| Feature | Status |
|---|---|
| Delete pages | — |
| Rotate 90 / 180 / 270 | — |
| Drag-reorder | — |
| Duplicate | — |
| Insert blank | — |
| Insert from PDF | — |
| Insert from image | — |
| Extract to new PDF | — |
| Merge PDFs | — |
| Split (ranges / one-per-page) | — |
| Crop | — |
| Resize pages | — |
| Replace page | — |
| Swap pages | — |
| Find duplicate pages | — |
| Deskew and enhance scans | — |
| Page transitions (`/Trans`) | — |
| Generate TOC from bookmarks | — |
| Bates numbering | — |
| Headers and footers | — |
| Watermark | — |
| Background | — |

## D3 — Annotations and markup · ribbon: Comment · Stage 3

| Feature | Status |
|---|---|
| Highlight, underline, strikethrough (text-markup via selection) | — |
| Ink | — |
| Rectangle, ellipse, line, arrow | — |
| Polygon, polyline, cloud | — |
| Text box | — |
| Sticky note | — |
| Callout | — |
| Caret | — |
| Typewriter | — |
| Stamps: built-ins, custom image, multi-page apply | — |
| Measure distance / area / perimeter with calibration | — |
| Link (URL / page) | — |
| Snapshot region to PNG | — |
| Place image (move / resize / delete) | — |
| Eraser | — |
| Select: multi, resize handles, arrow-nudge, clipboard copy/paste, Delete | — |
| Redact marks (solid + blur preview) | — |
| Style controls: color, opacity, line width, font, size | — |
| Annotations panel (by page, click-to-jump) | — |
| Comment styles panel | — |
| Persistence as real PDF annotation objects | — |
| `srcRef` invariant: never rewrite annotations the app did not author | — |
| Annotations survive page ops via command remapping | — |

## D4 — Text · ribbon: Edit · Stage 5

| Feature | Status |
|---|---|
| Select and copy (native text layer) | — |
| In-place text editing: line-level (visual line clustering, run diffing) | — |
| In-place text editing: region replacement | — |
| Object-level edit (move / scale / recolor / delete any page object) | — |
| Document-wide replace-all | — |
| Typewriter | — |
| Find and replace | — |
| Spell check (nspell + dictionary management) | — |
| Translate document text | — |
| Word count | — |

## D5 — Forms · ribbon: Forms · Stage 4

| Feature | Status |
|---|---|
| Render and fill all AcroForm field types (text, checkbox, radio, dropdown, listbox, signature) | — |
| Create fields by drawing | — |
| Delete fields | — |
| Flatten | — |
| Forms panel | — |
| Export / import data (JSON, XFDF, FDF) | — |
| Heuristic field detection on flat documents | — |

## D6 — OCR · ribbon: Tools › OCR · Stage 6

| Feature | Status |
|---|---|
| Scanned-page detection | — |
| tesseract.js OCR, 13+ languages, page scope choice | — |
| Invisible selectable text layer | — |
| Search integration | — |
| Export searchable PDF | — |
| OCR region (drag a rectangle) | — |
| Local handwriting OCR (TrOCR small/base, on-demand download, cached, offline) | — |
| Azure Document Intelligence integration | — |

## D7 — Security and signatures · ribbon: Protect · Stage 7

| Feature | Status |
|---|---|
| Open encrypted PDFs (auto password prompt) | — |
| Set user / owner password (AES-256) | — |
| Permission flags | — |
| Remove password | — |
| True redaction (MuPDF content removal; solid and blurred; mixed in one pass; confirm dialog) | — |
| Find-and-redact by search | — |
| Sanitize / flatten document | — |
| Digital signing (PFX/P12, PKCS#7 detached) | — |
| TSA timestamping — **implemented correctly or not offered** | — |
| Certify | — |
| Signature verification (CN, org, validity, byte-range hash check) | — |
| Visible signatures (draw / type with font choice / upload) | — |
| DocuSign integration | — |

## D8 — Review · ribbon: Review · Stage 8 (AI items Stage 9)

| Feature | Status |
|---|---|
| Document compare | — |
| Annotation import / export | — |
| Spell check pass | — |
| Reading-order / tagged-PDF inspection | — |
| Accessibility check | — |
| Comment summarization (AI) — Stage 9 | — |

## D9 — Import and create · ribbon: Tools · Stage 8

| Feature | Status |
|---|---|
| Markdown → PDF (new or append) | — |
| CSV → PDF table | — |
| Office import (LibreOffice) | — |
| Image(s) → PDF | — |
| Open from URL (SSRF-guarded) | — |
| Webcam capture | — |
| Document scan (edge detection) | — |
| Edit page in external app and reimport | — |
| Import page as OCG layer | — |
| Cloud storage: Google Drive, Dropbox, OneDrive, Box, SharePoint — Stage 9 | — |

## D10 — Export and convert · ribbon: Home › Export + Tools › Convert · Stage 8

| Feature | Status |
|---|---|
| Pages → PNG / JPEG / WebP (range, DPI, quality) | — |
| Text extraction, plain and layout-preserving — **MuPDF** structured text; the founding record's "when Poppler available" is withdrawn, since Poppler was named in no matrix row and no provisioning list ([ADR-0013](DECISIONS/0013-pdfa-export-and-text-extraction-engines.md)). Layout fidelity is **unexecuted**: ENGINE-SPIKE H7 compares it against `pdftotext -layout` before this is built on | — |
| Word (rich / layout / text modes) | — |
| PowerPoint | — |
| Excel: table detection (automatic / force-OCR / local handwriting / Azure) | — |
| Excel: editable review grid | — |
| Excel: styled output with real fonts, fills, borders, merges, number formats | — |
| Excel: combine-pages option | — |
| Email document | — |
| Print (MuPDF raster at chosen DPI to system dialog — never print the DOM) | — |
| PDF/A-2b export (honest blocker reporting) — **Ghostscript**, which is **not provisioned and does not ship until this is built** ([ADR-0013](DECISIONS/0013-pdfa-export-and-text-extraction-engines.md)). The provisioning script and its registration into the external-converter seam are part of this row's work, not a prerequisite sitting in a binary list. **Unexecuted**: ENGINE-SPIKE H6 converts a document with a non-embeddable font, transparency and an untagged image, and validates with veraPDF rather than Ghostscript's own exit code | — |
| Optimize / compress | — |
| Barcode generate and read | — |

## D11 — AI · ribbon: Review › AI · Stage 9

| Feature | Status |
|---|---|
| Assistant dialog (chat about the open document) | — |
| Comment summarization | — |
| Vision analysis (table reading assist) | — |
| Provider registry: Anthropic, OpenAI, Google Gemini | — |
| First-run onboarding: choose provider → paste key → `validateKey()` → or Skip | — |
| Keys via `safeStorage`; refuse-and-say-so if unavailable | — |
| Honest no-key empty states everywhere | — |

## D12 — Shell and UX · Stage 0/1 substrate, completed Stage 10

| Feature | Stage | Status |
|---|---|---|
| Settings (registry-driven, full inventory) | 0 → 10 | wip |
| Keyboard shortcut reference (F1) + customizable bindings | 10 | — |
| Autosave (interval setting; **off by default**) | 10 | — |
| Crash recovery offer | 0/1 | — |
| Error boundary with reload | 0/1 | — |
| Toasts | 0/1 | — |
| Window title sync (`file ● — Monstera`) | 1 | — |
| File associations and drag-drop open | 10 | — |
| About (version, licences, source offer) | 10 | — |
| Packaging test — against a **built application**, not a source tree: (a) the unpacked `.node` addon is found through `process.resourcesPath` from `app.asar.unpacked` ([ADR-0010](DECISIONS/0010-native-mupdf-through-an-ffi-shim.md)'s correction records this as a packaging obligation, not an ABI one); (b) **`NOTICE` is present in the installed layout**. (b) is compliance, not robustness: the FreeType License's binary-distribution clause requires its disclaimer in the **distribution** documentation, and a file that exists only in this repository does not discharge it — `proof:licences` covers the content, so what is missing is delivery. Belongs to the **first build that reaches a user**, not to Stage 0: `electron` is not a dependency yet and there is no packaging configuration, so against Stage 0 this obligation could only block the stage wrongly or be waved past. Recorded-and-undelivered is the state it is in, and this row is where it stops being invisible. | SHIP 1.0 | — |
| Native-binaries manager (status, verify, download) | 10 | — |
| Updater: Store channel and web channel | 10 | — |
| Review prompt (EngagementService) | 10 | — |
| Onboarding | 10 | — |

---

## Stage 0 — architecture substrate

Not user-facing features, but the exit gate for everything above.

| Item | Status |
|---|---|
| Pre-commit guards (secret scan, file policy, lockfile integrity) + CI mirror + proofs. The file policy rejects text carrying characters **invisible to a reader**, in two families with one purpose: C0 controls and `0x7f` (a mangled escape sequence, scanned over the whole blob rather than the sniff window), and the **Trojan Source** class — bidi overrides and zero-width characters, [threat model §4.16](security/THREAT-MODEL.md), decoded rather than read byte-wise since a byte scan cannot express them. Ordinary non-ASCII prose is accepted, with a control case, because a guard rejecting em dashes and CJK would read as broken rather than as wrong. | **done** |
| Pinned-hash binary provisioning primitive | **done** |
| Governing documents (ARCHITECTURE, CLAUDE, README, CONTRIBUTING, SECURITY, ADRs) | **done** |
| Monorepo workspaces + enforced import boundaries | **done** |
| Contract: channels defined once; the four surface **types** are derived from it and exhaustive at compile time (proven — narrowing a handler map to `Partial<>` turns `proof:contract` red) | **done** |
| Contract: the four surfaces **implemented** — main handlers, preload bridge, renderer client, browser shim. **Three of four done.** *Browser shim* (`packages/testing`): a full `ContractClient` derived by `createClient`, running the real `wrapHandlers` boundary rather than canned envelopes, structured-cloning both directions so it rejects what the wire rejects. *Main handlers* (`apps/desktop`): `createContractHandlers` assembles the map annotated `ContractHandlers`, and `registerContractHandlers` registers every channel by iterating `channelIds` — **but nothing calls it yet**, because there is no Electron main entry, so the handlers are registerable rather than registered. *Renderer client* (`packages/ui`): `createRendererClient` derives the surface over a one-function transport and refuses to build when the bridge is absent, so a missing preload cannot be reported as a document error. *Preload bridge* (`apps/desktop/src/preload.ts`): exposes one function under `BRIDGE_KEY`, and its **import half is proven** — `proof:preload` derives its imports with the TypeScript compiler and checks them against an allowlist **read out of ARCHITECTURE §9 invariant 1**, so amending the invariant changes what the preload may import in the same commit. **All four surfaces now exist.** *(Corrected 2026-08-21 — the sentence that stood here said enforcement was owed because "no window loads the preload" and "no binary exists to run a window with". Both are false since the runtime is provisioned in CI and `createMainWindow` loads the preload.)* **The preload half is now enforced and was not**: `proof:rendererpolicy` asks the running page whether the bridge is reachable, and the first run said no — `tsc` emits the preload as ESM into a `"type": "module"` package and a sandboxed preload is loaded as CommonJS, so it had **never executed**, announced only through Electron's `preload-error` event. It is now bundled to `preload.cjs` and read back as present, alongside the absence of any Node global ([ADR-0020](DECISIONS/0020-the-preload-is-bundled.md)). What remains owed on this row is one thing and it is named precisely: **nothing calls `registerContractHandlers`**, because `startShell` takes its handlers as a parameter and no composition root constructs them. That is the composition root's row, not this one. | — |
| CapabilityRegistry (FileHandles, invariant L2) | **done** |
| DocumentService — the open-document index: `DocId` minted never derived, one file is one document (dedup on the merge-only identity rule), close removes from the index before awaiting teardown, and the **save-time write-target check** re-verified against the actual file. Four verdicts and only one permits a write; the walk carries its own positive control and refuses a verdict when it comes back empty (audit item 4b). Six widenings applied and confirmed red, including one that exposed a vacuous control. **`sole-writer` now requires more than a matching `dev:ino`** ([ADR-0009](DECISIONS/0009-document-identity-and-the-command-log.md), 2026-08-19): an inode number is a slot and slots are handed back out, so `unlink` + `create` can land a different file on the same index — measured, by an ubuntu runner reporting `sole-writer` for a replaced file while `windows-latest` passed the same case. The two directions are not symmetric: a **differing** index settles replacement, a **matching** one is necessary and never sufficient. `ctime` is the corroborator, because a reused inode always carries a fresh one; `birthtime` is the field this obviously wants and is measured to **lie** on NTFS, where file tunneling restores the previous file's creation time for exactly the delete-and-recreate pattern. A matching index with a moved `ctime` is `unverifiable`, not `replaced` — it cannot be told from an in-place edit by another application, and both refuse the write. Driven through the injected reader rather than a real `unlink`, because whether a filesystem reuses the slot is luck (this machine's NTFS: 0 reuse in 40 attempts), and a defect that reproduces only on someone else's allocator gets closed as flaky. Mutation-tested in **both** directions: restoring the bare index comparison reddens the hole case and the absent-corroborator case; making `sole-writer` unreachable reddens its control and two neighbours. **Not done, and not claimed:** version bump and `savedVersion`, the per-document serial lane of [ADR-0009](DECISIONS/0009-document-identity-and-the-command-log.md) §7, handle recycling, and the Save-As target check. | **partly done** |
| ADR-0009 §7's **per-document serial lane** — commands, queries, save and teardown queue per `DocId`; the lane lives on the record so lookup is get-or-miss and cannot resurrect a closed document; close splits into synchronous index removal plus lane-ordered teardown (dated clarification in the ADR); lane ordering is per-document → index only, enforced by an async-context marker so a `saveAll` reentry is a named error rather than a silent deadlock; the queue cap is 64, chosen so a proof can drive it. No accessor for a document's current version exists — the lane hands work the version it operates against and reads the stamp back **after** the work, so a command that bumps is stamped with the version it produced rather than the one it replaced. Same-document reentry is a named error, not a hang — for `run` and for `close`, the latter because `await close(A)` hangs while `void close(A)` behaves, so that hazard punishes the careful caller and a recorded note would not reach them. The cross-document sibling stays recorded rather than guarded: both its forms fail alike and it has no call site. Ten widenings confirmed red, two of them separating a removed guard from a misplaced one. | **done** |
| ADR-0009 §5's **version counter** — `DocVersion` monotonic from 1, bumped by every applied mutation including undo and redo; `savedVersion` seeded from the initial version so a freshly opened document is clean; `dirty` is `savedVersion !== currentVersion`, recorded as a **conservative approximation** with its false-dirty case (save, undo, redo back to identical content) proven, because it fails towards prompting rather than towards losing work — the opposite direction from cursor equality. No service-level `isDirty(docId)`: read outside the lane the stale answer is *clean*, which closes a document without prompting. Writer of record decided ahead of any caller: `bumpVersion` and `markSaved` narrow to the CommandBus and the save pipeline when those land. Four widenings confirmed red. | **done** |
| ADR-0009 §8 — the **engine seam**. Both writer shapes live in the type: `Apply<W, K>` is conditional on the declared writer, so a live-session writer mutates in place and returns void while a byte-image writer consumes an image and produces one. **Exactly one adapter** implements it — MuPDF, live-session — and the byte-image side deliberately has nothing behind it. Its control is a type-level fixture in `proof:contract` that builds a byte-image writer and a byte-image `Apply` **with no type assertion**; modelling the seam on live-session ops alone turns it red. Sessions are opened from the kernel's bytes and serialise back, so no engine owns authoritative state (ADR-0007's kill-the-process recovery stays open); non-retention is asserted on the **bytes**, not on their length, because equal length cannot separate a clean serialisation from a corrupted one of the same size — resolution-tested with a same-length, one-byte-different mutation. Sessions are **branded** (fabrication is a compile error, as for `CanonicalPath`) and the native document lives in a `WeakMap` beside the token — the brand and the map do different jobs: the brand stops fabrication, the map catches a real session that was already closed, so a second `close` is a named error rather than a double `destroy()`. | **done** |
| **`DocumentService` holds the canonical bytes** ([ADR-0009](DECISIONS/0009-document-identity-and-the-command-log.md) §8, [ADR-0007](DECISIONS/0007-memory-budgets-and-the-document-size-ceiling.md)). The seam **enabled** this and did not satisfy it: `open` takes bytes and `serialise` returns them, so no engine is forced to become authoritative — but the record held no bytes, so killing an engine host lost everything since the last save. The trigger `kernel-holds-canonical-bytes` turned `check:advisories` red the day `documentService.ts` named `Uint8Array`, as designed, and was answered rather than disarmed. **The record now holds the image** ([ADR-0021](DECISIONS/0021-the-canonical-image-is-retained.md)), and two of this row's three questions are answered. *How many are resident:* exactly one image per open document, its lifetime the record's — **sized, not assumed**: `perf:gate` reports `main` at 1.00× of file size holding one and 2.00× holding two, breaching ADR-0007's 1.5× on both content shapes, which is also that instrument's resolution test run before the decision rather than after. **Measured twice, and the second is the one that is evidence.** `roleMain.mjs` is a MODEL of main — it reads, hashes and holds, and never constructs a `DocumentService` — so its figures are arithmetic about bytes. `roleMainService.mjs` opens through the real service with the production reader and is asserted against the same budget: **1.00× and 1.03×**, so the implementation matches the model. Its resolution test is the argument for it existing at all — one leaked second reference inside `DocumentService` doubles it and breaches on both shapes while the model is unmoved and passes (JJ-1/LL-4, closed). And the resident count is of **every document-scaled byte, not only images**: `Checkpoint` is a whole byte image per terminal entry and uncapped, so with a 1.00× image the first checkpoint puts `main` over budget — a ceiling enforcing one of the two terms that consume it is a guard satisfiable while the thing it guards is breached (JJ-2). The log reports what it *physically retains*, redo tail included, because undo moves a cursor rather than popping. *What happens at the ceiling:* `documentBytesCeiling` is **required and undefaulted** — a number in the kernel would be a second opinion about §9.17, which `memoryBudgets.mjs` alone reads — and crossing it makes `open` return `at-capacity`, an outcome and not a defect, with no eviction (rejected: it defers the failure to an arbitrary later moment, since re-reading is safe only while the file is unchanged, which is the condition that may have changed). Capacity is checked from `stat` **before** the read, so an over-large document is refused without being allocated, and again from the bytes read, because `stat` and the read are two observations of a file anything may write between. **Still owed, and it is the third question:** that a killed host actually *recovers*, asserted against a **running** process. Retention makes recovery possible; it is not evidence that it works, and the host does not exist yet — invariant 25's `utilityProcess` trigger fires when one is written. | **partly done** |
| **The memory gate's scenario covers what main HOLDS, not what main WRITES** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 7, correction 2026-08-26). `perf:gate` measures main through the real service — `{ role: 'main-service', budget: 'main', script: roleMainService.mjs }` — and `perfBudget.proof.mjs` is an **unconditional** step of `ci.yml`'s `shim` job on every push to `main`, so this is an enforced number rather than a research one. Read here 2026-08-26: `main-service` at **1.01×** of file size on the image-heavy fixture and **1.02×** on the object-dense one, against ADR-0007's **1.5×** — just under 0.5× of headroom, and the ratio is `(peak − baseline) ÷ file bytes`, so **a second full copy adds 1.00× by definition** rather than by estimate. **No new instrument is owed:** a second memory reading built for Decision 7's snapshot write would be a second opinion about a question this gate already answers (B3a), and that is the mistake this repository paid for three times in one day. **What IS owed is the scenario, and this row is its trigger.** `roleMainService.mjs` cannot exercise a snapshot write because no snapshot write exists, so the green above means *main holding one copy is within budget* and **not** *main writing a copy is*. The day one lands — Decision 7's candidate, or any other producer of a canonical image on disk — that role must write one inside the measured window. Until it does, the gate reports green while blind to the exact operation Decision 7's third supporting argument names as the risk: *"Re-transmitting hundreds of megabytes from a main process already near its ceiling is the worst possible moment to do the most expensive thing."* That is audit item 4's *branch nothing arrives at*, sitting inside the instrument a decision leans on — which is why it is written down as an obligation with a trigger instead of being left to whoever writes the write. Kept here and **not** in `docs/security/engine-advisories.json`: a symbol scan cannot see an event, and this expires on one. **THE TRIGGER FIRED 2026-08-26** and is answered for the model, with one residual stated rather than absorbed. `roleMain.mjs` now writes the canonical bytes to a file **inside the measured window**, holding them across it — the same call `SessionAreaSurface.writeSnapshot` makes — and `main` stays at **1.00×** on both fixtures, so the hand-off adds no resident copy on a 199.4 MB document. Non-vacuous: writing `Buffer.from(canonical)` instead reddens both fixtures at **2.00×** and **1.99×**, a whole extra copy, so the gate can see the failure it is watching for. **THE RESIDUAL IS CLOSED, 2026-08-26** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 14). It was blocked on a call site rather than on effort — `DocumentService` holds the image on a private record — and the answer was not a lesser accessor but that **nothing should receive the bytes at all**: `writeCanonicalImage(supervisor, docId, destination)`, guarded by a capability whose only mint is module-private to the supervisor's module, exactly as `bumpVersion(writer: CommandWriter)` is. The service writes; the caller says where. So `roleMainService.mjs` now drives the **shipped** call site and invents nothing — reading the document a second time would have measured the harness's own copy, and inventing a call site would have measured something nothing ships. Read here 2026-08-26: `main-service` at **1.00×** on the 199.4 MB image-heavy fixture and **1.03×** on the object-dense one, with the service still holding exactly the document after the write. The figure is now a measurement of the real path rather than of a model of it. **And the gate caught a defect on the way that no reader would have:** the supervisor's module first wrote `import { type X } from '@monstera/kernel'`, which under `verbatimModuleSyntax` keeps the statement and emits a side-effect import of the barrel — reaching `mupdfWriter.js` and binding the native library. It failed on the **baseline** (97.6 MB against a 96 MB base limit) while the ratio still passed at 1.04×, so the number that moved was not the one anybody watches. `import type { … }` is erased whole. | **done** |
| ADR-0009 §6 + §3a — the **command union and routing table**. `Command` declared once as a zod discriminated union in `@monstera/contract`, TS type inferred. `commandSpecs` is a mapped type over the kind union, so a missing kind and an unrouted kind are both compile errors. Both axes are declared per command and **neither can be declared without its consequence**: `reproducible: false` requires `replay: 'stored-effect'` (§3a's sentence as a type), `invertible: false` requires `undo: 'checkpoint'` (§4's, so a checkpoint cannot quietly become optional). Ten compile-fail cases in `proof:contract`, three of them controls; every reject case declares the error code and the reason it expects, matched against the diagnostic with type dumps elided, and must declare a name that appears in the diagnostic but is **not** the reason (or `null` to say there is none). **`apply` is now bound**: `writer` and `apply` are one indivisible choice via a distributed mapped type, so declaring `pdf-lib` and supplying MuPDF's handler is a compile error rather than a review comment — proven both ways, by a reject case and a control that must still compile. **`capture` is bound the same way**, added with §4's log which fixed its type — so declaring one writer and reading prior state through another's session is a compile error too, the same B3 violation through the read path. | **done** |
| **`rotatePages` — the first command, end to end.** Capture and apply are separate exports because the bus captures prior state before `apply` (ADR-0009, 2026-08-19), and `apply` is written so it does not consume what the inverse needs. Capture reads **own-state** — `get('Rotate')`, not `getInheritable` — so a page that inherited reports `{ present: false }` and its inverse will be a delete; measured against MuPDF rather than read from the declarations, on a real inherited fixture. Raw values are captured **verbatim**: `45`, `-90` and `450` come back unchanged. Forward rotation takes the **inheritable** value and snaps it the way the engine snaps it — a port of `pdf_page_transform_box`, where `+45` rounds a half-way value up, so `45` renders as `90` and one quarter turn from it is `180` and not `135`. That port is checked **engine-to-engine**: the port's answer is fed back to MuPDF and the two page transforms compared over 23 raw values, with a control proving the transform separates all four turns (page bounds would collapse 0 with 180). Round trip read back by `@cantoo/pdf-lib`, not by the engine that wrote it. Refusals leave the document untouched: an out-of-range index is validated before the first write, a forged session never reaches a page, and and a non-numeric `/Rotate` returns **not-captured rather than throwing** — an ordinary outcome the bus answers with a checkpoint, so the user still gets their rotation on a document every other reader opens. Coercing it, or treating it as *absent*, were both rejected: the second is worse, since undo would then delete a key that was present. An invalid **command** still throws, because a bad page index is the caller getting it wrong and must not be hidden behind a byte snapshot. **The inverse restores prior state and is the first thing to exercise §3**: `{ present: false }` → **`delete('Rotate')`**, so a page that inherited inherits again rather than declaring what it used to inherit; `{ present: true, raw }` → the raw value **unnormalised**, so `45` and `-90` come back as they arrived. `Invert` **cannot see the command** — a signature that could would let the inverse be computed from intent, which reaches neither branch — and supplying one is a compile error, not a convention. Six mutations confirmed red, each separated from its control, and one of them is the demonstration §3 rests on: writing the value back instead of deleting reddens the structural assertion while **every rendering-based check stays green**, including the round trip through `@cantoo/pdf-lib`. **The kernel half only** — this legend's `done` requires a UI dispatch test as well, and no renderer exists to dispatch from. | **kernel done, unwired** |
| **The command log, and the one path to an entry** ([ADR-0009](DECISIONS/0009-document-identity-and-the-command-log.md) §4). A **cursor, not a stack**: undo steps back and never pops, redo steps forward over the same entry, a new command truncates the redo tail — kept would let redo replay against a document that has since diverged. Two entry shapes, and the wrong ones are **unrepresentable**: invertible-with-a-checkpoint and terminal-without-one are both compile errors. `Checkpoint` is **branded with its only mint module-private to the bus**, so §4's *"never by a handler"* is visibility rather than a comment — three doors shut, since a live-session `apply` returns void and `capture` returns a result that cannot carry one. The bus **captures, then checkpoints only if it must, then applies, then records**: nothing is snapshotted speculatively, an execution that throws records nothing and bumps nothing, and the checkpoint is proven to hold the *pre-command* document rather than merely to exist. **The terminal branch has a real constructor** — a page whose `/Rotate` is a name — so it ships exercised rather than present. Six mutations confirmed red, each separated from its control, including one that found a test asserting a failing *apply* while only reaching a failing *capture*. **Undo applies the inverse and redo re-applies**, both bumping the version (§5) since both are applied mutations. Which redo path is taken is **§3a's declaration doing work for the first time**, and as a compile-time trigger rather than an unreachable branch: every command replays by re-running today, so a runtime check could not fire — instead an assignment stops compiling the day any spec declares `replay: 'stored-effect'`, verified by making it fire. **The bus is stateless and application-wide; the log lives on the document's record beside the lane** (ADR-0009's composition decision). Both rejected shapes are recorded with the failure that rules them out: a log on an application-wide bus is one log across every open document, so undo on one walks another's entries; a `Map<DocId, log>` is get-or-create and mints a log for a closed `DocId`. On the record, the log's lifetime **is** the record's — dropped on close by construction, with every checkpoint in it. Asserted **against the service rather than a context stub**, because a control that reads a double proves the double is right: a mutation making every document share one log passes every bus test and reddens both service tests. The counter and the log share one capability, since B3 is about one *component* being permitted to write; the readable view has no mutators, proven by a compile-fail case, with a control that querying the log needs no capability at all. **Not yet done, and one of these is a live gap rather than a future unit:** checkpoint bytes are **already retained, unbounded** — every terminal entry holds a full byte image and `CommandLog` has no cap, so `ARCHITECTURE` §4's *"one document plus a few checkpoints"* is a budget nothing enforces. *(Corrected 2026-08-21: this said sizing it was "one decision … shared with `DocumentService` holding canonical bytes". It is **not**, and the two are deliberately not merged — the canonical image is decided in principle by §2 and needed only sizing, which [ADR-0021](DECISIONS/0021-the-canonical-image-is-retained.md) did; checkpoint retention and spill are deferred by ADR-0009's Left open to the Stage 0 performance gate, and §4's "every N commands" still has no N. Merging them would either block the image on a gate it does not need or settle checkpoint policy without the gate meant to settle it.)* Also owed: restoring from a checkpoint, refused by name because it means opening a *new* session from those bytes and who then owns the old one is `DocumentService`'s question; Restoring from a checkpoint is refused by name because it means opening a *new* session from those bytes and who then owns the old one is `DocumentService`'s question. **`bumpVersion` is now narrowed by capability**: it takes a `VersionWriter` whose only production mint is module-private to `commandBus.ts`, so a lane entry cannot bump — proven by a compile-fail case, with a control that it still compiles for a holder, since "narrowed to one writer" and "removed" are different claims. `markSaved` deliberately keeps no token: its writer of record is the save pipeline, which does not exist, and a token with no minter is a method nobody can call. | **partly done** |
| **Errors crossing to the renderer carry no filesystem path** ([ADR-0009](DECISIONS/0009-document-identity-and-the-command-log.md) §9, invariant L2). `readFileIdentity` rethrows every errno that is not `ENOENT`/`ENOTDIR`, which is correct — the kernel throws honestly and the boundary sanitises — but a rethrown `EACCES` is a Node `fs` error carrying the absolute path in both `.message` and `.path`. Nothing leaked while no IPC handler called `DocumentService`, so the expiry was mechanical rather than remembered: `kernel-error-path-sanitisation` in `docs/security/engine-advisories.json` turned `check:advisories` red the day `DocumentService` or `readFileIdentity` was named from `apps/*/src/**`. **It fired for real on 2026-08-19**, naming both new files, and its successor verdict `renderer-facing-errors-carry-no-text` now rests on the type instead of on the absence of a handler. That trigger caught "a handler reached the kernel"; this row is "and its errors were sanitised" — and the trap is that the mechanism **looks finished**: `wrapHandler` converts throws in one place, which is structurally what §9 asks for, so the trigger fires and a developer finds an error boundary already built. It is not sanitised. `toStructuredError` copies `message`, copies `stack`, and **recurses into `cause` with itself**; measured, a rethrown `EPERM` reads `EPERM: operation not permitted, stat '<absolute path>'` with the same path in the stack. ADR-0009's 2026-08-19 decision settles the shape: **two objects, not one sanitised object** — `StructuredError` keeps everything and stops crossing, and the renderer-facing failure is a closed union of codes with typed fields, no `message`, no `stack`, no `cause`, text looked up by code. A type rather than a filter, because any `message: string` can carry a path (B5). **Built.** `Failure` carries no `message`, no `stack` and no `cause`; `Result`'s error position defaults to it; `toError` and `unwrap` are **deleted**, since there is no message to rebuild an `Error` from and `remoteStack` was a field whose whole content was absolute paths. Codes are declared per channel, `internal` is implicit, and an undeclared code is a compile error **and** refused at runtime for the case the type cannot see. **Two shapes, not one** (ADR-0009, 2026-08-19): a declared code travels alone and `internal` travels with the incident id, so an id accompanies exactly the failures that **withheld** something. Writing the first handler is what asked the question — `wrapHandler` hands a handler its params and nothing else, so a declared failure had no source for the id the type demanded, and the only instances in the tree were fixtures inventing `'i0'`. A fabricated id points at no log line, and a second handler-side log would collide with the boundary's counter, which starts at zero in both. A handler therefore returns `DeclaredFailure` — declared codes only, no id, and **not** `internal`, which `channel.ts` had asserted in prose while the type permitted it. The wire schema is a union of two `.strict()` shapes made **disjoint by a refinement rather than by member order**, because a union tries members in turn and `{ code: 'internal' }` with no id would otherwise parse cleanly as a bare code — an unreportable failure arriving as a reportable one. Six controls, each mutation-tested to redden alone: a declared failure carrying an id is refused **main-side** (the type cannot catch it — `err()` infers its argument, so an excess field survives assignment), a handler reporting `internal` becomes a real internal whose id resolves, both wire shapes are refused in both wrong directions, and a **positive control that the well-formed shapes get through**, without which "rejects exactly these" and "rejects everything" are the same result. The diagnostic goes to an `IncidentSink` the composer must supply — **required, not defaulted**, after the module graph refused a `console.error` in `packages/contract` and was right to. The control asserts `message`, `stack` and a **nested** `cause` separately, with its own control that the path **is** in all three main-side, so the fixture cannot be a path-free error passing vacuously. `failureSchema` is `.strict()`, so an envelope carrying a `message` from a drifted main build is rejected rather than ignored. And everything that crosses is asserted to survive `structuredClone` deep-equal, which is the hard shape in-process tests cannot see (audit item 2) — with a control that the clone genuinely refuses a function and silently drops a class method. **Now evidenced at a real handler, not only at a fixture:** the same pair of assertions runs against `executeCommandHandler` through the real `wrapHandler`, with an error carrying an absolute path in `message`, `stack` and a nested `cause`. Mutation-tested **in the direction that separates** — removing the path from the fixture reddens the control alone, while the "no path crosses" case stays green, which is exactly why the control is there. **Still owed:** the renderer's code → i18n key mapping, with the first UI. | **kernel done, unwired** |
| **The composition point, and the first IPC handler** ([ADR-0009](DECISIONS/0009-document-identity-and-the-command-log.md), 2026-08-19). One place owns `DocumentService.run → CommandBus.execute`, because a handler that assembles it itself is a handler that can forget the lane — and a **second place where a feature is wired**, which is what the command registry exists to forbid. It lives under `apps/desktop/src` and imports no Electron, and that location is a constraint rather than a convenience: the reachability trigger scans that glob and only that glob, so handlers anywhere else would leave the verdict green through the whole unit it was armed for. **Everything about the ordering is load-bearing and is proven by a control whose failure is invisible in the document's final state:** two concurrent rotations leave the page at 270 either way, so the evidence is the second entry's *inverse* — serialised it records the state the first command produced, interleaved both entries record the pre-command state and undoing twice would leave the document somewhere it had never been. Mutation-tested by making the lane stop serialising, which reddens that case alone. The session is resolved **inside** the lane, so a session torn down between queueing and running is a miss; the version returned is the one the lane stamps **after** the work, not the one it handed in. `Executed` is dropped at the boundary — it carries the log entry, and an entry holds an inverse or a checkpoint, which is a whole byte image (L11). **What it deliberately does not own is engine session lifetime**: sessions are looked up **get-or-miss, never get-or-create**, and a miss is a defect rather than an outcome. *(Corrected 2026-08-21: this said who opens a session was "§8's open question held behind `kernel-holds-canonical-bytes`". It is **not open** — §2 and §3.2 state that `DocumentService` owns canonical bytes and lazily-created engine handles, and the architecture document outranks an ADR wherever they diverge. What was open was the policy, now [ADR-0021](DECISIONS/0021-the-canonical-image-is-retained.md). The sentence widened one source's silence into the project's.)* What the composition point still does not own is the **process** the session lives in: §2's diagram puts MuPDF in a utility host and says *"NO in-main fallback — native faults are uncatchable"*, so a session opened in `main` is forbidden by invariant 20 and by §9.17's argument for `main`'s budget both. The channel declares three failure codes and all three are **outcomes**: `document-not-open` (a document closed while a command was in flight — L10's lookup miss, by design), `document-busy`, and `document-poisoned` (2026-08-26, [ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 9a — the supervisor stopped rebuilding this document's engine session, which is a decision it made and not an inconsistency it found). Both are matched on the **error class**, never on the message, which is why `DocumentNotOpenError` exists. Everything else — a missing session, an unregistered writer, an engine throw — is rethrown untouched and becomes `internal` with the diagnostic recorded main-side. **The union-dispatch shape is NOT verified and says so in the code:** with one command kind the wire union *is* one member, so every correlated lookup resolves concretely; with two, a caller holding the union cannot correlate the session it selects with the `apply` that receives it. That is a compile-time trigger (`[CommandKind] extends ['rotatePages']`), the same mechanism `redo` uses for `stored-effect`, so it arrives when the path becomes reachable and not before. **Still owed:** the UI-level dispatch test this legend's `done` requires, impossible until a renderer exists. | **kernel done, unwired** |
| **The composition root, and the app launching for the first time** ([ADR-0021](DECISIONS/0021-the-canonical-image-is-retained.md), 2026-08-21). `createShellDependencies` is the one place that builds the object graph — registry, service, bus, commands, handlers, both sinks — and it **decides nothing**: §2 fixes what `DocumentService` owns, §3 the writer of record, ADR-0009 the lane and the routing, ADR-0021 the retention policy. It imports no Electron, so the whole graph is constructible without a runtime; `entry.ts` is two lines and is the only file that both builds and starts. `package.json`'s `main` names the entry and `exports` names the module surface, deliberately kept apart — pointing both at the entry would make *importing the package launch the application*, which is the shape that had a unit test downloading Electron. The document ceiling is **derived from §9.17, not typed**: `proof:composition` recomputes `absolute cap − declared baseline` from the invariant and fails when the constant differs, with a control that a ceiling forgetting the baseline is rejected. **And the root is proven to RUN, because one known only to compile is one nobody has executed** — twice found this week. `proof:shell` spawns Electron on a harness calling the same two functions `entry.ts` calls, and the **page** invokes `app.info` across the real bridge, the real `ipcMain` registration and the real handler; mutation-tested by changing the handler's value. `installChannel` is asserted rather than `version`, because it comes from this repository's code while `app.getVersion()` reports whatever `package.json` Electron was started from. **The engine has no session and that is the law, not an omission** — and the cost is smaller than predicted, measured rather than argued: `document.execute` returns the **declared** `document-not-open`, not `internal`, because opening a document is not a channel, so the renderer cannot construct an input that reaches the missing-session path. **Owed:** the `mupdf-host` utility process, which is what makes a session possible at all. | **done** |
| Both utility hosts on the shared worker contract | — |
| **The engine host's runtime loop: bytes in, dispatch, bytes out** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) §4). The join between two things that already existed with nothing between them — `frame.ts`, which turns a byte stream into messages and refuses a hostile one, and `wrapHandler`, which validates a call against its channel declaration. It adds **no validation of its own above the envelope**: params are checked by the channel's schema in the same wrapper the renderer path uses, which is XX-1's correction followed rather than a second discipline written (B3a). The request envelope is declared once in `packages/contract` for both ends, and the correlation id sits **beside** `envelopeSchema(channel.result)` rather than inside it, so a client parses the same object over this pipe as over Electron IPC. `params` is required and `unknown`: the peer must send the field, and what may be in it is the channel's question. **Every protocol violation is terminal, and there is no error reply** — resynchronising means guessing where the next message starts, which is the peer choosing our parse offsets, and a reply composed in answer to bytes we could not understand is a parser that recovered when it should have refused. Seven terminal codes, each separated from a *declared channel failure*, which is an answer the connection survives. `unsendable-response` is deliberately named for **our** defect rather than the peer's, because a channel whose declared result can exceed the frame maximum sends the reader to the wrong side of the pipe. Two guards the compiler argued against and lost: the dispatch table is a `Map` because the mapped type claims every key is present, which is true of the registry and says nothing about the string a hostile peer sent; and the stop flag is read through a call because narrowing survives one, and the lint rule reporting an "unnecessary" re-check inside the frame loop was reporting the compiler's unsoundness. **Not done:** the transport. No socket, no pipe, no `net` — the caller hands it chunks and somewhere to write, and the Win32 pipe belongs to a factory in `apps/desktop/`, whose **ordering half now exists** — `enginePipeFactory.ts`, the same split as `engineHostFactory.ts`: an injected `PipeCreationSurface`, a descriptor assembled from two branded SIDs so a caller cannot supply DACL text, every instance or none with the partial set closed, and the descriptor freed once on both paths. Its adapter exists too — `win32PipeSurface.ts`, B7's second sanctioned native boundary — and `lowboxSpike.mjs` **drives both**: the row that measures the shipped descriptor against a contained cell on three Windows builds is now built by the shipped factory rather than by a copy of it, so the spike's own pipe creation is gone. The transport's **ordering and lifetime** exist as well — `hostTransport.ts`, over an injected `ReaderChannel`, six cases: an ending has two causes (`terminate` is us, the reader going away is the host) and they are never one field, the **first** cause wins so a later `terminate` cannot relabel a dead host as a clean shutdown, `stop` is signalled only when this call is the ending, and everything after an ending is dropped in both directions. What does **not** exist is the I/O behind that channel, though its **shape is decided and measured** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) §4's 2026-08-25 addition and its decision the same day), and the two directions are **not** the same mechanism. **Reads: a worker thread** waiting over the operation's completion event **and** a stop event, never a thread blocking inside `ReadFile`. **Writes: main issues them itself** and reaps the completions on the next write and on `terminate` — because a waiting reader cannot be told anything (`postMessage` is not delivered until its wait returns, measured), and of the three remaining mechanisms this is the only one with nothing to tear down and no third native boundary. Its premise is measured rather than argued: 64 writes into a peer that never reads, slowest **1ms**, **63** genuinely outstanding when reaped, and the stream delivered **in issue order** byte for byte — which is what a length-prefixed framing on a byte-mode pipe requires. The write side's **ordering and bound now exist** — `hostWriteQueue.ts`, over an injected `OverlappedWriteSurface`, eleven cases: completions are collected on the next write and on `close` and by nothing else, the collect walks every outstanding write rather than stopping at the first still pending (stopping early would spend the issue-order measurement inside the one module that has no Win32 facts in it), **every refusal is terminal** — a frame the peer never saw leaves the next length prefix landing in the wrong place, so there is no resuming, which is Decision 8's shape one layer down — and the limit is required and undefaulted, as the job's `ProcessMemoryLimit` is. Its load-bearing case is a **control**: the limit counts what is *outstanding*, not how many frames were written, and a queue that counted totals passes every assertion in the overrun case because nothing completes in it. `hostTransport.ts` takes the queue beside the channel, ends on a refused write as the **peer** — the host stopping consumption is the cause and it is a defect to report, even though main is what saw it — and signals the stop event there, because unlike a dead reader that thread is still in its wait. **And the write side's teardown is measured before the adapter exists** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) §4's second 2026-08-25 addition): `CancelIoEx(handle, NULL)` with **63 of 64** writes outstanding, after which every one becomes collectable in **0ms** reporting `ERROR_OPERATION_ABORTED`. Two facts the adapter turns on came from the resolution tests rather than from the reading — Windows answers `ERROR_NOT_FOUND` when there was nothing to cancel, so a false return is not automatically a failure; and a wait after a cancel that did **not** happen never returns, measured at an external `timeout 25` and exit **124**. So `abandon` waits only after a cancel it has seen succeed, and the probe polls rather than waits, because a probe that hangs in CI is a job timeout instead of a named failure. **The write adapter now exists too** — `createWin32WriteSurface` in `win32PipeSurface.ts`, on the same native boundary as creating the pipe, because B7 asks for one adapter per boundary rather than one per verb. It holds each issued write in a **strong** map keyed by the queue's token, deliberately not a `WeakMap`: a queue that lost a token would otherwise have its buffers collected out from under the kernel, and that failure is corruption at a random later moment, where the same bug against a strong map is a bounded leak somebody can see. **And it is RUN, not merely typed** — `scripts/research/transportWriteSurface.mjs`, nine cases on both containment jobs, drives the built `createWin32WriteSurface` and `createHostWriteQueue`: 32 frames into a peer that is not reading, **131072 bytes delivered in issue order**, the bound refusing the frame past 8 outstanding and closing rather than dropping it, and a teardown that freed **31** outstanding writes in **0ms** with none stranded. Its load-bearing case is the bound's **control** — 16 frames pass a limit of 8 while the peer *is* draining — because a limit counting total writes passes the overrun case, where nothing completes. Two of its mutations did **not** bite and are recorded at the constants rather than acted on: the `OVERLAPPED`'s completion event is read by nothing (this design only polls) and is kept so that a per-write wait added later cannot be silently wrong; and overwriting the source buffers before the peer reads them cannot tell the adapter's copy from a view, because the named-pipe file system takes the bytes at request time. **THE READER WORKER NOW EXISTS** — `packages/nodemode/src/readerWorker.ts`, in that package because it runs in Node mode, with the stop event and the handle addresses supplied by `createWin32ReaderControl` in `win32PipeSurface.ts` (the same native boundary as creating the pipe). **`proof:readerworker` closes CCCC-2**, which was that `proof:teardown` measures termination and nothing about bytes crossing, so a reader that ended cleanly having read nothing satisfied it: eight cases drive the shipped reader over a pipe from the shipped factory — **8192 bytes delivered in write order across 3 chunks**, and the thread ending **15ms** after the stop event was signalled while waiting for bytes. Its load-bearing case is that they arrived in **more than one chunk**: written as a single batch first, all sixteen frames landed in one read and the loop's second iteration — where a transport spends its life — had never executed while every other case passed. Mutated to the rejected one-handle wait, the stop cases go red at **2006ms with the thread alive** while the delivery cases stay green. **And a chunk weighs more than it carries:** posting `buffer.subarray(0, n)` left every case green, because `postMessage` clones synchronously and the aliasing hazard it looks like is not the one that bites — what bites is that cloning a TypedArray clones its **entire** underlying `ArrayBuffer`, measured at `byteLength` 512 against `buffer.byteLength` 65536. Finding that mechanism turned a mutation nothing could catch into a case that catches it. Nothing is sent to the reader and it registers no `parentPort` listener; main says *stop* by signalling an event the wait already watches. **And the `ReaderChannel` factory now exists** — `engineReaderChannel.ts`, the ordering over an injected `ReaderHostSurface`, twelve cases. The stop event is created **before** the worker and its absence is a refusal rather than a worker started with a null, because a reader with no stop address is one nothing can stop and the only remedy left is killing the thread. A worker that cannot start closes the event on the way out — a refusal that leaked a handle would cost something every time it was taken, on the path least likely to be exercised. An ending is forwarded **once, from whichever source spoke first**: the reader has three ways to stop being useful and all three arrive separately, so a thread that threw produces an error *and* an exit, and **an exit with nothing said before it is still an ending** — a reader that vanished without a word is a dead host, and silence is exactly what a missing case would render as nothing. `stop()` signals the event and does **not** terminate: a terminate there would mask a reader that could not be stopped, which is the failure the two-handle wait exists to make impossible, and `dispose()` terminates only what is still running so nobody can later conclude the terminate is what stops it. **Its adapter exists too** — `readerHostSurface.ts`, which composes the Win32 event calls with starting a `worker_threads` Worker (two things that are not the same boundary, which is why it is not inside the koffi module), resolving the reader **through the package** rather than by walking out of its own `dist`. **And `proof:readerworker` now drives the shipped COMPOSITION**, not the worker in isolation: ten cases, with the channel creating the stop event and starting the reader. Its two new controls are end-to-end versions of what the unit test asserts against a fake — **the stop EVENT ended the reader and no terminate was ever called**, and disposing an already-ended reader terminates nothing. Mutated by making `stop()` also terminate, **three** cases go red and the third is the one worth having: the reader never gets to post its own ending, so the diagnostic becomes *exited with code 1* instead of *stopped while waiting for bytes*. Killing the thread does not merely prove nothing about the two-handle wait; it destroys the only account of what happened. **And main's half of the protocol now exists** — `packages/kernel/src/host/client.ts`, fourteen cases: a call goes out as a framed request carrying a correlation id, and answers resolve their own promise in whatever order the host finishes. It adds **no validation above the envelope**, because `createClient` already parses each answer against `envelopeSchema(channel.result)` and a second parse would be a second opinion about what a channel returns. **Every protocol violation is terminal on this side too** — a response for an id nobody sent, an id answered twice, bytes that are not a response — because the only alternative to stopping is guessing which of our own calls it meant. **There is no timeout, deliberately**: a slow host and a dead one are different facts and only the transport can tell them apart, so a timeout here would invent the distinction from a duration. **A dead host rejects rather than leaving promises pending** — the quiet failure that costs most — and `HostTermination` gained `malformed-response` and `unknown-correlation`, one vocabulary for both ends because `terminate` takes this type and the client calls it over the same transport. Its load-bearing case is the one no other reaches: **a transport that answers synchronously inside `write`**, which the pending entry being registered *before* the write is what survives — written the other way round, the answer arrives at an empty map and is reported as an unknown correlation, a violation manufactured by the order of two lines. **AND THE COMPOSITION POINT NOW EXISTS** — `engineHostConnection.ts`, thirteen cases: one call that creates a pipe, puts a reader thread on it, bounds the writes, gives both directions one lifetime, turns that into an `invoke`, and starts the contained process — in that order, with a teardown that undoes exactly what was built. **The host is created LAST for two mechanisms rather than a taste:** the reader is what issues `ConnectNamedPipe`, so a server instance nobody has connected cannot be read; and the host is the only stage that creates something outside this process, so it is the only one that can fail *after* it succeeds — creating it last means every earlier refusal frees everything with no process having existed, which is the property `createContainedHost` already has internally, one layer up. **`close()` does nothing but terminate the transport**, so a deliberate close takes the same path a dead reader takes and there is exactly one function that knows what to free and in what order; `hostTransport.ts` guarantees that sink runs once, so it needs no re-entry guard of its own. Three properties are decisions the first implementation would otherwise have settled silently: callers are **settled before handles are freed**, since a promise nobody settles is the failure that costs most; the ending is reported **after** everything is freed, so a caller may rebuild inside it without racing this module; and a connection that never started reports its failure **once**, through the `Result`, because calling `onEnded` as well would report one failure as a refusal *and* as a death. **`TransportEnd.by` cannot answer which ending it is** — the client calls `terminate` for a protocol violation, so `us` covers a deliberate close and a violation alike — and the mapping reads the client's own termination first, which is what separates them; by the time `us` arrives with nothing raised, `close()` is the only thing that can have caused it. Its load-bearing case is a **control on the fixture, not on the code**: a well-formed response for an id nobody sent, because a malformed one is refused before the correlation is ever looked up, so the case would pass on `malformed-response` and prove a different thing than its name claims — measured, that is what the first version of that fixture did. Mutated: calling `hostFor` twice reddens the once-only case alone (the first draft did exactly that, and the teardown would have run through a different adapter from the one that created the process); mapping a dead peer to `shutdown` reddens three; reporting an ending for a host that never started reddens the host-refusal case; and dropping the explicit `terminate` reddens three, which is the **vacuity guard working** — every order assertion here goes through a helper that requires both calls to be present, since `indexOf` answers `-1` for a call that never ran and `-1 < n` is true, so an absent step would otherwise pass every order case in the file. **What is still owed:** nothing *calls* this yet, and the call has no consumer until a remote engine writer exists — `mupdfWriter.ts` holds the native document **in this process**, so a session over the pipe is Decision 7's handle protocol and not this row. **Its PLACEMENT was settled by measurement rather than by reading ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) §4's third 2026-08-25 addition). `proof:workermode` drives a harness under the pinned Electron binary and reports that a `worker_threads` Worker inside main has `process.versions.electron` set and `process.type` **undefined**, and that `import('electron')` there yields a module carrying **no `app`** — while main's control in the same run carries one. So a worker is **Node mode**, and this is the **fourth** failure of the `apps/desktop/src/` proxy and the quietest: the other three broke at the import, this one succeeds and fails later at the first property access, where nothing points back at it. The body therefore lives outside `desktop` and the factory that spawns it inside — the same split ADR-0022 made for the host. Windows only, because Electron hangs without a display server on Linux rather than failing. **Which package is settled by [ADR-0024](DECISIONS/0024-execution-mode-is-a-placement-axis.md), a B4 amendment taken before the worker was written**: execution mode is now a placement axis in ARCHITECTURE §1, and `packages/nodemode` is the Node-mode side for anything whose subject is not the document engine. `packages/kernel` was rejected on **subject** rather than on mode — a Windows-only reader there breaks §1's own reason for the kernel's Electron-free property, which is that the document pipeline stays unit-testable in milliseconds. The engine host body is unmoved. `proof:teardown` runs it on both Windows containment jobs, which assert a **bound** — the reader exits within 2000ms and with code 0 at each of its two waits. The single-digit-millisecond figures in the ADR are the developing machine's and are labelled there; a job's log is not readable without owner authentication, so CI contributes a bound rather than a measurement. The rejected shape, measured by mutating the wait to one handle, wedges until that budget expires with the thread alive. Two things about that pipe are now measured rather than assumed ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) §4's two 2026-08-24 corrections): its DACL must name this user **and** the container SID, because an AppContainer's access check is conjunctive and the container's ACE alone refuses the contained host — with the container's mask `0x0012019B` rather than `GA`, since `GENERIC_ALL` maps to `FILE_ALL_ACCESS` and would grant the hostile principal `WRITE_DAC` over its own trust boundary and `FILE_CREATE_PIPE_INSTANCE` over the channel (demonstrated: on a pipe still carrying `GA` the contained cell opens it for `WRITE_DAC` and succeeds); and the pipe **cannot be handed to Node** — `node.exe` links its CRT statically, so an fd minted by any DLL an FFI can reach answers `EBADF` in node's own runtime, and the overlapped reads and writes are therefore ours to issue through the Win32 surface rather than Node's — the worker doing the reads, main issuing the writes — feeding bytes to this loop. That is the shape this file already has, which is why the correction changes the factory's size and not the seam. Nor does the client half that turns this into an `invoke`. **A handler already running when the loop stops still finishes**, which is stated rather than hidden: this loop cannot cancel a promise it did not create, and the property it does have is that the answer never reaches the pipe. | **kernel done, unwired** |
| **Every reachability symbol is derived, witnessed, or printed as unverifiable** (audit finding T-1). A control per path glob proves the walk *reads files*; it says nothing about whether the string searched for could ever match. Measured: two symbols misspelt, `check:advisories` exits 0, `18 symbol(s) checked` unchanged — the count counted declarations. Three states now, and the summary prints **two numbers, never one**: *derived* (the OCR doors, computed from the engine source and cross-checked, with coverage taken from what the derivation actually confirmed rather than from a declaration), *witnessed* (found on every run in a scope **disjoint** from the paths the verdict scans, and never the register itself — a misspelling is present there too and would find its own typo), and *unverifiable*, counted apart because "could not look" must not render as "looked and found nothing". `in: null` is not an author's exemption: it is accepted only while an `acceptedWhile` condition the register resolves itself still holds, so the day Electron becomes a dependency the null expires with no second mechanism. Nine proof cases including the T-1 mutation verbatim, a non-zero-verified control, and a resolution case asserting both counts move by one — the last reddens on exactly the mutation that made T-1 invisible. | **done** |
| **A failed job assignment kills the process, never resumes it** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 8). The **ordering**, over an injected Win32 surface: create suspended, create the job, apply the limits, assign, **read `IsProcessInJob`**, and only then resume. Membership is read rather than inferred from `AssignProcessToJobObject`'s boolean — a call returning success while the process is not in the job is the `available: true` shape at the kernel boundary — and the read has **three** answers, because `IsProcessInJob` can itself fail: *could not read* terminates exactly as *not in job* does, since could-not-look is not looked-and-found-it. WW-1's matrix is why this is a requirement rather than tidiness: the **job** is what delivers invariant 25(b) everywhere, so a host with the assignment failed has (c) and (d), lacks (b), and answers yes to every cheap containment question including `classifyContainment`. **The container's own refusal is BUILD-DEPENDENT, measured 2026-08-23:** a contained host with no job of ours spawns freely on Windows 11 and is refused `EPERM` on `windows-latest`. That makes this requirement stronger rather than weaker — a mechanism present on some builds and absent on others is precisely one you cannot rely on, and unlike *it is absent* that cannot be dismissed by testing a single machine. The suspend count is checked too, which Decision 8 implies and does not state: `ResumeThread` reporting a previous count other than 1 means the thread was already running, so it executed before the job was assigned. Every refusal terminates **before** closing any handle — relying on `KILL_ON_JOB_CLOSE` makes the kill a side effect of tidying up, which stops being true in exactly the case where the assignment is what failed. The job's `ProcessMemoryLimit` is required and undefaulted, and is §9.17's absolute cap with **nothing subtracted** — the opposite arithmetic to `MAIN_DOCUMENT_BYTES_CEILING`, from the same line, because a job bounds the process commit rather than document bytes; `proof:composition` recomputes both. **The Win32 surface now exists** (`apps/desktop/src/win32HostSurface.ts`, 2026-08-23) and its shape was not invented — every member is a call `scripts/research/lowboxSpike.mjs` already makes, in that order, on every cell of every run. **It carries no `any`**, which B7 permits at a native boundary but does not ask for: koffi's `func()` returns a callable accepting anything, so each entry point is declared with the signature from the C prototype on the adjacent line. Four returns are deliberately left `unknown` rather than `boolean` — process creation, the limits, the assignment and the membership read — so the compiler *requires* a comparison against `true` and a marshalling surprise cannot read as success at a `!x` test; typing them would be the adapter deciding a security question. `isInvalidHandle` is a **copy** of `scripts/lib/win32Handle.mjs`, which this package cannot import, and MMM-1's rule applies: `proof:win32handle` drives the owner with an identity address reader and requires the two to agree on every value, with a control that the comparison can see a disagreement. **And it RUNS** — `scripts/research/hostSurfaceProbe.mjs` creates two real processes through the shipped surface, differing in one variable: the child under the job is refused a spawn while the same child with no job of ours spawns freely, so invariant 25(b) is obtained through shipped code rather than through the spike. Resolution-tested by removing `ActiveProcessLimit`, which turned the verdict `UNREADABLE` rather than leaving it green. **Not done:** nothing creates a host in the application — no composition root calls the factory, which is `engine-host-factory-wired`'s trigger. *(Sharpened 2026-08-25: the factory now has ONE caller, `engineHostConnection.ts`, which composes it with the pipe, the reader and the transport. The trigger is unchanged, because that caller is itself uncalled — `composition.ts` still supplies a `SessionLookup` that always misses. What changed is that the gap is now one wiring step rather than four missing links.)* **And that is the LAST of four links, not the next one** (2026-08-24): calling the factory produces a *process*, and a `SessionLookup` needs a transport, then the client half that turns it into an `invoke`, then session lifetime, before the call has a consumer. Verified rather than read off the row above — a search for `CreateNamedPipe`, `createServer`, `net.` and named-pipe spellings across `apps/desktop/src`, `packages/kernel/src` and `packages/contract/src` returns **nothing**. The transport is the buildable unit and it **registers** into an interface that already exists, `HostRuntimeTransport` in `packages/kernel/src/host/runtime.ts`, so it needs no amendment. **What the host unblocks is TWO rows and one owed clause, not five:** this row, the runtime-loop row above, and row 280's owed `mupdf-host`. The other three `unwired` rows are blocked on a **renderer** and say so in their own last clauses — a UI dispatch test, a UI dispatch test, and the code → i18n mapping. Recorded because the five-row figure was spoken and never written, and an uncorrected count outlives the conversation it came from. Neither probe cell is contained (`containerName` is `null`), so (a), (c) and (d) are still measured only on the spike. **KKK-1 is discharged:** the surface names the four watched Win32 symbols as genuine uses, four verdicts expired on schedule, and the re-triage the ruling deferred to this moment is done — the absence inputs are removed, because a symbol that cannot be absent is not an absence claim. **Session lifetime is DECIDED and not yet built** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 9, 2026-08-25). It needed no amendment, and both premises were checked in the documents rather than read off a summary: §2 makes engine handles "a cache that can be thrown away and rebuilt, not the document", and Decision 8's subject is a host created with the assignment already failed — two of three *at birth* — so its "never resume" governs creation and reaches no properly contained host that later dies. **But *handles are a cache* settles only that a dead host MAY be rebuilt**, and three questions it leaves open are decided before the supervisor exists rather than by whatever the first implementation happens to do. **(1) The rebuild is bounded per document**: a consecutive-failure count per `DocId`, incremented by a death that rejected one of that document's calls, **reset to zero by any call the host answers**, poisoning at **2** — because the only thing a retry tells you is whether the failure is deterministic, and one retry tells you that. Reset-on-success is what makes the plain counter usable, since one host per engine means a death rejects calls for documents that had nothing to do with it, and any call the host later answers for such a document puts it back to zero. **It does not make the counter innocent, and the residual is stated rather than implied** (finding DDDD-17): a document with a call **in flight at two successive deaths** caused by a third document's bytes reaches 2 having caused neither, because it never got the success that would have reset it. The window is narrow — an idle document is never incremented — and it stays open because every mechanism that would close it is the **attribution** 9a rejected as evadable by concurrency. **RECOVERY is what repairs it, and recovery needs no new mechanism** (finding DDDD-16): the count and the poison live on the supervisor's per-document state, whose lifetime is the record's — the same rule the lane and the command log already follow — and `DocId` is **minted, never derived** (ADR-0009; `documentService.ts` mints 256 random bits inside `#openNow`, rejecting a reusable counter by name because a reused id lands a late message on a different document, invariant L10). So closing and reopening starts a fresh `DocId` at zero, and *never, for the life of the process* is **unrepresentable** rather than rejected — it has no key to live on. Two candidates are rejected with mechanisms: resetting on the next successful **host build** would clear the count of exactly the document that kills the next host, which is the loop restored wearing a repair's clothes; and a dedicated **"try again"** affordance would put a human inside the loop and make the second attempt cheaper than the first. **The two findings answer each other, and that is the load-bearing part:** recovery was written up as a convenience — the reason refusing beats closing the record — and admitting the innocent case makes it a **requirement**, since without it a document that caused nothing would be permanently refused engine work for someone else's input. A poisoned document is refused with a **declared** code rather than `internal` — the supervisor decided it, so it is an outcome and not an inconsistency — and it stays open, which is why refusing beats closing the record and discarding an intact command log to report a fault in neither. **The reason for that is STRANDING versus DESTRUCTION, and it is not that the document can still be saved** (finding DDDD-18): this row previously asserted it stays "saveable, because main holds canonical bytes and never parses", and the cited premise is what makes that false — invariant 17 is *never parses*, a save is a parse-and-write walking the xref (§4), so **a save is engine work** and a poisoned document has no session. Main unaided can write the **last-saved bytes** back out, which is a copy of the original rather than the user's work; the work is the command log, and invariant 22 puts its application — as re-executed intent or re-applied effect — equally in the engine. So a poisoned document's edits are **stranded**: intact in main, unappliable for now. Refusing keeps every later resolution reachable and closing forecloses all of them, which is the argument, and it needs no claim about saving. **OPEN, AND OWED BEFORE THE SAVE PIPELINE RATHER THAN BEFORE THE SUPERVISOR — this is the row's expiry trigger:** what happens to work that exists only as a command log when the engine is permanently refused for one document is **invariant 18's** question (*"a failed save never loses work … never by a dialog whose only option discards their edits"*), and settling it is a **B4 amendment**. **The candidate that leaves L18 intact — letting the log outlive the record so a reopen replays it against the fresh `DocId` — is carried as the one with the LARGEST KNOWN COST, not as the leading one** (finding DDDD-19), because three collisions with decisions already recorded here were not noted when it was first written down. **(a)** It is **resurrection**, and ADR-0009 removed resurrection *by construction* — "no map, so no get-or-create and no resurrection; the log's lifetime is the record's, dropped on close **by construction** rather than by discipline" — so something must hold the log across the interval keyed by something outliving both records, which is the map and the get-or-create that ADR chose its shape to prevent. **(b)** That key is the one the DDDD-16 correction said did not exist: its B5 claim that *never, for the life of the process* is **unrepresentable** holds only because the poison has no key to live on, and a carry-over identity that can hold a log can hold a count — so that unrepresentability is **contingent on this candidate not being taken**, and was recorded as settled. **(c)** The automatic replay is 9a's rejected "try again" made automatic: rotate → death → rotate → death → poisoned → close → reopen at a fresh `DocId`, count 0, log replayed, **suspect intent reaches a fresh host** — two deaths per cycle and still unbounded. The reopen is a user act but **not a retry decision**; the replay rides on it, which is further in the direction 9a rejected rather than less. It is not dead — recovering stranded work may still be built on it — but only with a mechanism that stops the replay being automatic and unbounded, **and that mechanism is the expensive part nobody has costed**. A fourth candidate is named in the ADR and is **not analysed**: bound per `(DocId, command kind)`, which degrades to today's behaviour when the choke is in `open(image)` itself. It is not taken yet for two measured reasons: there is **no save pipeline** (`documentService.ts` says so in its own words at `markSaved`, and the contract declares exactly two channels, neither of which saves), so L18 can today be neither violated nor satisfied; and the candidate's premise **cannot be tested** — `commandLog.ts` names `DocId` nowhere, so the log is not identity-bound, but whether a **checkpoint** replays against a freshly opened document needs a whole-log replay path, and none exists (`commandBus` replays one entry for redo). One fact that is true today and must not be read as a decision: every command declares `replay: 'reapply-intent'` and `'stored-effect'` is a compile-time trigger with no user, so checkpoint replay is not on the replay path at all — which makes that candidate look cheaper than it will be. **THE TRIGGER IS WHICHEVER OF THE SAVE PIPELINE OR CHECKPOINT RESTORE LANDS FIRST, and it carries two dates because the two reasons expire on different days:** the save pipeline expires reason 1, and **checkpoint restore** expires reason 2, since `commandBus.ts`'s `CheckpointRestoreNotBuiltError` states that §4's *restore the nearest checkpoint and replay forward* "means **opening a new session from those bytes**" — the whole-log-replay machinery this candidate needs. Written as one date, checkpoint restore landing first would have made reason 2 false with nothing firing. **And the timing call — owed before the save pipeline rather than before the supervisor — holds only while candidate 3 (*let one save through the poison*) stays rejected**, because it is the only candidate that reshapes the supervisor: it would have to tell a save from a command. That rejection is an argument, not a measurement; if it is revisited the supervisor is what changes, and by then it will be built. **Consequently the DDDD-16 recovery is right about the COUNT and insufficient as recovery whenever unsaved work exists**, since close-and-reopen drops the record and the log with it — 9a's own rejected alternative with the user's hand on it. **(2) A host death is reported on `ShellFailureSink` as a named `engine-host-gone`**, not on `IncidentSink`, whose `channel` field means *the IPC channel a failure was crossing* and a host death has none; a stub there would be the preload defect again — a channel a runtime announces on with nothing subscribed — which is the finding `shellFailure.ts` exists for, and the user-facing half is separate and already exists, since in-flight calls reject through `client.ts`. **(3) Nothing else is drained and nothing else fails.** In-flight calls rejecting is a deduction; **queued** commands are the decision, and they stay queued because they never touched the host and the canonical bytes they will run against are intact. The supervisor enters **each** document's lane at the moment of the ending, with an entry that awaits the rebuild and reopens that document's session inside it — so the reopen sits ahead of every command issued afterwards, queued work serialises behind it by the lane's own guarantee, and a document closed meanwhile is skipped by `run`'s get-or-miss rather than by a check. Creating at the lookup instead was rejected as **B4**: `SessionLookup` is get-or-miss for ADR-0009's stated reason, and widening a seam to fit a feature is what B4 forbids. The rebuild set is derived from the supervisor's own session map, whose failure direction is *smaller*, so it is anchored against `DocumentService.size` — open minus poisoned must equal sessions held (item 4c). A rebuild slow enough to saturate a lane's queue turns some commands into `document-busy`, which is declared and is stated here so it is not later read as a bug. **Not measured:** none of the three. *Whether a killed host actually recovers* is what this row still owes against a running host, as `documentCommands.ts` has said since ADR-0009. **And the unit after the transport is NOT the one this row named** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 10, 2026-08-26). There is no remote `EngineWriter` to build: `rotatePages.ts` calls `withDocument(session, work)` where `work` is a **synchronous** `(document: mupdf.PDFDocument) => T`, and `commandBus.ts` calls `spec.capture` and `spec.apply` directly in main — so the calls that block belong to the **bus**, not to the writer, and a synchronous callback over a live native handle cannot become an RPC. **Decided:** the bus asks the registered writer to run the spec; `commandSpecs.ts` stays the single place a command's `apply`, `capture` and `invert` are declared; the host's own in-process adapter performs that same lookup against its live session — one implementation per command (B3a), ADR-0009 §4's one code path still in main, §6 still true, `ARCHITECTURE.md` §2 unchanged. `open` and `serialise` exchange **files** in the two handed directories the verb-split rows already measured, so only the handle crosses the pipe, which is what Decision 7 says crosses. **A first statement of this was wrong and the ADR carries the correction:** it called the checkpoint crossing *per terminal command rather than per operation*, and invariant 11 has no rarity clause — per terminal command **is** per operation, so that route breached L11 and Decision 7 both. Two things settled rather than left to whoever writes it first: the prior-state payload is bounded, and **the host owns session identity**, main holding an opaque handle it could not dereference. **10a's bound was corrected on 2026-08-26 before anything was built on it:** it protects nothing — `CommandPrior[K]` returns inside a `HostResponse` and `ENGINE_HOST_FRAME_MAX_BYTES` already bounds that pipe in both directions, so L11's protection does not depend on it. What it decides is *where* the degradation happens: gracefully at the seam as `captured: false` → checkpoint, or at the transport as a frame refusal. A **fallback-quality choice, not a safety threshold** — and the economics run opposite to the question that was asked, because the alternative to recording an inverse is a **whole document image**, so the bound should be as large as the transport permits. It is therefore **derived** — the frame maximum minus the response envelope, computed at the call site, never a second literal (B3a, in `hostProtocol.ts`'s own words about its own derivation). Breach needs no new failure path: `captured: false` already makes the entry terminal with a checkpoint. **And that branch is the ORDINARY outcome, measured:** a select-all `rotatePages` inverse at this project's stated 20,000-page extreme weighs **809,018 bytes** (all `/Rotate` absent) or **969,018** (all present) inside the real envelope — 40.45 and 48.45 bytes a page, binding at ~6,480 and ~5,410 pages against intent's ~43,669. Three to four times the whole frame at the extreme, so the fallback is a live path to be built as one, not a defensive branch. **And that reaches row 278's live gap** (DDDD-31, 2026-08-26): the fallback is correct *about behaviour* — a terminal entry with a checkpoint is ADR-0009's designed answer — but **where the checkpoint lands is unbounded**, since `CommandLog` has no cap and `perf:gate` puts `main-service` at 1.01–1.02× holding the canonical image alone, so canonical plus one checkpoint is ~2.0× against a 1.5× ceiling. The stated reason is therefore not *the fallback is fine* but *the fallback is fine given a budget nothing enforces*, and this measurement moved that budget from theoretical to reachable with today's only command. **Not a B4 amendment, and that was checked rather than assumed** — no `ARCHITECTURE.md` sentence becomes false under Decision 10, and an amendment commit editing nothing would assert a change that did not happen. The sweep did find two sentences already false from ADR-0022 (§3 and invariant 20 still said *utility processes*), corrected in their own commit. **AND IT IS BUILT** (2026-08-26): `CommandExecution<W>` and `RegisteredWriter<W>` in `commandSpecs.ts`, `localMupdfExecution` beside them, `localEngine.ts` assembling the local writer, and `CommandBus` calling `writer.capture`/`writer.apply`/`writer.invert` at all four call sites. The assembly is a third file for a **structural** reason worth not rediscovering: `mupdfWriter → commandSpecs → rotatePages → mupdfWriter` is a cycle, so the execution members cannot hang off `mupdfWriter` however tidy that looks. **Six controls, each mutation-tested**, and they assert two things apiece — that the substituted member ran **and** that the declared one did not also run, since a bus calling both would satisfy either alone. Reverting `execute` to the spec reddens two; reverting `undo`/`redo` reddens two more; deleting the two new unregistered-writer guards reddens the last two, which cover a branch nothing reached before (a log outlives the registration that produced it, because ADR-0009 puts the log on the record and the registry on the bus). The fixture is the **flat** document deliberately — a malformed one produces a terminal entry either way, so a case built on it would pass whether or not the substitution took effect. `contract.proof.mjs` gains the byte-image pair for `CommandExecution` by the seam's own rule that an unimplemented variant nobody constructs is a vacuous check; **its resolution test refused the first version of that pair**, because the new reject case's diagnostic was word-for-word the existing one's, and two matchers that accept each other's reason certify neither. Both are now anchored on the parameter name. **Still unwired:** `composition.ts` registers an empty bus, and it stays empty until a **remote** writer exists — registering the local one in main is native code in the main process (invariant 20). **THE EXECUTION HALF OF THAT REMOTE WRITER EXISTS** (2026-08-26): `engineChannels.ts` declares `engine/apply`, `engine/capture` and `engine/invert` in **kernel** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 11 and its `ARCHITECTURE.md` §5 amendment, because `capture` answers with prior state the renderer must not see); `remoteEngine.ts` is main's side, with a `WeakMap` registry that is 10b — the host mints the identity, main holds a token whose meaning is map membership rather than a string it can invent; `engineHandlers.ts` is the host's, looking the spec up in the same `declaredSpecs`. **Seven cases drive BOTH halves against ONE real document**, which is the only way Decision 10's claim is testable rather than asserted: `capture` over the wire is asserted **equal to `localMupdfExecution.capture`** on the same session, not equal to a literal, because a second remote implementation — the candidate Decision 10 rejected — would agree with a literal most of the time. Mutation-tested: gutting the host's `apply` reddens three, and making `handleFor` return a fixed handle instead of throwing reddens the two that separate *refused here* from *refused by the host* — a distinction only the **request count** can make, since both arrive as a rejection. `no-such-session` is **returned, never thrown**, because a throw becomes `internal` with its diagnostic withheld and Decision 9's supervisor cannot rebuild on a code it cannot read. **A tag check was written and then removed on evidence:** lint reported it as always-false, since `mupdf` routes one kind, so what guards it is the type — `CommandPrior[K]` stops resolving the day a second command is routed here, which is where the narrowing and its runtime check get written. **`open`, `serialise` and `close` landed 2026-08-26** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 13), exchanging **files** in the two handed directories rather than payloads. The asymmetry is the security content: main → host may carry a path, because main created those directories and wrote their DACLs — the string is an address and the ACE is the capability; host → main carries **no path and no component of one**, so main mints the output file name in the request and the answer is a byte count. A host that could name the file main reads back could have main open an arbitrary path and take the bytes as the user's document. Proven by driving both halves against one real MuPDF session over real directories: the image goes in, comes back, and **reopens**; no message reaches a twentieth of the document; each session gets its own pair; and the pair is removed on close, on a failed open, and when the **transport** is gone mid-close. That last case is the one that matters and it was got wrong first — a `wrapHandler` catches anything a handler throws and answers with a `Result`, so overriding the host's writer can never make a call reject, and the `finally` survived its own mutation until the case rejected at the transport instead. **DECISION 9a IS BUILT** (2026-08-26): `EngineSessions` in `apps/desktop/src/engineSessions.ts` holds one entry per open document — its sessions **and** its consecutive-failure count in a single map, because the DDDD-16 correction puts both on one per-document state and two maps keyed by `DocId` would be two owners of it, drifting at exactly the moment that matters. A death increments the count and **drops the sessions**, which is not bookkeeping: the process holding them is gone, and a handle left in the map is one a queued command finds and calls into a process that no longer exists. `document-poisoned` is declared on `document.execute` and mapped from `DocumentPoisonedError` in `commandHandlers.ts`. The composition point reads the poison **inside the lane and before the session**, so a document poisoned while its command sat in the queue is refused rather than run, and the decided outcome does not arrive wearing `MissingSessionError`'s `internal`. Both questions come from **one** parameter, `EngineSessionSource`: they are two answers from one authority, and two parameters could be wired from two places (B3a) — while `SessionLookup` itself is untouched, which is what keeps this a registration beside the seam rather than the widening this row already rejected as B4. Recovery still needs no mechanism and is now asserted rather than argued: a released entry leaves nothing behind, and a fresh `DocId` reads as unpoisoned. **AND THE RELEASE IS REGISTERED, NOT A METHOD SOMEBODY MUST REMEMBER TO CALL** (finding FFFF-1, fixed the same day it was raised): the entry's lifetime being the record's was asserted in the present tense by this row's 4c anchor, by the ADR's DDDD-16 correction and by the code's own comment, while **nothing invoked it** — a search across `apps/desktop/src` and `packages/kernel/src` found two test callers and no production one. `DocumentService` is the only thing that knows a record ended, so the supervisor learns it from there: `releaseOnClose` is typed as `DocumentTeardown` — the seam that already existed for exactly this, in its own words *"releases whatever a document holds outside this index — the engine session, above all"* — and `composition.ts` hands it to the service at construction. Nothing fans out, and the compiler checks the fit rather than an adapter arrow. Three cases drive it through the **real** service over a real file: a close drops the entry, an **unregistered** service leaves it behind (the control, which is what goes red if the registration is ever dropped), and the release runs **after** that document's lane drains, so work already executing still sees its session. Mutation-tested both ways — gutting `releaseOnClose` reddens four, and moving `close`'s teardown ahead of the lane reddens the ordering case together with `documentService.test.ts`'s own. **What remains absent is the close CHANNEL**, not a caller: the contract declares no close, so nothing in the running app reaches `DocumentService.close` yet, and the day it does this needs no further work. **Five mutations on the state itself, each red at the right line:** the bound moved to 3 (7 cases), reset-on-success removed (1), the poison read after the session lookup (1, and it fails naming `MissingSessionError`, which is the whole distinction), the session drop removed (1), and `openEngineSession`'s rollback removed (2) — the last covering a function that until this unit had **no test and no caller**, which is `proven-in-the-wrong-file` in its own right. `composition.ts` now builds the real component holding nothing, rather than two inline stubs: its `poisoned` answering `undefined` everywhere is *true* rather than stubbed, since no host has been asked for anything. **Still not measured:** that a killed host recovers — that needs 9b's subscription to the transport's ending and 9c's lane entry, and neither is built. **Still owed:** `composition.ts` is the last wiring step. | **kernel done, unwired** |
| **OPEN — `main` loads the MuPDF native library at startup, and invariant 20 says it must not** (found 2026-08-26 while measuring something else; `scripts/research/barrelCost.mjs`). Measured on this machine, peak RSS of a bare Node process loading one built kernel module: `capabilityRegistry.js` **+1.9 MB**, `documentService.js` **+8.1 MB**, `mupdfWriter.js` **+39.7 MB** (the anchor — it binds the library at module scope), **`commandBus.js` +47.0 MB**, the barrel **+41.3 MB**. Read from source: `commandBus.ts` imports `declaredSpecs` from `commandSpecs.ts` as a **value**, and that reaches `rotatePages.ts` → `mupdfWriter.ts` → `import * as mupdf`; `composition.ts` imports `CommandBus` *and* the barrel as values. So a main process built from `composition.ts` pays it twice over. **Not directly measured in Electron main** — `composition.js` cannot be loaded in plain Node, because a `import { type X } from './main.js'` leaves a side-effect import of a module that requires Electron — so the conclusion follows from the two readings above rather than from a reading of main itself, and that gap is stated rather than closed by assertion. **`proof:kernelload` does not cover this:** its subject is `documentService.js`, which is clean at +8.1 MB, and its *control* asserts the barrel does reach the adapter — so the guard is working and the exposure is beside it. **The fix is not obvious and is not taken here:** the bus reads only `spec.writer` from `declaredSpecs` — routing, not execution, since ADR-0023 Decision 10 moved `apply`/`capture`/`invert` onto the writer — so splitting routing into a data-only module would cut the edge; but `composition.ts` would still load the adapter through the barrel, and changing what the kernel's public surface re-exports is an amendment (B4), owed before any of it is built. | **open — amendment owed** |
| **The handed directories are created WITH their DACL** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 12). `CreateDirectoryW` takes a `SECURITY_ATTRIBUTES`, so the pair's first observable state already carries its grant — there is no window in which a directory the container is about to be granted exists carrying whatever it inherited, which is not theoretical, since the ACE union measured on 2026-08-25 is an inherited `M` surviving an explicit `R`. The DACL sets **`P`**, so inheritance cannot apply at all; the sibling layout that first solved that is kept and is now belt-and-braces. Rejected: `spawnSync('icacls', …)`, which lets PATH resolution decide which binary establishes a security boundary — the same objection invariant 23 makes to a filename selecting a native library — and which would have expressed as argument strings a rule `hostPipeDacl` already expresses as SDDL (B3a). Both DACLs now live in `hostDacl.ts`: one resolver, two callers. **Measured before the shape was committed to, with the control that makes it mean something:** a sibling created by `mkdir` under the same parent renders `(I)(OI)(CI)(M)` while the protected one renders `(OI)(CI)(R)` — without that row, *no inherited ACE* is indistinguishable from a reader that cannot see one, and the reassuring answer here is an absence. `0x00120089`→`(R)` and `0x001301BF`→`(M)` are the two rights the 2026-08-25 verb split was read against. **The spike grants this way now, and that is what keeps those four rows meaning what they say:** `lowboxSpike.mjs` creates its pair through `createSessionDirectories` and asserts per directory that the container is named, that `icacls` renders **exactly** the expected right, and that no `(I)` ACE is present; mutating the shipped read mask to `0x001200A9` turns it red naming what it read. `proof:hostcontainment`, 28 cases, 2026-08-26. **OWED, and this row is the trigger:** the pair is removed on close and on a failed open, but **nothing sweeps a root left behind by a main process that died without unwinding**. That is not a forgotten line — it is a decision about what a second running instance of this app may delete — and it expires the first time this application can have two instances, or on Store packaging, whichever comes first. Kept here rather than in `docs/security/engine-advisories.json`: a symbol scan cannot see an event. | **partly done** |
| **Engine host containment asserted against a RUNNING process** (invariant 25). Not against the options passed to `utilityProcess.fork` — a flag that did not take effect and one that did are indistinguishable until it matters, which is the whole lesson of the compiler-mitigations check reading the PE image rather than the build flags. **FOUR assertions, one per property the invariant names, each on a live host.** It carried three for as long as it existed, against a law naming four — so the row could have gone green while "Engine host containment asserted against a RUNNING process" was true of three quarters of it, which is the display-only shape at the level of the checklist rather than the code (finding PP-1). (a) its **integrity level** is the lowest workable one, read **by main against the child's token — not by the host against its own**; (b) its **job object** bounds memory and forbids process creation, read from the job **and from behaviour, beside a control that does not assign one**; (c) a **network connection attempt from inside the host fails** — the only form of "no network" that is evidence rather than configuration; (d) a **read of a file the host was not handed fails, while a read of one it was handed succeeds** — both halves, because a refusal outside proves containment only if the same call succeeds where it is permitted — **and the read is made through the path the adversary has, not through JavaScript.** Naming the caller is the whole content of this assertion: invariant 25's threat is native code executing inside the host, and a `readFileSync` refused by Node proves nothing about a `CreateFileW` that never reaches Node (finding QQ-3). **What is measured, and against what:** three of invariant 25's four properties now have a mechanism confirmed on a running process — integrity (the LowBox token is Low at creation, `0x1000`), network and filesystem (the AppContainer), and process creation, which WW-1's per-property variant matrix showed is delivered by the **job object** — so a host with the container applied and the assignment failed looks contained and is not ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) Decision 8). **Whether the container ALSO refuses it depends on the Windows build** (measured 2026-08-23: no on Windows 11, `EPERM` on `windows-latest`), which is why the job is the mechanism and the container is not something to lean on. **Updated 2026-08-23 — (b) has moved off the research instrument.** `scripts/research/hostSurfaceProbe.mjs` drives the SHIPPED surface and factory ordering against two real processes differing only in the job, and the child under the job is refused a spawn while the same child without one spawns freely. So the property that WW-1's matrix attributed to the job is now measured through the code that will run in production, with the differential intact. The other three are unchanged: both probe cells run uncontained, so nothing there speaks to (a), (c) or (d). **What is not:** the readings for (a), (c) and (d) are still taken against `scripts/research/lowboxSpike.mjs` — a research instrument, which asserts nothing — but **as of 2026-08-23 every cell in it is created by the SHIPPED surface**, so they are no longer measured against a hand-rolled second implementation of process creation. That was B3a's shape and a proof built on it would have proven the wrong artefact. The rule the file now states is the transferable part: *creation belongs to the surface, observation belongs to the instrument* — reading the child's token from outside and waiting for exit stay in the instrument, because no shipped host does either. Verified by requiring every property verdict to stay byte-identical across the migration, which caught a real regression: `process.execPath` silently changed meaning when the driver moved into a plain-Node parent, so the cells briefly ran system Node instead of the Electron binary and one row went from `same` to `UNREADABLE`. **The job's *memory* limit is MEASURED as of 2026-08-23, with a stated scope.** It passed through three states and each was printed rather than skipped: `no mechanism` (nothing set a limit) → `no probe` (the limit was in force and nothing allocated past it, which arrived as a side effect of moving onto the shipped surface, since `applyLimits` has no undefaulted form) → `measured`. Two rows on running processes: a host whose job carries a 256 MB cap is **refused at 192 MB** where the same host with no job of ours commits 512 MB, and — the control that makes the first row mean something — a host under a job carrying §9.17's real 3 GB cap commits it too, so the refusal is the limit's **value** rather than the presence of a job. Resolution-tested by giving the memory cell the real cap, which reddens exactly that row. The probe runs **last and releases its chunks**, because `hostFixture.mjs` committed 768 MB *before* its reads and the 235 MB document read then failed with `ERR_MEMORY_ALLOCATION_FAILED` — which its table read as the filesystem property being enforced. **What this is NOT: evidence about the 3 GB figure.** A differential needs the uncontained side to succeed, and committing past 3 GB on a runner fails for memory pressure as readily as for the job, which would report the runner's RAM as this project's defect. The figure is a derivation and `proof:composition` already requires the surface to carry §9.17's line — a behavioural probe cannot check a derivation and a derivation cannot check enforcement. **And until 2026-08-23 the shipped surface's containment path had been executed by NOTHING** (finding UUU-4): both cells of `hostSurfaceProbe.mjs` pass `containerName: null`, so `win32HostSurface.ts`'s `CreateAppContainerProfile`, its SID derivation and its `PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES` attribute — the code carrying invariant 25's actual containment mechanism — had never run in any job, on any machine. It has now been run **once, by hand**, and it works: a contained process was created, with an uncontained one beside it as the control so a refusal could not have been read as *the container cannot reach the binary*. The executable was `C:\Windows\System32\cmd.exe`, chosen because System32 grants `ALL APPLICATION PACKAGES` read+execute by default — against the Electron install under a user profile a refusal would have meant the ACLs, not the surface. The profile it created was deleted afterwards; the surface never deletes one, deliberately, because deleting a profile drops every ACE naming it. **Still unexercised in CI, and RR-3's proof is what expires this**, which is a further reason the spike becomes that proof rather than sitting beside it. **RR-3 IS DISCHARGED, 2026-08-23: these readings are now ASSERTED, in the shim job.** `proof:hostcontainment` carries thirteen cases — three that the uncontained cell is a working host, nine property rows each declaring the verdict the invariant requires, and the absence control — and the shim job passes `--require-containment`, so a machine that cannot look fails there rather than reporting UNVERIFIABLE. Two of the nine expect `same` and that is the substance — the engine and the document must work *inside* the container, so a difference means the host cannot do its job. **One expects `either`, and that is a coverage reduction with a measurement behind it:** the LowBox-alone process-creation row expected `same` on WW-1's reading, and the runner refused it `EPERM` where this machine allows it. Neither direction can be asserted, so the row asserts the half that is invariant — the *uncontained* cell must still be able to spawn, without which two dead cells would satisfy it — and reports the container's answer. Mutation-tested by declaring one row's expectation backwards, which reddens exactly that case. The file did not move to become a proof — `check:docs` requires every `scripts/` path in a tracked document to resolve and this one is named by records, which take appended corrections and are never edited; ADR-0023 §6 carries the note. **The remaining trigger is the memory figure**, and it is now one probe rather than one mechanism away. **RR-3's control is settled, 2026-08-23, before the proof is built on it.** The spike's route control was a differential against a `baseline` cell forked by `utilityProcess.fork`, and ADR-0022 retired that reference — so it is replaced by a **working-host control**: the uncontained cell must be seen loading koffi, loading the shim and opening the document it was handed on every run, and the instrument prints `HOST NOT WORKING` with no property verdict when it cannot. Measured both ways — every property verdict byte-identical to the run before the removal, and a deliberately broken control exits 2 having printed zero rows. Removing the cell also removed the Electron **app**, whose GPU process crash-looped twice and killed whole runs (LLL-1); the spike's parent is plain Node now. The loss is stated at the control and in ADR-0023's 2026-08-23 correction: a differential can catch an unanticipated difference between two creation routes and a working-host check catches only the three things it names. **The platform axis is bought, 2026-08-23 (finding XXX-1), and NOT for the reason that raised it.** The finding came from the process-creation row that split between Windows builds, and that row asserts `either` now, so a second image cannot change its verdict. What a second image buys is the **DIFFERS** rows — (c) network, both (d) filesystem rows and IPC — which are what this invariant actually claims and whose build-dependence has never been measured; one Windows image is a sample of one, and the `either` row is the standing demonstration that this sample size hides real variation. The `containment-2022` job runs the same proof with `--require-containment` on `windows-2022`, which is Server 2022 where `windows-latest` is Server 2025 — read from `actions/runner-images`, not recalled. A separate job rather than a matrix leg, because a matrix would put `proof:cff` on both legs and excluding one needs an `if:` that fails in the reassuring direction: a stale label matches neither leg, the CVE proof skips on both, and the job is green. Cost measured rather than estimated — a cold MuPDF build is **336s**, read from the first shim run after the cache step landed, against 62–105s warm, so the job is ~375s cold and ~110s steady state. **The first reading is taken, `ab45472`: the job ran on Server 2022 and the proof passed under `--require-containment` in 2s**, so every DIFFERS row holds on a second Windows build and the working-host control passed there too. Both cold builds landed within 15% of the prediction — 340s on the shim job's rotated key, 294s on the new job's first miss. **AAAA-1 is closed and the row now has a recorder.** The `either` row's contained outcome had none — it was printed for a reader, CI has no reader, and so the only event anybody cares about, the answer *changing* on an image, was unobservable. `--expect-lowbox-spawn` (taking `refused` or `allowed`) pins it beside each job's `runs-on:`, is **mandatory** under `--require-containment` so a job cannot opt out by omission, and makes the row assert both halves: the uncontained cell allowed, and the contained outcome equal to the pin. **Three data points, and the axis is UNDER-DETERMINED** (finding AAAA-8) — Windows 11 client allows; Server 2025 and Server 2022 both refuse (measured 2026-08-23 at `0909970`, CI run 32659310667). Server 2022's pin was landed as a deliberate probe and resolved on its first run, which killed the chronological hypothesis. It was then recorded as *client versus server* for one day, and that is stronger than the evidence: **two of the three points are GitHub-hosted CI images and the third is a developer machine**, so the one client point is also the one not-a-CI-image point, and *client vs server* and *this machine vs a CI image* survive all three readings equally. **The discriminating test is reachable at a price and is asymmetric.** `windows-11-arm` is Windows 11 — a client SKU — and is GitHub-hosted (read from `actions/runner-images`; the same table no longer lists a 2019 image), but it is arm64, so it swaps the image-provenance confounder for an architecture one and costs an arm64 MuPDF and an arm64 Electron. Client and arm64 move together there, so a **refusal** would falsify the reading and an **allow** would confirm nothing — it can falsify and cannot confirm, and money spent to confirm buys an outcome the design cannot produce. **What it would change today is nothing:** Decision 8 rests on *the container cannot be relied on for (b)*, which all three points support; this row asserts `either` across images and pins per image, so no verdict moves. The axis bears on the shipping configuration — real users run Windows 11 client **x64**, the one point where the container allows — so if the reading holds, the job carries (b) on every machine this product will run on, which is what Decision 8 already asserts. **Worth recording, not worth scheduling**; it becomes worth buying the day a design leans on the container for (b). | **partly done** |

**THE PRINCIPLE THAT ORDERS ALL FOUR, and it predicts more than it summarises: only kernel-enforced mechanisms contain native code.** A job object, an integrity level, a token, an AppContainer — the kernel checks these on every access, whoever makes it. Node's permission model is enforced inside Node's own filesystem bindings, so it constrains JavaScript and nothing else. This is why QQ-1 leaves (a) and (b) **untouched** while it removes (d)'s only candidate: integrity levels and job objects are kernel objects and bind the MuPDF shim exactly as they bind the host's JavaScript. Do not read "the containment conclusions were about the wrong adversary" as applying to all four — it applies to the one mechanism enforced in userland. It also settles (c) without a further measurement: no Node-level mechanism will ever deliver it, since the permission model has no network dimension and would be enforced in the same place if it grew one. And it generalises past this flag — of any proposed containment mechanism, ask **who enforces it** before asking what it denies.

**Measured 2026-08-21 and 2026-08-22** (`scripts/research/`). **All four now have a mechanism, a fixture and a differential — and two of them decide the process type** ([ADR-0022](DECISIONS/0022-the-engine-host-is-a-process-we-create.md)). (a) and (b) hold on an Electron utility process. (c) and (d) do not, and no Node-level mechanism will ever supply them; both come from an **AppContainer**, which `utilityProcess.fork` cannot create, so the engine hosts are processes this application creates. Measured against a route control — our own `CreateProcessW` with containment off, so a refusal cannot be a broken spawn route — a contained host with koffi and the shim loaded is refused a file it was not handed at `EPERM` through Node **and at `ERROR_ACCESS_DENIED` from `CreateFileW` itself**, refused a loopback connection at `ETIMEDOUT`, and still opens a document it *was* handed with the right page count. **The costs are measured, not accepted:** five grants of two kinds — read+execute on the runtime, the FFI, its platform sibling and the shim, and **modify** on what the host was handed, which means those paths are now part of the containment and anything that can write them defeats it; `--preserve-symlinks` and `--preserve-symlinks-main`, because Node realpaths every ancestor and a LowBox token has no rights on the volume root; and a pipe that needs its own DACL, since Node's named-pipe server sets none for the container. (a): a host can lower its own integrity, and afterwards cannot read its own token at all — `OpenProcessToken` fails ACCESS_DENIED, because the process object's descriptor was created at Medium. That is a *could-not-look*, so the reading comes from main, which sees the child at **0x2000 before and 0x1000 after** — two readings differing across the one action, which is the only shape that separates a working reader from one that always says Low. (b): obtained and **behaviourally verified** — under `ActiveProcessLimit = 1` and a process memory limit, the host cannot spawn and cannot commit past it, where the same host with no job spawns `v43.4.1` and allocates freely. That measurement was taken on a utility process, where Chromium already places the child in a job and ours nested inside it — **and the nesting was never the mechanism**, which is why (b) survives the move to a process we create: the job is one main assigns against the child's pid, and that works on any child. (d): **the mechanism is the container, and the candidate that failed is kept here because it is the one a reader will re-propose.** Node's permission model was that candidate and it does not survive the adversary this invariant names. Measured in one process under one policy, with the model active and `--allow-addons` granted: a JavaScript read outside the allow-list is refused `ERR_ACCESS_DENIED`, and a **native** read of the same file through `CreateFileW` returns 4,096 bytes beginning `MZ` — both beside inside-the-allow-list reads that succeed, so neither result can be a broken call. **The dial has no setting that works:** without `--allow-addons` the host cannot load koffi at all and so cannot reach MuPDF; with it, native code is outside the model by design. One end has no engine, the other has no containment. (c): the **fixture** is a loopback connection, so a refusal cannot be a runner with no network (HH-2), and the container delivers it — `ETIMEDOUT` contained, connected uncontained. Three candidates were measured not to: `session` governs Electron's `net` module and not `require('node:net')`; integrity is a write control and a verified-Low host still connected; the permission model has no network dimension and `canListen` stays true with it enforcing. All three are userland, which is the principle above predicting rather than summarising.

> **Correction, 2026-08-21 — the answer moved three times in one day, and only the reasons separate the positions.** All three are kept because a reader who knows only the final one will re-propose the middle one.
>
> 1. **"The permission model is withdrawn."** Inferred from `--permission` through `execArgv` being accepted, arriving in the host's own `process.execArgv`, and leaving `process.permission` absent. Right answer, wrong route: what was measured is one route being inert, not the model being unavailable.
> 2. **"(d) is obtained through `NODE_OPTIONS`."** Right that the model is reachable — `ELECTRON_RUN_AS_NODE=1` shows Electron's own Node 24.18.1 carries it, and `NODE_OPTIONS` is read during bootstrap where `execArgv` is applied after it — and wrong that this delivers (d). The probe had no koffi in it, so it measured the JavaScript surface and concluded about the host.
> 3. **The measured position, above:** reachable, enforced on JavaScript, irrelevant to native code. (d) has no mechanism.
>
> **Two observations survive independent of all this**, and they are why position 1 read so well. `--permission` via `execArgv` is **set, readable back as set, and inert**, so a check reading `execArgv` reports containment in force; the route that *works* leaves `execArgv` empty, so the same check reports it absent while it is active. **The read-back misleads in both directions** — a shape worth more than either half, and it generalises: any check reading configuration rather than behaviour can now be wrong optimistically *or* pessimistically. And `process.permission` being present is not enforcement: `fs` still loads, because the model throws per call rather than per require.
>
> **The allow-list is not empty, measured rather than predicted (QQ-4).** Loading one FFI needed three directories — `koffi`, its platform sibling `@koromix/koffi-win32-x64`, and `koffi.node` under Electron's own `resources`. Each surfaced only after the probe was changed to report `error.resource`; the message alone says access was restricted and names nothing, so the first denial was read as "addons blocked" when it was a file read. Whatever mechanism delivers (d) inherits that list, so *"reaches no filesystem path it was not handed"* and *"reaches the runtime install, the FFI, its platform package, and what it was handed"* are different properties and the ADR names which one.
>
> The declaration in `docs/security/engine-advisories.json` (`engine-host-containment`) turns the build red the day shipped code names `utilityProcess`, which is what forces this row rather than leaving it to whoever writes the host. That trigger catches "a host was written"; this row is "and it was contained".
>
> **And that trigger is now aimed at a symbol shipped code will no longer name** (ADR-0022). Re-pointing it at the creation route is owed *before* the host lands — a trigger that can no longer see its subject reports the reassuring answer for the wrong reason, which is the failure this row's own instruments exist to refuse. Its symbols were the project's only UNVERIFIABLE ones while Electron was not a dependency, and **all three predicted consequences landed together on 2026-08-20**: the `in: null` expired, a witness became possible, and the list stopped being hand-picked. They are now **derived** from `electron.d.ts` by the TypeScript compiler (`scripts/security/electronSurface.mjs`), not witnessed — a derivation is stronger, and a witness here would digest a 1.1 MB file and report that it *changed* rather than that the symbol is still declared in it (finding T-1). **The derivation immediately proved the hand-picked list short**: it reports the spawn surface by TYPE — every declaration typed as the utility-process factory — which is `utilityProcess` *and* `UtilityProcess`, and the class was missing. A PARSE and not a search, because the file is 56% comments and on 43.4.1 `utilityProcess` occurs once in prose: a text match would witness a symbol that had been **removed**, which is the reassuring answer. What it witnesses is **declarations only** — every workflow installs with `--ignore-scripts`, which fetches the 1,125,720-byte `.d.ts` and no binary at all, so nothing here says an Electron process can start. Where `node_modules` is absent (the Guards job) the symbols report as unverifiable; `--require-derivation`, in the one job that installs, makes them mandatory. | — |
| **The exact CSP is pinned as an invariant, once a renderer exists to read it from** (threat model §4.13). The value is deferred because the renderer does not exist and the policy would be a guess — an invariant relaxed in its first week teaches that relaxing invariants is normal. **The mechanism is not deferred, and this row is it:** a stage item does not fail a build, so the CSP would otherwise be the one security decision holding a note where invariant 25 holds a trigger. When the renderer lands, this row requires (a) the **exact directive list** written into `docs/ARCHITECTURE.md` §9 as an invariant, not "a CSP is set"; (b) the policy **read back from the running renderer** — `document.querySelector` on the delivered meta tag, or the response header as received — and compared to the invariant, never read from the source that sets it, for the same reason the mitigations check reads the PE image and the containment row asserts against a live process; (c) a control proving the comparison can fail, by asserting a policy the renderer does not have. Any later relaxation is then an ARCHITECTURE diff someone justifies, which is the whole point of pinning it. **All three landed** ([ADR-0019](DECISIONS/0019-the-renderers-csp-is-pinned.md)): invariant 27 pins eleven directives in a fenced block, `proof:rendererpolicy` extracts that block and compares it to the constant — refusing to return an empty list, since a search's silence is its most convincing failure — and the read-back reads the header as Chromium received it plus two directives observed being obeyed. Two things the row did not ask for and this record needs anyway: **enforcement is evidenced for two directives of eleven**, not all of them, and `style-src`'s `'unsafe-inline'` was **dropped rather than pinned**, because a grant nothing needs that arrives before the pin is never argued for afterwards. | **done** |
| **Active content runs on no open path** (invariant 24). A fixture PDF carrying embedded JavaScript, an `/OpenAction`, an external reference and an embedded file, opened through the real shim: nothing executes, nothing is fetched, nothing reaches disk. Needs a fixture whose active content is *observable when it fires* — a proof that the JS did not run is worthless if the same result appears when the JS is absent, so the control is the same document opened by something that DOES run it. | — |
| Per-document stores | — |
| Command / dialog / settings registries | — |
| Design substrate: tokens, lint rules, `docs/UI-GUIDE.md`, 4 primitives. **Carries one debt already incurred:** `WINDOW_BACKGROUND` in `apps/desktop/src/windowPolicy.ts` is a raw hex, which §10 forbids anywhere. It is owed rather than exempt — there is no token to use yet, and `apps/desktop` could not import one from `packages/ui` in any case (the module graph allows it `shared`, `contract` and `kernel` only), so **this row is what replaces it**, and the hex-ban lint rule this row adds must not ship with an exemption for it. Its value was `#00000000` until measured: Electron honours an alpha channel only for a `transparent` window, so the window painted opaque black while the source claimed full transparency. `proof:rendererpolicy` now reads the colour back off the running window, and the mutation that proves that case is the original value. | — |
| i18n scaffold + literal-string lint rule | — |
| Logging + crash reporter consent | — |
| CI: typecheck, lint, unit, proofs (Windows + Linux) | **done** |
| CI: Playwright smoke + axe on the browser shim | — |
| Packaging skeleton — **Store (MSIX) is the only distribution** ([ADR-0018](DECISIONS/0018-distribution-is-the-microsoft-store.md)); the flavour switch stays as a seam so a signed direct download is later a config change rather than an amendment. **Two MSIX assumptions must be checked here, early rather than at submission:** (a) an MSIX application **cannot write to its install directory**, so anything that expects to — provisioned binaries, caches, the shim's neighbours — has to be found now, not by a rejected submission; (b) its **data paths differ from the installer flavour's**, so any path assumption baked in while developing unpackaged is wrong under MSIX. Both are executable only once this skeleton exists, which is why they are recorded against this row. Plus installer size arithmetic. **(c) PREMISE P1, and it can invalidate a decision rather than add work to it** ([ADR-0023](DECISIONS/0023-how-the-contained-engine-host-is-built.md) §5, carried here 2026-08-22): the contained engine host's primary branch assumes the shipped install root grants `ALL APPLICATION PACKAGES` read and execute, so the app makes **no runtime grant on an install-root path** — which it could not do anyway, by (a) above. That premise is **unmeasured**: `icacls "C:\Program Files\WindowsApps"` returns *Access is denied* without elevation, so the seat that designed the host could not read it, and an installer is not built to find out (B8 — the owner's call). If P1 is false the answer is a decision to retake, not a fix. **It lives on this row and not in `docs/security/engine-advisories.json` because its expiry is an EVENT, not a code movement** — packaging, an elevated read, or Stage 7, whichever is first — and the register's whole mechanism is *"the day shipped code names X"*. A premise waiting on an event has nothing for a symbol scan to see. | — |
| **Gate:** engine-capability spike — MuPDF/@cantoo rows executed, matrix amended (ADR-0006); PDFium, @signpdf and PDF.js rows pending their stages | **partly done** |
| **Gate:** performance budget assertion — **per-process** peak RSS against the `main` and `mupdf-host` budgets, read from invariant 17's machine-read line rather than restated here or defined as constants ([ADR-0012](DECISIONS/0012-memory-budgets-are-machine-read-from-the-invariant.md)). `npm run perf:gate` runs **both content shapes**, because they are not interchangeable evidence. Measured: image-heavy 199.4 MB → main **1.00×**, host **1.30×**; object-dense 25.1 MB / 127K objects → main **1.00×**, host **3.71×**. The multiple is of the document's cost, above each role's measured baseline, per invariant 17. `proof:perfbudget` mutates the declared line and requires the verdict to follow it, stating no limit of its own. **Owed before this is done:** these are the roles in their own processes, not Electron's, so it must be re-measured when the utility process lands. **The renderer half cannot be asserted at all yet** — invariant 17 declares it provisional and two-term, both unmeasurable until a renderer exists, and asking for its limit throws rather than substituting one; the single figure this row used to carry is recorded in ADR-0007 as the mistake it was, having no derivation. | **partly done** |
| **Gate:** no document-size ceiling is enforced, and the reason is recorded — the ~650 MB ceiling this row used to gate on was a **WASM** ceiling, withdrawn by ADR-0007's correction; natively that file opens in 144 MB and saves incrementally in 4.5 s (ADR-0010, invariant 17). What replaces it is the per-process budget row above, plus invariant 18: a save that fails never loses work. Reinstating a ceiling requires a native measurement showing one exists. | **done** |
| **Gate:** the PreToolUse write guard has been **observed to fire** — recorded in `docs/hook-probe.json`, verified by `proof:hookprobe` and enforced by `check:docs`, which fails if this row is marked done without the evidence. **Observed 2026-08-18T06:45Z:** a `node -e` call was denied by the guard, unprompted, while doing ordinary work. The denial is self-certifying — a session that never loaded the guard cannot be blocked by it — which is why the record accepts it despite the session predating the configuration. | **done** |
| **The control-character reporter is registered and OBSERVED BEING INVOKED** (findings AAAA-11 and AAAA-14). `scripts/hooks/reportControlCharacters.mjs` was registered for `Write` and `Edit` on 2026-08-24, and the harness was seen invoking it the same day: a `Write` under the reserved `.claude/hookprobe/` path made it emit its live-ness line, which reached the agent as a PostToolUse error. That is self-certifying — nothing that failed to load a hook can produce that hook's own output — so the session's age does not weaken it. **The record says `fired` with `certifies: invocation`, in a field and not in prose**, because a firing that quietly came to stand for detection as well would be the same widening one layer down; detection is `proof:reportControlCharacters`, whose fixtures build the bytes numerically because no keyboard emits them. The probe path is a second **trigger** and never a second detector: it decides only whether the live-ness line is emitted, and a probe file carrying a real control character is still reported as one. It REPORTS and cannot prevent: a PostToolUse hook runs after the write, so `guardFiles.mjs` at commit remains the only fail-closed gate on this class, unchanged. What it closes is **latency** — three `Write` calls emitted control characters on 2026-08-23, `guardFiles.mjs` blocked the third at commit and the first two were found by luck, in the window between the write and `git add` where the file is corrupt and the symptom is unattributed. **CORRECTED 2026-08-24 (finding AAAA-12).** This row said the registering commit "cannot be made from a session that started before it", and that was false in the direction that produces a false green: someone would register the reporter, watch the escape guard's probe pass, and read the gate as satisfied while the reporter had never fired anywhere — the exact widening the rest of this row warns about. The session-age rule applies to **one** outcome, a mechanism that was exercised and stayed **silent**, because that is indistinguishable from one that is absent. An observation that a hook **fired** is self-certifying — nothing that failed to load a hook can produce that hook's own output — and is accepted at any age. That property is what the gate is keyed on, not which hook kind produced the evidence. **Both steps are done as of 2026-08-24.** Registering changed `.claude/settings.json`, which is an input to every entry, so the escape guard's entry was re-established by running the probe verbatim — denied at 2026-08-23T22:28Z. **AAAA-14 is why the second step did not wait for the defect:** left as "closes the first time it catches one", the only certifying event would have been the defect recurring, and this repository has already sat in that state — `engine-host-containment` stayed green watching a symbol shipped code could never name. The escape guard's precedent is a probe input harmless in effect while being the banned shape; this hook has no equivalent, because its trigger is a byte nobody can author, so the benign trigger was supplied on a different axis. What is **not** closed and is stated rather than glossed: detection has never been observed in force. It is proven, and proof is not observation. | **done** |
| **Owed:** `npm run local` cannot sweep more than one `proof:*` script, and the debt is the MECHANISM, not the refusal (findings WWW-2, AAAA-6). A full sweep printed **35 failures at 0.0 seconds each** on one measured pass, every one of which passed when run alone. **WHAT they were is established as of 2026-08-24 (finding AAAA-6); WHY is not**, so the mode is refused with no override rather than repaired. **They never started a process.** The harness prints seconds to one decimal, so `0.0s` means under 50ms, and the cheapest possible successful spawn of node on this machine — a script whose entire body is `process.exit(0)` — measured **116ms at its fastest over 15 runs, median 129ms, max 179ms, 0 of 15 under 50ms** (measured 2026-08-24 by timing `spawnSync` around `process.hrtime.bigint()`). Nothing that started node can render as `0.0s` here, while a `spawnSync` that fails to *create* the process returns in ~3ms with `status: null` and zero bytes — that signature exactly. **Which errno is still unknown, for a reason that was in the instrument rather than in the evidence:** `checkLocal.mjs` read `status`, `signal`, `stdout` and `stderr` and never `error`, the only field naming why no process appeared. So AAAA-23's question — what did those 35 print — has an answer: **nothing, and nothing could have.** `scripts/lib/spawnOutcome.mjs` now classifies every spawn, the harness reports `DID NOT START` as its own state rather than as a failure, and **stops there**, for the reason it stops at a timeout: a machine that just refused to create a process will refuse the next one, and the founding observation is thirty-five in a row. That bounds the damage to one invented failure instead of thirty-five; **it does not explain the machine, so this row does not close on it.** **THE BOUND IS EARNED, dated rather than assumed (AAAA-22):** the obvious suspect is the harness defect recorded in `checkLocal.mjs` — `npm run` under a shell, where the timeout killed the shell and left node running, after which twenty scripts failed in 0.2s with no output. Both remedies, direct interpreter invocation and stop-at-first-kill, landed in `f7dc5fb` (2026-08-23T08:35+02:00); the 35-at-0.0s pass was recorded in `7b7824e` (2026-08-23T17:20+02:00), **nine hours later, on a harness that already had both**. Stop-at-first-kill rules out a cascade started by a kill **inside** that run and nothing about an **earlier** one — the comment above that break says a timeout's orphans *accumulate*, which is cross-run by construction, and a kill leaves tracked files deleted after its run has ended. So the boundary is *at most one proof* because the mechanism is unknown, **not** because kills have been excluded (AAAA-23). **Measured 2026-08-24, with provenance:** a 90s-capped reproduction killed `proof:hookprobe` at 90.04s with SIGTERM, its `finally` never ran, and `docs/hook-probe.json` was left **deleted** in the working tree — a direct observation independent of the cap. **WITHDRAWN, both previously stated here:** that sequence slows a script (`proof:shim` measured 1.5s and 9.4s at the *same* position across two runs, so machine state was never isolated), and that orphaned proof processes were accumulating — checked by command line rather than by age, two of the four are an MCP server with **living parents** spawned by the editor, and the other two were never identified. **No process on this machine has been shown to be proof wreckage.** Nor is the 0.0s signature explained: a missing record makes `check:docs` fail in ~130s, not instantly. **And the founding observation kept no evidence** — the harness prints one diagnostic line per failure, or `(no diagnostic line found)`, which separates a spawn that never started from an import-time throw; the 2026-08-23 pass preserved the count and the conclusion and not the lines, so that question cannot be asked of the only occurrence. Every run now writes its rows to `.cache/checkLocal-runs/` as it goes — one file per run, its **state in the filename**, so a run this harness killed stays `-running` for ever and is self-identifying in a listing (AAAA-25; one slot is destroyed by the act of investigating it, which is the sequence that lost the original evidence). **Three of that mechanism's four properties are ASSERTED as of 2026-08-24 (finding AAAA-29), where they had shipped on a single hand-run kill:** retention, as a pure decision over a directory listing — `scripts/lib/runLog.mjs` exists because `checkLocal.mjs` starts a sweep on import, so nothing could reach a function inside it without running one — the `-failed` seal on a timeout, and `-running` surviving a SIGKILL mid-sweep with the completed script's row still in it. `proof:checklocal` carries all three, so they are on the board on two platforms. **The clone route is the stated exception**, needing a real clone and a provisioned tree, and it is the piece whose failure would be met by the person investigating rather than by CI. **`.cache/` is gitignored, so this is a CAPTURE and never a record:** the rows must be copied into the journal entry by hand when an occurrence happens, and that copying step is exactly what was missed for the 35-at-0.0s pass. **Rule 0 permits a workaround only where the root cause is proven to lie *outside* this repository.** This is not that; it is unfinished work. **THE JOB-OBJECT HALF IS WITHDRAWN, 2026-08-24, because its premise did not survive measurement.** This row required "each script runs inside its own job object, so its children die with it", on the strength of `checkLocal.mjs` asserting that a timeout leaves the killed script's children running to accumulate. Measured, three runs of each variant against the real harness at `--timeout 5`, every survivor identified **by command line rather than by age** (the discipline the withdrawn orphan claim was missing): an ordinary grandchild **died with the harness 3 of 3**, a `detached` one **survived 3 of 3**. The discriminating variable is `detached` — the signature of the job object libuv already places an ordinary Windows child into — and this repository contains **zero** detached spawns. So on the platform this sweep runs on, the tree-kill the row asked to be built already happens for every spawn shape in use. That dependency is now a case in `proof:checklocal` rather than a sentence, **derived from the tree because the failure to fear is somebody adding one, which makes the set bigger** (item 4c). What a timeout genuinely leaves behind is a **changed tree** — a killed script never runs its `finally` — which is why the sweep still stops. **AND THE PLATFORM HALF IS NOW ASSERTED RATHER THAN RECALLED (finding AAAA-31).** A withdrawal removes a check instead of adding one, and this half is a property of the **runtime** — whatever libuv does with an ordinary Windows child is libuv's decision, and a node bump is precisely the event that would falsify it in silence, which made it a claim owed an expiry with nothing able to fire one. `proof:checklocal` now runs the differential on every push: the grandchild must be seen **advancing a counter** before anything is killed, because *it stopped* and *it never started* are otherwise the same observation. Both halves are asserted on their own Guards legs — win32 tears the tree down, and on Linux the ordinary grandchild **survives**, so neither side is a platform nobody runs — with a third control that kills the survivors (which it must do anyway) and requires the counter to go still, since a probe that can only report *advancing* would pass wherever survival is the expected answer. Mutation-tested both ways: making the ordinary probe detached reddens the win32 case alone, and removing the differential reddens the control **and** the stop-check, whose survivor list then goes empty. **The trigger is an event, which is why it lives here and not in the advisory register:** this row closes when the errno behind the 0.0s signature is established. Until then the refusal is an open question wearing a design decision's clothes, and `scripts/lib/sweepScope.mjs` states the same thing at the point a caller meets it. **RE-RANKED 2026-08-24 (finding AAAA-6): this is no longer a debt being carried, it is IN THE WAY.** It blocks the real fix for AAAA-16 — the sweep should *run* the proofs that read the files you changed, and it cannot, because running them means more than one `proof:*` in a run and this refusal forbids exactly that. So the unexplained wreckage now has a named consequence: on 2026-08-24 a push reddened `main` for a proof a local sweep could have executed and was not allowed to. What shipped instead is the interim — `scripts/lib/affectedProofs.mjs` **names** those proofs, derived from the changed set and the import graph, which is what is available while the refusal stands. **Naming them is the interim; running them is what closes this row.** A debt with a named consequence gets paid; one without gets carried. | **not done** |
| **Exit:** open via FileHandle → render → `rotatePages` + undo → save → one registered dialog, setting and shortcut | — |
