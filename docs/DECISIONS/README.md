# Architecture Decision Records

Every amendment to `docs/ARCHITECTURE.md` under rule B4 is recorded here, and
so is every decision a future reader would otherwise have to reverse-engineer
from the code.

## Why these exist

The expensive question in an inherited codebase is never "what does this do" —
it is "why is it like this, and what happens if I change it". An ADR answers
that in the place where it will still be found in three years.

The **rejected alternatives** section is the load-bearing one. A decision
recorded without its rejected alternatives reads as arbitrary, and the next
person re-derives the same options from scratch, or worse, adopts one that was
already ruled out for a reason nobody wrote down.

## Format

One file per decision, numbered sequentially, never renumbered:

```
NNNN-short-kebab-case-title.md
```

**"Never renumbered" is not tidiness, and here is the mechanism** (finding
UU-1). A renumber cannot be caught by link-checking, because **both targets
exist**: `check:docs` verifies that a link resolves, and no check can verify it
resolves to the *decision the sentence means*. The reader lands on a real ADR
that says something else, which reads as authoritative — **strictly worse than a
broken link, because a broken link announces itself.**

So a renumber is a **manual sweep of every prose reference**, done in the same
commit and never deferred, and it is a cost to weigh before splitting or merging
an ADR rather than a chore afterwards. It has been paid once: ADR-0022 was split
into 0022 (the process type) and 0023 (the mechanism), and three references in
`scripts/research/` still pointed at 0022 for questions 0023 answers.

The cheapest way to avoid the sweep is not to need it: **number a decision when
it is written, and split by writing a NEW number rather than by moving an old
one.**

Each contains:

- **Status** — Proposed / Accepted / Superseded by ADR-NNNN. ADRs are never
  deleted or edited to say something different; a reversal is a new ADR that
  supersedes the old one, and the old one stays as the record of what was
  believed and why.
- **Date**
- **Context** — the forces at play, including what was actually verified versus
  assumed.
- **Decision** — what is now true.
- **Rejected alternatives** — each with the reason it lost.
- **Consequences** — including the unpleasant ones. An ADR that lists only
  benefits is marketing.

## Index

| ADR | Title | Status |
|---|---|---|
| [0001](0001-agpl-on-the-microsoft-store.md) | AGPL-3.0 on the Microsoft Store | Accepted; mechanism corrected 2026-08-17 |
| [0002](0002-brand-mark-treatment.md) | Brand mark treatment — composite logo used as supplied | Accepted |
| [0003](0003-token-role-typing-and-declared-pairings.md) | Token role typing: five categories and declared pairings | Accepted |
| [0004](0004-toolchain-versions.md) | Toolchain versions, and two deliberate steps back from "latest" | Accepted; `lucide-react`'s licence corrected 2026-08-28, and the component-test vehicle added the same day |
| [0005](0005-ui-foundation-libraries.md) | UI foundation: Base UI, Zag machines, Lingui, zustand | Accepted |
| [0006](0006-engine-capability-spike-results.md) | Engine capability spike results, and the matrix amended to match | Accepted; corrected 2026-08-16, the same day |
| [0007](0007-memory-budgets-and-the-document-size-ceiling.md) | Per-process memory budgets, and the measured document-size ceiling | Accepted; largely withdrawn by correction 2026-08-17 |
| [0008](0008-save-mode-is-determined-by-purpose.md) | Save mode is determined by the purpose of the save, not by a default | Accepted; **Correction appended 2026-09-06 — item 4 has been executed and its answer is *no*.** A full save does **not** preserve foreign annotations byte-identically: two entries of ten re-encode on MuPDF 1.28.0, spelling rather than meaning, with the divergence set pinned. It does **not** invert the decision, which is the outcome item 4 was written to allow for — the property L5 protects survives a full rewrite, and a signature-bearing save was already routed to an incremental one. Items 1, 2, 3 and 5 remain unexecuted, and the full-rewrite default now rests on those rather than on item 4 |
| [0009](0009-document-identity-and-the-command-log.md) | Document identity, the command log, and the engine seam | Accepted; the identity mechanism corrected four times, 2026-08-18/19 |
| [0010](0010-native-mupdf-through-an-ffi-shim.md) | Native MuPDF through an FFI shim; WASM withdrawn | Accepted; instruments corrected 2026-08-17 |
| [0011](0011-engine-upgrade-cadence.md) | When the native engine is upgraded, and when it is not | Accepted |
| [0012](0012-memory-budgets-are-machine-read-from-the-invariant.md) | The memory budgets are machine-read from invariant §9.17 | Accepted |
| [0013](0013-pdfa-export-and-text-extraction-engines.md) | PDF/A-2b export gets a matrix row and no binary; the Poppler conditional is dropped | Accepted; **Correction appended 2026-09-10 — the TABLES half of the layout question is executed.** It was unexecuted because no fixture contained a table; the corpus grew to eleven documents and `FZ_STEXT_TABLE_HUNT` was scored on against off, changing 2 of the 6 carrying text at −1.5 and −17.5 points of line agreement with none improving. It stays off. MuPDF is not found insufficient and Poppler does not return |
| [0014](0014-ocr-stays-inside-the-engine.md) | OCR stays inside the engine: Tesseract and Leptonica are kept | Accepted; **Correction appended 2026-09-10 — ground two does not survive.** *"Stage 6 needs them … the integration it needs is exactly the one already present"* is true of the shim and **false of the engine the application loads**: `tesseract`, `leptonica` and `ocr_` occur 0 times in the WASM binary the kernel's own import resolves to and 0 across its whole surface, against 2, 4 and 9 in `monstera_mupdf.dll`. The decision stands on grounds one and three; recognition is `tesseract.js` by §3's new matrix row, and `OCR_DISABLED` becomes a live option because the re-add cost it was rejected over is now zero |
| [0015](0015-a-filename-may-not-select-a-native-library.md) | A filename may not select a native library (invariant 23) | Accepted |
| [0016](0016-the-document-handler-set-is-named.md) | The document handler set is named, not inherited | Accepted |
| [0017](0017-the-security-substrate.md) | The security substrate: invariants 24 and 25 | Accepted |
| [0018](0018-distribution-is-the-microsoft-store.md) | Distribution is the Microsoft Store | Accepted |
| [0019](0019-the-renderers-csp-is-pinned.md) | The renderer's CSP is pinned as invariant 27, and `'unsafe-inline'` is dropped rather than pinned | Accepted; the contrast it argues from corrected 2026-08-23 |
| [0020](0020-the-preload-is-bundled.md) | The preload is bundled to CommonJS and enters the contract by a leaf export | Accepted; one stated reason corrected 2026-08-21 |
| [0021](0021-the-canonical-image-is-retained.md) | The canonical image is retained, one per open document, under a supplied ceiling | Accepted |
| [0022](0022-the-engine-host-is-a-process-we-create.md) | The engine host is a process we create, not a utility process | Accepted; two stale references corrected 2026-08-22/23 |
| [0023](0023-how-the-contained-engine-host-is-built.md) | How the contained engine host is built | Accepted; corrected 22 times, 2026-08-22 to 2026-08-28 — read the corrections before the body. Decision 15 (2026-09-01) puts invariant 25(c) on the startup check |
| [0024](0024-execution-mode-is-a-placement-axis.md) | Execution mode is a placement axis, and `packages/nodemode` is the Node-mode side | Accepted; a cost read off a file rather than paid, corrected 2026-08-25 |
| [0025](0025-mains-baseline-budget-is-derived-from-what-it-must-catch.md) | `main`'s baseline budget is derived from what it must catch, and 96 MB was not | Accepted; the ceiling, `R`'s runtime and the drift attribution corrected 2026-08-27, and a bound withdrawn 2026-08-28 when `perf:gate` turned out never to have run in CI |
| [0026](0026-a-declaration-is-not-an-implementation.md) | A declaration is not an implementation, and the kernel's public surface carries no native binding | Accepted |
| [0027](0027-a-development-grant-belongs-to-provisioning.md) | A development grant belongs to provisioning, not to the application | Accepted; corrected 2026-08-27 — the grant is necessary and not sufficient, and the set is reshaped |
| [0028](0028-main-holds-the-process-creation-binding.md) | `main` holds the process-creation binding, and §9.17's `main` clause is amended to say so | Accepted; §9.17 amended 2026-08-28 |
| [0029](0029-how-the-registries-are-built.md) | How the registries are built: registration is a value, not a side effect | Accepted as a design; nothing built |
| [0030](0030-a-remote-writer-does-not-open-from-an-image.md) | A remote writer does not open from an image, and the bus never asked it to | Accepted |
| [0031](0031-the-renderer-reads-the-document-by-demand-paged-ranges.md) | The renderer reads the document by demand-paged byte ranges, not a per-version snapshot | Accepted; §2 amended 2026-08-29; **corrected 2026-08-30** — Decision 1's *"the view model already carries bounded structured data"* was false when written, and ADR-0032 is where it stopped being |
| [0032](0032-the-view-model-is-a-scoped-query.md) | The view model is a query scoped to the pages the renderer draws, not a delta the command returns | Accepted; §2 amended 2026-08-30 |
| [0033](0033-a-ratio-budget-governs-a-process-that-holds-bytes.md) | A ratio budget governs a process that holds bytes, not one that parses them | Accepted 2026-09-01 — answers the failed `mupdf-host` gate per `BUILD-PROMPT.md:680`; §9.17 restated to `3 GB, base 128 MB`, the multiple withdrawn. Gives up amplification detection, stated in the ADR |
| [0034](0034-the-text-substrate-owns-the-engines-options-not-its-own-clusterer.md) | The text substrate owns the engine's options, not a clusterer of its own | Accepted 2026-09-02 — discharges ADR-0013's unexecuted columns-and-tables half by measurement; supersedes `BUILD-PROMPT.md` Part E2's mechanism, §3.2 amended. `FZ_STEXT_SEGMENT` on, `TABLE_HUNT` off with a trigger. **Addition 2026-09-02** — Part E2's *"deep tuning lands with Stage 5"* had lost its subject when the constants went, so tuning becomes a scored choice among the engine's options, and a Stage 5 post-pass that reads GEOMETRY to decide grouping is the second extraction path K.0 bans |
| [0035](0035-extracted-text-is-never-resident-in-main.md) | Extracted text is never resident in main, and search is a per-page query | Accepted 2026-09-02 — answers D1's invariant-11 gate for the first channel that could break it; §9.17's `main` clause amended. Measured at **3.59×** the file size for a text-heavy document, against a 1.5× budget |
| [0045](0045-a-removals-garbage-collection-belongs-to-the-command.md) | A removal's garbage collection belongs to the command that removes | Accepted 2026-09-07 — amends §4 and §8's seam. §4 has required a removal-purpose command to classify itself since 2026-08-16 and flatten is the first one; there was nowhere to put the classification, and the cost is measured rather than argued: `bake(false, true)` unlinks nine widgets and `saveToBuffer("")` writes all nine out again, the object count **growing** 49 to 55, so every flattened field's value stays readable to anything walking the xref instead of the catalog. A command declares `purpose: 'ordinary' \| 'removal'` — and, per the correction below, the adapter that owns the session carries it rather than `serialise` taking it. **The second half is the decision**: the collection happens where the bytes are MADE, not where they are written — `saveDocument` takes a `flush` and cannot collect bytes handed to it already serialised, and deferring to the disk save leaves the canonical image, the checkpoint, `readRange` and every copy/extract/export path each re-deriving the removal rule (B3a). Collecting every save rejected on ADR-0008's *never a default* and on `foreignAnnotations.test.ts` pinning a divergence set measured for the plain path only; a `serialiseForRemoval` sibling rejected on B5, since a second method makes not implementing it the silent default. The axis has two members where §4's table has three rows: *incremental to preserve a signature* is a property of how a file is written, not of what a command's bytes contain, and it arrives when Stage 7 has a signature. **Corrected 2026-09-07**, the same day, while building it: the purpose is the **session's**, not the executing command's, and one sentence supporting the body was false when written — `CommandBus.execute` serialises **before** apply to mint the checkpoint, so a live-session removal produces no bytes of its own, and MuPDF exposes no in-session collection. The orphans live in the session until it closes, so the adapter that owns the session carries the fact and `serialise` takes no parameter. That partly rehabilitates the rejected *classify from the command log*: the objection was to its SCOPE, since classifying one call site leaves four leaks and classifying every serialise is the design |
| [0046](0046-a-strict-xfdf-reader-rather-than-an-xml-parser.md) | A strict XFDF reader, not an XML parser | Accepted 2026-09-08 — amends nothing; the founding record names XFDF once, in a feature list, with no guidance. No workspace here declares an XML dependency, so reading one needed a new production dependency on a hostile-input path or a reader of our own. **Three of the six measured shapes are DTD features**, so refusing `<!DOCTYPE` answers XXE, entity expansion and the external DTD together and a depth bound answers the fourth — which removes what a general parser is valuable for and leaves a fixed, shallow schema. A bound on the VALUES is not among the answers: entity expansion happens inside the parser before any value exists to bound. This build already writes XFDF, so a dependency would split one format across two owners (B3a). **Against it, recorded rather than answered:** a hand-written reader on a hostile path has its own bug surface, which is why the refusals ship as declared cases with negative fixtures for all six shapes. **Re-argue trigger:** the reader needing to grow past the XFDF shape — a namespace-prefixed vocabulary, a non-UTF-8 encoding, or a second XML format in this codebase |
| [0049](0049-the-editor-groups-its-own-engines-runs-and-a-person-confirms-the-grouping.md) | The editor groups its own engine's runs, and a person confirms the grouping | Accepted 2026-09-09 — amends §3's matrix row and §3.2, and takes [ADR-0034](0034-the-text-substrate-owns-the-engines-options-not-its-own-clusterer.md)'s **route 2**, which reserved *"a recorded engine gap"* for an engine that cannot answer. **The gap, measured** (`scripts/research/pdfiumLines.mjs`, PDFium 155.0.8044.0, 2026-09-09): `FPDFText_CountRects` answers **4 rects for four runs** whether the two sharing a baseline sit 170pt or 3pt apart, so PDFium's rects are per-RUN and it groups nothing into visual lines — route 1, *an option on the engine's own answer*, has nothing behind it. **What it does answer** is `FPDFText_GetTextObject`, which resolved 40 of 45 characters to their page objects, the remaining five being PDFium's own *generated* spaces that `FPDFText_IsGenerated` flags — so a range of text becomes editable objects **inside one engine's frame**. The cross-engine alternative stays refused on evidence: `proof:lineagreement` scored **52.9%** of our lines verbatim among an independent reader's, so a join between the reader's lines and the editor's objects would replace the wrong text about half the time, which is `pageNumbering.ts`' failure one engine worse and with no shown value to disagree with. **Decision: the editor owns a grouping of PDFium's runs by vertical OVERLAP** — a relation and not a threshold, so Part E2's *"constants change only with a corpus score"* has nothing here to govern; exact equality was rejected on the same run, where two runs on one baseline came back with tops 237.9 and 238.0. **What keeps it outside ADR-0034's ban is where its output goes rather than what it reads.** That ADR's test is *does it read a coordinate to decide grouping*, and this does. An extraction path's grouping becomes text somebody takes as the document's content — feeding search, export and the text layer, where a wrong answer is silent; this one reaches **a dialog and nothing else**, so a wrong answer is on screen beside the words it will replace, with a dismiss. The rule stays checkable: *does this grouping's output reach any consumer other than a dialog a person answers?* — and a second caller is the moment it has become an extraction path. **Also settled: the chooser shows TEXT.** The region-replacement row's channel deliberately carries none, because an object's string is prior state; here the text is the thing being edited, and a person cannot edit what they cannot see. **Rejected:** the geometry join (52.9%); PDFium's rects as lines (measured per-run); a vertical-overlap TOLERANCE, which is the constant "required to mirror exactly" that Part E2 describes; joining by TEXT rather than geometry, which fails on repeated text, hyphenation and PDFium's generated spaces; and no line-level editing at all, which would leave the stage with the mechanism and none of the feature |
| [0048](0048-what-a-second-engine-host-owes-and-what-it-holds.md) | What a second engine host owes, and what it holds between commands | Accepted 2026-09-09; **corrected 2026-09-09, the same day, by writing the host against it — `engine/serialise` is NOT one of the seven and a second host in the same AppContainer is not contained from the first, so read the correction before Decision 1's table.** A byte-image host's session has no current bytes, so that channel has nothing behind it and `engine/apply` carries its answer — a count, into the granted directory, which is `engine/serialise`'s own result schema; `CommandExecution<W>` had declared the asymmetry all along. **A channel is engine-agnostic when its ANSWER means the same thing, not when every engine can be asked it.** The correction also settles ADR-0047's deferred *how the input bytes reach the host*: names, never places, on `apply`/`capture`/`invert`, supplied through the schema set an engine hands `coreEngineChannels` — cost is two whole-image writes and one read per command, and *whether capture and apply share one open* stays open with a second reason to measure it. And the third thing this ADR did not name: `hostSessionDirectoryDacl` grants a SID derived from a shared profile moniker, so a second host needs its own profile or each reads the other's areas. **Corrected AGAIN the same day, and this one WITHDRAWS Decision 3 in full** — found by writing main's side of the call. A per-document open mints a per-document id and main has nowhere to keep one: the bus hands a byte-image writer the document's BYTES, which carry no identity, and `SessionsByWriter`'s slot is typed the same way. And the benefit claimed — one `open-failed` handling for both hosts — is the wrong handling for the second: main POISONS a document whose engine session cannot be created, which is right for the engine it is read through and wrong for one only needed to edit. A byte-image host's `engine/open` registers a granted AREA and parses nothing; the area's lifetime is the host's, a transfer buffer with a fresh name per call; a document this engine cannot read fails the call that needed it. Decisions 1 and 2 stand. **The general form: the two hosts differ as `byte-image` and `live-session`, not as PDFium and MuPDF**, so the factory takes an engine's commands plus one WIRE SHAPE constant per `writerShapes` entry — amends §3, which has required *one host body, parameterised by engine* since 2026-09-08 and left two things unstated that a second host cannot be built without. **Decision 1: a second engine owes the seven engine-agnostic channels and its own reads, and NONE of the twelve MuPDF document-model ones.** `engineChannels` holds nineteen; seven are engine-agnostic in shape (`probe-containment`, `open`, `serialise`, `close`, `apply`, `capture`, `invert`) and twelve are MuPDF's document model. A PDFium host that declared the twelve and stubbed them would be a process answering questions with nothing behind it — the wired-tools rule at process scale — and implementing them for real would be a second reading of a model this package already has one reader for (B3a). `EngineChannelsFor<W>` is what says it: main's PDFium client cannot express `engine/annotations`, which is B5 over a runtime refusal, and the runtime still terminates on an undeclared channel because the peer is hostile by invariant 25's premise. **Decision 2: the table entry is a granted AREA, and a document session is what a live-session engine adds to it.** `HostSession` holds its two directories against the id deliberately — *a `serialise` that carried a directory would be a channel through which a confused main could redirect the document's bytes on every save* — so a byte-image host that took directories per call would give up a containment property as a side effect of ADR-0047's decision about where a parse lives. The entry generalises to the pair main already names `SessionArea`. **Decision 3: a byte-image host's `engine/open` parses once and discards it**, so `open-failed` means *this engine cannot read this document* from both hosts at the same point in the same protocol; the cost is a parse the first command would perform anyway, and what it costs on a large document through PDFium is **not measured and not claimed**. **Not decided:** how input bytes reach a byte-image host (ADR-0047's open item), whether capture and apply share one open, HD render's reader session, and the containment branch — ADR-0023 Decision 16 is decided and unmeasured, and gates the branch rather than the body. **Rejected:** a copied host body (§3, 2026-09-08); one process serving both engines (invariant 25); one channel map with a runtime refusal; a wholly stateless host; and deferring until the second command, which would let the first one decide a table whose shape is a containment argument |
| [0047](0047-an-in-place-text-edit-is-a-byte-image-command.md) | An in-place text edit is a byte-image command, and it generates content once | Accepted 2026-09-09 — amends §2 and §8's seam: `writerShapes.pdfium` becomes `'byte-image'`. `engineSeam.ts` had declared it `'live-session'` since Stage 0 with nothing behind it, and building the second host on that would have answered **by accident** the B4 `savePipeline.ts` carries in writing — *two live-session writers each return the whole document from `serialise`, and nothing in the law says which bytes win* — which ADR-0039 Decision 2a names as firing on "PDFium in Stage 5". **The shape argument needs no timing:** the renderer reads `main`'s canonical image through `PDFDataRangeTransport` and the view model carries `{version, pageCount, rotations}`, so a live-session edit is sound, undoable, savable and **invisible** — ADR-0039 Decision 3's defect in a second engine. Making it visible means the bytes become `main`'s image, which is the round trip a byte-image command already performs, so the live session buys nothing the renderer can use and costs the rule, a session table in a contained host, and a second live entry in `SessionsByWriter`. **Decision 2 is measured** (`proof:editcost`, seven controls): `FPDFText_SetText` is 0.007–0.029 ms and flat while `FPDFPage_GenerateContent` after a set runs 0.17 → 29.18 ms and tracks the **document's content**, not the edited page — 500 pages of one line costs 12.54 against 500 × 40 at 29.18, and 50 × 400 costs 23.93 with a tenth of the pages. Forty replacements: **199.6 ms generating per call against 14.6 ms generating once, 13.7×**, one generate flat in k. So generation belongs to the **command**, ADR-0045's shape one operation along, and `replaceTextObject`'s set-and-generate is right for exactly one replacement. The PDFium mechanism behind the scaling is **not** established and is not guessed at. **Not decided:** how the input bytes reach the host (`ByteImageAccess.current` answers a `ByteImage` in `main`, which is the 2.00× ADR-0030 exists because of; the candidate is `adopt`'s `SnapshotWrite`), whether capture and apply share one open, and the host's generalisation. HD render's session is a **reader** and is explicitly not the two-writers problem. **Corrected 2026-09-09**, the same day: *"buys nothing the renderer can use"* overstates it — a live session avoids the **input** serialise, roughly 60 ms of 190 on a 997 KB document, and `commandDeclarations.test.ts` had the arithmetic written down before this ADR was drafted (*"for an INVERTIBLE one there is no checkpoint, and the serialise is a cost its live-session equivalent does not pay"*), a text replacement being exactly the invertible case. The decision stands on kind rather than size: that saving is available **inside** this shape, since `adopt` makes the new bytes main's canonical image and `current()` then re-serialises to reproduce them, while the coherence rule the other shape costs is not optional. The `current()` branch is **owed**, triggered by the first PDFium-routed command |
| [0044](0044-an-image-reaches-the-engine-the-way-the-document-does.md) | An image reaches the engine the way the document does | Accepted 2026-09-07 — place image and stamps are one object, a `/Stamp` whose appearance draws an image XObject, and the image had no route to the writer. **The recorded block was the wrong wall**: row 128 said *"what blocks it is a number"*, and the host's wire is JSON, so a `Uint8Array` arrives as an object of numeric keys at **8.4×** the bytes and every schema demanding one refines it away — raising `ENGINE_HOST_FRAME_MAX_BYTES` fixes nothing. §3's matrix is **not** amended; an `asset: 'none' \| 'image'` axis joins `sources` and `reads`, and the bytes travel `snapshotDirectory`, the granted directory the document itself arrives through, in the direction the host may only read. The pdf-lib alternative was **executed**: it writes the stamp, MuPDF's walk sees it, `setRect` moves it without regenerating the appearance, delete removes it, the private mark survives — and it loses on a cost keyed on object count, **197.3s and 224.8s in two runs at 127,082 objects against 0.57s and 0.64s at 122 objects and 199 MB**, which is a finding about the shipped byte-image rows and is not acted on here. Built-in stamp artwork keeps its own block. **Corrected 2026-09-08:** the member is `'bytes'`, not `'image'`. The axis asks a transport question in this ADR's own words — *bytes the wire cannot express* — and carried a content label from the day it was written; nothing branches on the member, so an FDF declared as an image would have worked and been a lie, which is why it is renamed rather than reused |
| [0043](0043-an-annotation-this-build-wrote-carries-a-private-mark.md) | An annotation this build wrote carries a private mark, and its absence is what foreign means | Accepted 2026-09-06 — invariant L5 named a `srcRef` marking scheme and defined none, so *annotations it did not author* was never a computable set; ADR-0041 said explicitly that a handle answers *which one* and not *may I rewrite it*, and the eraser and select are the first callers of the second question. The mark is a **private key on the annotation's own dictionary**, `/Monstera_Authored`, boolean `true`, written at the one creation site and read at the one walk — measured surviving a save, a reopen and a second save on MuPDF 1.28.0, with an absent key reading as null rather than throwing. **One-sided by construction**: we may not write onto a foreign annotation, so nothing can ever be marked foreign and absence is what foreign means. **Provenance, not permission** — erasing or moving somebody else's annotation is what the tools are for; L5 forbids the pipeline touching an object the person did not aim at. `/NM` rejected (a name **producers write**, so reading it as provenance misclassifies most foreign annotations, and it is one of the two entries MuPDF re-encodes); `/T` rejected (it means the author a person will type, which the first comment row collides with); a table in main rejected (does not survive the save and reopen L5 is about); inferring from `/AP` rejected (right about this project's fixtures, wrong about real documents). **L5's own body is rewritten in the same commit**, an invariant being a live specification rather than a record: byte-identity was measured 2026-09-06 and is **false** — two entries of ten re-encode, spelling not meaning — while text-identity holds, and the divergence set is pinned. **Amendment only: nothing is built on it in this commit.** |
| [0042](0042-a-gesture-may-span-several-presses-and-the-tool-says-when-it-is-complete.md) | A gesture may span several presses, and the tool says when it is complete | Accepted 2026-09-06 — the tool lifecycle modelled one press, some movement and a release, which eight tools needed and polygon, polyline and cloud cannot express: `down` called `begin` on every press so a second vertex discarded the first, `up` cleared and committed so a three-vertex shape committed after one, and nothing carried a finish signal because for a drag the release *is* the finish. The cheap widening was **checked for first and found unnecessary** one commit earlier — a click tool reads `startOf` and discards the rest, costing the platform nothing — which is why this is a B4 rather than a fourth reflex. `Gesture` records **`presses`** beside `points`, its second widening and the same move as the first; rejected were a `press` controller member (five lifecycle members, eight tools declaring a press means nothing to them) and deriving vertices from the decimated path (a click and a slow drag through one pixel are identical in it). `ToolController` gains **`complete(gesture)`** asked at pointer-up, and **no existing tool implements it** because the shared `pointerPath` answers `true` — which is what separates it from the `cancel` member the registry rejected as an empty body in twenty tools. Rejected: an optional member, where a misspelling silently gets the default; and a third `commit` return, putting the lifecycle decision inside the payload builder. The finish signal is a **double press** recorded by the platform and read by the tool. **Measured before designing**: `Polygon.setVertices` stores `/Vertices` and computes `/Rect`; `Polygon.setRect` is refused; `Polygon.setBorderEffect('Cloudy')` stores `/BE << /S /C /I 2 >>` and grows `/RD` from 2 to 11; and `PolyLine.setBorderEffect` is **refused** — so a cloud is a polygon with a border effect and the format rejects the polyline reading outright. It also dissolves the **callout** question: one drag defines a box or a pointer and not both, and multi-press removes the premise. **The honest limit** — `complete` is asked only at pointer-up, so a tool finishing on a move, a timer or a keystroke is the stated trigger for the other call sites. **Amendment only: nothing is built on it. Corrected 2026-09-06** — the §6 clause this quoted as *what is being superseded* was itself false in three of its four entries: `cancel` was never a member, `preview` is one and went unnamed, and `commit` had been able to answer a Promise since the text box four commits earlier. `begin` and `update` stayed right, which is the half a reader checks. So the correction restated three stale claims in the sentence naming what it corrected, and quoting the clause is what made it feel checked. §6's body is fixed; the decision's own reasoning is unaffected, because the seam it could not express is a fact about the overlay rather than about the clause |
| [0041](0041-an-annotation-is-named-by-its-place-in-a-walk-and-a-version.md) | An annotation is named by its place in a walk, and the walk's version | Accepted 2026-09-06 — `document.annotations` answers with page, kind and contents and no identity, so the eraser, select, `addAnnotation`'s inverse and a jump to the annotation could not say *that one*. Named by `{ page, index }` where the index is a position in **MuPDF's own annotation walk and not the page's `/Annots` array**: measured on a page carrying a text field and three squares, `getAnnotations()` answers 3 where `/Annots` holds 4, because widgets are filtered — so the two indices differ by the field count above them on every document Stage 4 exists for. One function mints and resolves, since *which objects here are annotations* is MuPDF's rule (B3a). The payload carries the `DocVersion` the walk was read at and is refused if the document moved, which is ADR-0031's rule on a different noun: a stale offset builds a document out of two versions, a stale index deletes an annotation out of two of them. **No command payload has ever carried a version**, which is what makes this a B4 rather than a registration. `/NM` rejected — the format's own answer, unwritable on a foreign annotation under `srcRef`, and *`/NM` where it exists, position where it does not* is two schemes for one question; content addressing rejected because two stacked identical marks become unerasable and the rect is the select tool's to move. **Corrected 2026-09-06** — *nothing is built on it* was true when written and false one commit later: `5a5c87b` built the mint, and the ADR said otherwise until the commit after that. The mint exists and **nothing consumes it**; no command names an annotation, so Decision 2's refusal has no caller and Decision 3's declared axis is unbuilt and is the next unit |
| [0040](0040-a-command-names-a-second-document-by-docid.md) | A command names a second document by `DocId` | Accepted 2026-09-04 — `Apply<W, K>` takes one session, so merge, insert-from-PDF and replace-page could not be expressed at all; §3's matrix names them, so the architecture must accommodate them. A command names another **open** document by id, the sessions reach the bus as a resolved map rather than a lookup, and a `sources: 'none' \| 'one'` axis keeps every one-document command's `apply` signature unmoved. The visible cost is deliberate: *Insert from PDF* opens the source as a tab, because a transient hidden open would be a second document-opening path re-answering identity, dedup, the byte ceiling and the contained directory. **Extended 2026-09-05** — an apply may also need a value READ through another engine, so a `reads: 'none' \| 'outline'` axis sits beside `sources`; generate-TOC asked for it, since a byte-image apply holds no session and a pdf-lib TOC would be a second opinion about the outline that `destinations.ts` owns. **Corrected 2026-09-05** — the `sources` axis is now built, and building it showed two things the ADR stated too strongly: `Apply` needed a third type parameter rather than a lookup (the seam cannot import the declarations that import it), and the axis **binds in one direction only**, since a two-parameter apply is assignable to a three-parameter signature. No command declares `'one'`; Decision 3's bus parameter and the `reads` axis are still unbuilt |
| [0039](0039-a-byte-image-writer-round-trips-the-live-session.md) | A byte-image writer round-trips the live session | Accepted 2026-09-04; **corrected 2026-09-04 — the cost argument's reason was backwards and its scope is narrower than its sentence, so read the correction before the body**; **corrected again 2026-09-09 — Decision 2a named PDFium as the second live-session writer whose arrival fires `savePipeline.ts`'s open B4, and ADR-0047 made PDFium a byte-image writer, so the note stays live and PDFium is not what fires it**; **extended 2026-09-09 — an invertible byte-image command is priced against the choice a declaration actually makes.** The 2026-09-04 correction compared it against its *live-session* equivalent, which is true and is not the axis: the writer of record is settled by §3's matrix before invertibility is asked, so the comparison is invertible against terminal with the writer held fixed — and there `#sessionFor`'s serialise is common to both, leaving **retention** as the whole difference (`CommandLog.trimTo`: *"an invertible entry retains no document-scaled bytes"*, against one whole document image per terminal entry). Text editing is what makes that structural rather than tidy. The shared serialise stays unmeasured, and the remote round trip is ADR-0047's open question, not this one's. `commandDeclarations.test.ts`' trigger case has had its instruction carried out and is replaced by the retention property when `replaceTextObject` lands. amends §2 and §8's seam so the seven content-generation rows routed to `@cantoo/pdf-lib` become buildable. Its input is the live writer's `serialise`, never main's canonical image, which OOOOO-1 measured as stale for the life of an open document; its result replaces the live session through ADR-0037's restore path and becomes main's canonical image. A pdf-lib session is minted per call and never stored, which makes *which bytes win* unaskable rather than answered. Takes the byte-refresh ADR-0032 rejected, on the trigger that ADR wrote: the cost was one serialise per command where none was performed, and is now none per command on a path that already performs one |
| [0038](0038-a-dialog-answers-the-command-that-opened-it.md) | A dialog answers the command that opened it | Accepted 2026-09-04 — amends §7's dialog registry row, which had no way for a dialog to produce a value. An entry may declare a **result schema**; `ask` returns a promise settling with the parsed result or `undefined` on dismissal, and the **command** dispatches. The gate is then structural rather than a rule: a dismissal produces no value to apply. `resolve` is not a prop, because a function in a `.strict()` validator is B7's `any` argument one layer down |
| [0037](0037-checkpoint-restore-and-the-replay-that-is-not-needed.md) | Checkpoint restore, and the replay that is not needed | Accepted 2026-09-04 — chooses invariant 18 clause (ii)'s mechanism once both its triggers had fired. `CheckpointRestoreNotBuiltError`'s two stated reasons were stale: the ownership question was answered on 2026-08-28 (the supervisor), and the replay §4 describes is **empty for every terminal entry**, because the checkpoint is minted immediately before the command it is stored on and undo is last-applied-first. That property expires as a compile error, not silently |
| [0036](0036-the-error-boundary-is-the-one-class-component.md) | The error boundary is the one class component | Accepted 2026-09-03 — amends B7's *"React function components only"* with a second confined exception, `monstera/no-class-components` enforcing it. React 19.2.8 declares `getDerivedStateFromError` on `StaticLifecycle` alone; there is no error-boundary hook, and an uncaught throw unmounts the whole tree. **Corrected 2026-09-03**, the same day: placement below the state is necessary and not sufficient — the remounted scroller reports its seeded first page over the preserved one, so the retry re-issues `goTo` |
