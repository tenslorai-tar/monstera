{
  "$comment": [
    "The last commit covered by a stage audit whose findings are written into docs/JOURNAL.md.",
    "",
    "The stage audit used to be periodic — run at the end of a stage, against the tree. That was",
    "right for the 43-finding audit, which caught things that had sat for weeks. It is the wrong",
    "shape for what the evidence now says produces most defects: this project's own record shows",
    "them arriving INSIDE the proofs and instruments written an hour earlier to close the previous",
    "defect. The separator gap that gave the guard its only false negative, the crash the",
    "history-reach fix introduced, the verdict state that printed in no output, and two wrong",
    "entries in a licence notice were all fix-induced.",
    "",
    "So the audit is scoped to a range rather than a moment. `npm run audit:scope` reports",
    "watermark..HEAD, and CLAUDE.md's checklist is applied to that range instead of to the tree.",
    "",
    "This file advances ONLY in the commit that writes the findings, and check:docs enforces that",
    "by requiring this sha to appear in docs/JOURNAL.md — the same shape as a FEATURES.md row that",
    "turns the check red when it is claimed without evidence.",
    "",
    "ORIGIN, CORRECTED 2026-08-18. It first read 710cd94 with 'batches 1-7, each audited at close'.",
    "That was ASSERTED from the presence of batch COMPLETION records, not established from audit",
    "records, and it is wrong: docs/JOURNAL.md marks Batch 7 'PART DONE'. A batch that never closed",
    "was never audited at close, and Batch 7 is 31 commits.",
    "",
    "The origin is now a969ae4 — 'Record Batch 6' — the last commit of the last batch that closed.",
    "Batches 1 to 6 each carry a journal entry with findings; 3 and 6 carry explicit stage-audit",
    "sections. That is a defensible lower bound. 710cd94 was not.",
    "",
    "The consequence is immediate and correct: the gate reports an audit owed for Batch 7 from the",
    "day the mechanism exists, rather than starting green on a claim nobody checked.",
    "",
    "This is the failure the origin was always going to have. It is the one value in this mechanism",
    "nobody revisits, so an assumed origin makes every later range inherit the gap in silence —",
    "which is precisely what the watermark exists to prevent. It survived one question."
  ],
  "commit": "7669d69",
  "audited": "Batches 1-6; Batch 7 in four ranges (a969ae4..2aaa8f7, ..63242af, ..bcdf209, ..513b061), findings R1-1, R2-1, R3-1; 513b061..caa59d0 folded into the security-substrate amendment; caa59d0..d9f01b0 — identity, DocumentService and the advisory register — findings R-1 to R-8, both open ones since closed in f1737be and fe2274b; and d9f01b0..8f097e3 — the lane, the version counter and the command declaration — findings S-1 (the compile-fail harness accepts any error as a rejection, so a case can pass for a reason it does not claim) and S-2 (the engine seam's both-shapes claim has no control until a byte-image fixture exists); and 8f097e3..b315e2c — the compile-fail harness hardening, the guard's NUL widening and the engine seam — findings T-1 (a reachability verdict's path glob has a positive control and its SYMBOL has none, so a misspelt symbol leaves the verdict green forever; measured) and T-2 (the adapter's retention test asserts equal LENGTH where equal bytes were available and hold). Both since closed — T-2 by comparing the bytes, T-1 by requiring every symbol to be derived, witnessed, or printed as unverifiable against a separate count; and b315e2c..77d4c2c — the witness rule, rotatePages and the command log — findings U-1 (the file guard scans C0 plus 0x7f byte-wise and cannot see the Trojan Source class at all, CVE-2021-42574; verified absent from all 183 tracked text files with a positive control, so a gap rather than a defect) and U-2 (the modified-proofs column reports NET range change, so a proof loosened in one commit and re-tightened in another shows zero deletions; measured, and open with the fix named) — both since closed; and 77d4c2c..d61d995 — C's inverse, the capability narrowing and the log moving onto the record — findings V-1 (the append-only control in auditScope.proof.mjs has never tested anything: its fixture is ADDED in the range so it never reaches the modified-proofs column, and `appended === undefined ||` makes not-found pass) and V-2 (the report a human reads has no test at all — the proof exercises auditScope, never scope.mjs, so deleting the hidden-deletions line passes every case). Both since closed; and d61d995..f144768 — section 9's failure type, the composition point and the first IPC handler — findings W-1 (this report's own proof classifier matched *.proof.mjs and proofs/ only, so every *.test.ts was invisible to both columns: a range that added 254 lines of test carrying its strongest control reported 'proofs ADDED: none'), W-2 (the contract compile-fail proof holds its fixtures as STRINGS, so typecheck cannot see them; it drifted twice in one range, once leaving main red for three pushes), W-3 (the reachability walk reads tracked files only, so a new source file is invisible to every verdict until it is staged — not a defect, an unwritten consequence) and W-4 (a comment in a scanned file is scanned text, second occurrence). All four closed in the recording commit; W-2 with a conditional pre-commit gate rather than a rule; and f144768..81b9b2b — the lockfile repair, the toolchain pin, the write-target correction and the first CI runs in two days — findings G-1 (HEAD did not typecheck and the file that failed was a control; proof:toolchain, lint and test were run before committing and typecheck was not), G-2 (NOTICE was under-declaring mupdf@1.28.0, a bundled production AGPL package, because the pruned lockfile marked it dev), X-1 (the audit report's INSTRUMENT column was scoped to scripts/ while a filesystem probe landed under packages/kernel/src — W-1's blind spot recurring by root rather than by pattern), X-2 (that probe's own case-folding guard read the whole path instead of the basename, so it was measuring whether the DIRECTORY name folds; found by writing its first test), X-3 (proof:documenthandlers took its control PDF from a gitignored directory another step writes — present locally, absent on a runner, skipped on 13 runs and failed the first time it ran) and X-4 (proof:hookprobe asserted check:docs' global exit code, so it went red for an unrelated gate). All six closed; and 81b9b2b..9303bb5 — the Node floor job, the error reporter and the publish decision, a range made almost entirely of instruments — findings Y-1 (four proofs print a fixed roster of `ok` lines and a hand-written total that no executed case has to justify; measured by deleting a check and watching its `ok` line still print, exit 0 — and preCommit.proof.mjs fixed exactly this for itself inside the same range, leaving four siblings), Y-2 (the pre-commit gate measures watermark..HEAD, where HEAD is the parent, so the commit that crosses one batch is invisible to it by construction and the board goes red one push later; specified as commits + 1 and files unioned with the staged list, with a bidirectional control and a check that it cannot block the audit-recording commit), Y-3 (the reporter closed sixteen top-level handlers by enumeration and nothing stops the seventeenth — a claim with an unstated expiry, whose guard would be search-shaped and needs a positive control) and Y-4 (9303bb5 called its own guarantee unproven; re-adding the removed check fails typecheck with TS2304, so it is enforced mechanically — the commit message understated it); and 9303bb5..8519e64 — the roster, the audit gate, the quarantine sweep and the lockfile trigger, a range made almost entirely of the fixes that closed the previous one — findings Z-1 (the audit report's own classifier drops every rename state, so a proof moved AND edited lands in no column; measured at R100 and R090, and the identical field-alignment bug was fixed in the OTHER instrument inside this same range), Z-2 (the pre-commit gate takes recordsAudit from the index and the RANGE from the working tree, so an unstaged watermark edit passes the gate with nothing staged at all; measured live, EXIT=0), Z-3 (the lockfile guard's header still states the trigger this range removed, as a compound claim whose surviving clause vouches for the dead one — item 7's shape, created one commit after its detection signal was written into CLAUDE.md) and Z-4 (the roster makes a deleted case silent rather than false, and nothing anywhere pins how many cases a proof must run). All four since closed — Z-3 in 7347699, Z-1 in 93cd471 by moving the parser into gitScope.mjs, Z-2 in 98c31b1 with its own hole closed in 7fbc7e4, Z-4 in 418bcea; and 8519e64..418bcea — the four Z closures plus the Windows EPERM that took Guards down at 93cd471, the first defect in this stretch found by CI rather than by an audit — findings AA-1 (the instrument column filters ADDED FILES, so the three instruments this range added inside existing modules appeared in no column at all; the fourth axis of one classifier after pattern (W-1), root (X-1) and state (Z-1), each fix correct and each leaving the next standing), AA-2 (passRoster's contract paragraph still says there is no second thing to keep in step, thirty lines above the section that added one — item 7's fourth occurrence, fail-closed) and AA-3 (the spawn guard's Set is keyed on a case-sensitive resolved path on a case-insensitive filesystem; no live defect because both sides derive from one computation, which is a property of the callers rather than of the guard — the X-2 shape); AA-2 since closed in 6827c1d, AA-1's granularity half and AA-3 still open; and 418bcea..6827c1d — Electron landing, the Guards red it caused, the npm resolution defect underneath it, the doctrine pair and the honest-output split — findings BB-1 (advisoryRegister.proof.mjs hand-writes the roster passRoster.mjs owns, including a second copy of the skipped channel added in this very range, and declares no case count; its total is invariant at 29 across both worlds, so the fix is one line and was left undone one commit after two siblings got it), BB-2 (engineAdvisories.mjs's 'what this still does not do' documents the limit this range removed — item 7's sixth occurrence and the sharpest, because the surviving clause is SCOPE-true of the function while the two beside it are false, and the wrong belief it produces is 'build a second mechanism'), BB-3 (CLAUDE.md calls the instrument column 'new scripts', stale after both X-1's root widening and AA-1's added-files-only disclosure — the digest that tells an auditor where to look has tracked none of that column's four fixes), BB-4 (the npm_execpath harness fix has no control: measured, 22/22 still green with it reverted, because the defect was what the test INHERITED and assertions look at outputs) and BB-5 (T-1's no-derivation branch asserts two independent presences rather than that the misspelt symbol IS the unverifiable one, and UNVERIFIABLE appears in that world regardless of the mutation). All five since closed — BB-4 in e4e2e3c, BB-2 in 6d87877, BB-1 in 2a34b26, BB-3 and BB-5 in 708c414. Also recorded: the board waiter failed twice in one day and both times printed the reassuring answer, was rebuilt with an in-loop positive control, and was TRACKED NOWHERE — given a repo slot with a network-free proof in 79b368f; and 6827c1d..598b50e — the five BB closures, the board instrument, item 4b's sixth axis, and the FIRST RENDERER-SIDE CODE in this repository — findings CC-1 (the board instrument decided greenness by substring-matching its own rendered summary, so a CANCELLED run beside a successful one read GREEN: measured on the real strings, and 9292d1f — the cancelled run this module was written after — is exactly that shape; two defects in one, since the judgement also lived in the fetch shell that has no proof, and a split leaving logic on the untested side has not been made; closed in 598b50e by deriving greenness from the data in the proven module), CC-2 (channels.ts says a channel is added only when a real handler exists and calls a declared channel with nothing behind it a call that hangs; that has been false for app.info since it was declared — its only implementations are test fixtures and contract.proof.mjs, and no ContractHandlers map is registered with ipcMain at all) and CC-3 (CLAUDE.md's item-1 example describes MONSTERA_GITLEAKS in the past tense as an override that was removed; it is live at gitleaks.mjs:464 — but the mechanism is sound, both callers run the canary immediately, verified rather than taken from the comment, so this is a documentation mismatch in the checklist an auditor reads, not an open door). CC-1 closed, CC-2 and CC-3 open. Y-1 verified fully closed: all 32 total-printing proofs derive their number. THE RANGE THAT CHANGED WHAT THE RANGES ARE FOR — the audit's input had become its own output, seven Stage 0 rows are renderer-side and all seven read '—', and the CSP invariant is deferred because no renderer exists to read it from; next is the skeleton, not the queue; and 598b50e..90d9b6e — ALL FOUR CONTRACT SURFACES, packages/ui stopping being `export {}`, and Electron provisioned against a recorded pin — findings DD-1 (the pin is a RECORD, not an ENFORCEMENT: node_modules/electron/index.js ends with module.exports = getElectronPath(), which calls downloadElectron() when the binary is absent, so --ignore-scripts DEFERS the electron_use_remote_checksums hatch to first require() rather than closing it; nothing consumes the pinned binary — git grep finds no caller outside its own proof — and require('electron') resolves to node_modules/electron/dist rather than .tools/. The naive fix is a TRAP: an .npmrc carrying ignore-scripts=true also disables this repository's own prepare script, which installs the git hooks, silently disarming the secret scan and the escape-guard. The remedy is ELECTRON_OVERRIDE_DIST_PATH, read before both downloadElectron call sites, which makes the download path UNREACHABLE rather than discouraged — plus a guard that fails when node_modules/electron/dist exists; with one measured wrinkle, that index.js:31 joins the override with executablePath || 'electron' read from an absent path.txt, so it names a file with no .exe on Windows) and DD-2 (a process finding: a design question already settled in docs/JOURNAL.md at 132c2e7 was brought as an open one, and a correction was filed stating no Electron pin existed — which would have put TWO ENTRIES DISAGREEING ABOUT ONE AUTHORITY into the record, B3a inside the journal eleven commits after B3a became law. Two separable failures: the digest search was scoped Grep(path:'scripts') and never looked at docs/, which is X-1's root axis again; and `git grep -il` then RETURNED the right file and its filename list was read as the answer — not 4b's shape, because the search worked and no positive control catches a reader stopping early). Both open. Also corrected inside the range: 'two independent sources agreeing byte for byte' overstated the checksum cross-check — the release SHASUMS256.txt and the package's checksums.json are TWO CHANNELS FROM ONE PUBLISHER, which defeats a compromise of one distribution path and is not two attestations. The watermark never equals HEAD at recording time — see scripts/lib/auditWatermark.mjs; and 90d9b6e..d4c01c1 — a range made almost entirely of instruments, audited BEFORE the window unit so that instruments and the first feature code would not share one range — findings FF-1 (a shared list with two consumers had a proof on one of them: PLAIN_NODE_EXTENSIONS widens the lint glob and the runtime scan together, only the scan had a case, and the fact that hid it — no .js or .cjs exists under scripts/ today — is the fact that will change) and FF-2 (ESLint does not expand a SINGLE-element brace, so `scripts/**` + `/*.{mjs}` matched nothing while `/*.mjs` matched everything; invisible with three extensions, and it fails as 103 `not found by the project service` parsing errors that name tsconfig rather than the glob — a failure impossible to miss and impossible to attribute). Both closed in d4c01c1, FF-2 by making the length-one case unrepresentable as an array rather than checking for it, and by putting the attribution into the new case's own diagnostic. Nine reviewer findings EE-1..EE-10 all raised and closed inside the range, including EE-7, a process finding in which two documents' over-generalised 'ESLint does not reach scripts/' — false; it globs every .mjs with one rule enabled — produced a wrong B4-sized ruling for work that was three lines. ITEM 2a PAID IN ITS CORRECTED FORM: the electron-import coverage did not move from two workflows to one (it was in guards.yml only, then ci.yml only, both two-platform); it moved from a job that could ALWAYS look to one that can only look after `npm ci` succeeds, and both halves — ESLint's static and the scan's runtime — now share that single dependency, with the 138-commit precedent as the reason that is not theoretical. AA-1's granularity half open for a THIRD consecutive range and again the axis that hid something: three instruments arrived as functions inside an existing module and appeared in no column, and both findings came from reading modified-proof diffs. The net-diff caveat earned its keep — electron.proof.mjs reads +8 -0 while its roster went 8 -> 11 -> 8 inside the range, so a rule check was added and removed and the net shows neither; and d4c01c1..a66e1e6 — Electron provisioned in CI and THE SHELL STARTS: a hardened BrowserWindow, both permission handlers, the sender check, a launcher naming the pinned binary through electronBinaryPath(), and on windows-latest the FIRST security property this repository has verified against a running Chromium rather than against the source that configures it (the CSP read from the response as received, and the renderer observed refusing a connect-src fetch and an eval). RECORDED WHILE MAIN IS RED, deliberately, because the gate counts commits+1 and the pending Linux fix waits behind this entry — findings GG-1 (a commit added a file under scripts/ and did not run the proof that scans scripts/, G-1's shape; rule: when a commit adds a file to a directory some proof scans, that proof is in the pre-commit set whatever the change is about), GG-2 ('only the read-back step fails' was ASSERTED and was the premise under four hypotheses about a step that had not run — step CONCLUSIONS and DURATIONS are on the public API, only raw logs are behind the 403, and the one-second duration eliminated every hang hypothesis at once), GG-3 (the harness could only fail by hanging, FF-2's shape — and the commit that fixed it is itself a finding: correct fixes written against an unconfirmed symptom, the banned reflex with good engineering attached), GG-4 (THREE second opinions in one module, all B3a, all found by running it: the direct-invocation guard that exited 0 provisioning nothing on Windows, tmpdir staging that rename could not leave across volumes, and an extractor comment true on macOS and false on Linux while the list was used on both), GG-5 (the CSP enforcement probe was a fixture the defect handles correctly — a fetch to a bogus host fails on DNS whatever the policy says, so it reported 'blocked' for a request CSP had just been loosened to permit, and survived the exact mutation it existed to catch; now read from securitypolicyviolation, which only CSP raises) and GG-6 (nothing established the published binary could be EXECUTED — the available:true shape, raised by the reviewing seat, fixed in the commit this entry unblocks). Item 2a: the read-back is a strengthening where provisioned and a new 'could not look' elsewhere, printing UNVERIFIABLE rather than 'nothing to check'. Open: AA-1, AA-3, CC-3, DD-2, BB-6, Y-3, and the MuPDF cache's restore-without-reverify, which is not the same shape because it caches a built tree with no pin to check against; and a66e1e6..03fcf9b — the CSP becoming law as invariant 27 with this document as its writer of record, and the §2 read-back widening from one item to five, owed on the FILE threshold at 24 of 24 rather than on commits — findings HH-1 (the preload had NEVER EXECUTED: tsc emits ESM into a \"type\": \"module\" package and a sandboxed preload is loaded as CommonJS, announced through Electron's preload-error event and through nothing else — no stderr, no exception, a window that came up looking correct; proof:preload and proof:contract both passed CORRECTLY about a file nothing had run, because the defect is not in the file. Closed in-range by bundling to CJS through a new @monstera/contract/bridge leaf: 137,809 bytes through the package root, 233 through the leaf, since channelIds = Object.keys(channels) is a top-level evaluation nothing prunes around), HH-2 (the range's own new navigation probe was VACUOUS and only the mutation step found it — https://example.org/ produces zero loads when the guard refuses it and zero when the machine has no network, so it could not tell a working guard from an offline runner; item 4's fixture rule in the same file that already records being bitten by it on example.invalid four commits earlier, in a comment that was on screen while the probe was written beneath it. Now the loaded document plus a query string: on disk so it loads with no network, different href so the guard refuses it), HH-3 (ADR-0020 offered two independently-sufficient reasons and had executed one; the resolution claim was documentation written as evidence, measured afterwards as `Error: module not found: @monstera/contract/bridge` from a CJS preload — the claim is true, its status was not, and the ADR carries a dated correction rather than a silent upgrade), HH-4 (the UNVERIFIABLE block hand-writes `9 case(s)` and nine lines beside a roster declared 13/4, so the mechanism whose whole job is to not overstate carries a literal that goes stale the moment a runtime case is added — B3a inside the honesty mechanism), HH-5 (window.ts's HERE comment still says the sibling `preload.js`, eight lines above the comment written IN THE SAME COMMIT explaining why it is preload.cjs — item 7's compound claim, ninth occurrence and the second created BY the fix, with the surviving clause vouching for the dead one) and HH-6 (proof:rendererpolicy resolves the harness, the declared policy and the preload from dist/ and nothing detects a STALE dist; proof:testresolution exists for exactly this class on the test side, and staleness is the one failure CLAUDE.md names as unreachable by a positive control). HH-1, HH-2 and HH-3 closed; HH-4, HH-5 and HH-6 open. Item 2a: the pin agreement is a strengthening with NO provisioning condition — two strings, every machine — while the UNVERIFIABLE block grew 3 lines to 9, which reads as a blind spot tripling and is not one: the intent half runs everywhere and gained an assertion, and what is newly unverifiable is enforcement that was previously verified nowhere. Also recorded: style-src 'unsafe-inline' was DROPPED rather than pinned, outside the ruling that ordered the amendment and flagged as such; and GG-8's convert-on-touch trigger has NOT fired — two workflow steps were edited and neither touches the Electron tree; and 03fcf9b..7223851 — the three HH closures, the owner's rulings recorded, DocumentService gaining the canonical image, and the shipped app learning to hear the failures Electron announces — findings KK-1 (A UNIT TEST WAS DOWNLOADING ELECTRON: `import { type App } from 'electron'` emits `import {} from 'electron'`, the braces keep the specifier and the side-effect import SURVIVES, so vitest running that module in plain Node fetched the unpinned runtime — measured, node_modules/electron/dist appeared at the minute the new test first ran. Neither of invariant 26's enforcers reaches the route, and the invariant's own rationale — apps/desktop/src runs inside the Electron runtime — is what made the import look safe; true of the app, false of its tests, corrected in the same commit), KK-2 (AND CI WAS STRUCTURALLY BLIND: ci.yml asserted the directory absent 90 lines before npm test, so the same download would have happened inside the run that went green. An assertion about what a job has NOT YET DONE is only true at the point it runs, and its position in the file is part of the claim. Fixed by running the proof again after the suite, and the class was swept rather than ruled: one instance, already found; guard:tree, scan:secrets and proof:lintignores read inputs that do not change mid-job; proof:testresolution was already position-aware by explicit comment; the two checks that read dist/ are protected by HH-6's staleness comparison and by running immediately after the build), KK-3 (II-1's listener-count probe PASSED with the subscription deleted, because Electron attaches its own listener to each of those events — HH-2's class an hour after HH-2's rule was written down, by its author; and the differential fix was STILL half vacuous because the harness kept its own preload-error listener, so a probe about the shell's subscription was satisfied by the test's own), KK-4 (the window's backgroundColor was '#00000000' and read back as #000000 — Electron honours an alpha channel only for a transparent window, so it painted opaque black for its whole life; worse than the raw hex it was raised as, because a reader checking it got an answer that had never been true) and KK-5 (a number's cause inferred from a FEATURES row rather than from the instrument: perf:gate's 1.00x was read as 'main holds nothing' when roleMain.mjs:35 already does readFileSync, so 1.00x has always meant one resident image. A document explains a number; only the instrument produces it). KK-1, KK-2, KK-3 and KK-4 closed in-range; KK-5 corrected before it settled anything. Reviewer findings II-1 closed, II-3 closed, II-2 OPEN with a hard trigger of BEFORE STAGE 0 EXIT — five of §2's seven items verified reads as complete to a skimmer and the two that are not are the two nobody will re-check — and JJ-1/JJ-2/JJ-3 raised at the range's end and fixed in the next commit. Item 2a, three movements: the advisory register lost a verdict and two symbols (5/19 to 4/17) when kernel-holds-canonical-bytes was retired, deliberately; proof:rendererpolicy grew 13 cases to 16; and proof:electronimports now runs TWICE in CI, which is not duplication — the early run is fast feedback on the static shapes and the late run is the only one whose absence assertion means anything; and 7223851..9a951d6 — the accounting findings closed and THE COMPOSITION ROOT, the first commit in which this application runs — findings LL-1 (importing DocumentService LOADED THE NATIVE MUPDF SHIM: commandLog.ts wrote `import { type PriorPageRotation } from './rotatePages.js'`, the specifier survives as `import {}` and RUNS, and rotatePages.js imports withDocument from mupdfWriter.js as a value. Measured at 54.5 MB to 92.6 MB — 38.1 MB of parser pulled into the module whose whole argument is that it holds bytes and never parses them. `import type` erases it: 38.1 MB became 9.0 MB. SECOND INSTANCE IN TWO DAYS of one mechanism with a different bill, the first having made a unit test download Electron, so the fix ships with proof:kernelload reading the EMITTED JavaScript — the source cannot distinguish the two forms and only the emit can. CI could not have caught it: nothing measured what importing the kernel costs), LL-2 and LL-3 (the ceiling counted images and not checkpoints, so the first checkpoint written puts main over budget while open still reports capacity; and byteLength is the VIEW's length, so a reader returning a subarray retains the whole backing allocation while reporting the slice — under-reporting in the unsafe direction. The log now reports what it PHYSICALLY retains, redo tail included, because entries is the applied view and undo moves a cursor rather than popping), LL-4 ('sized by measurement' was arithmetic about bytes and not evidence about the implementation: roleMain.mjs is a MODEL of main and never constructs a DocumentService. BB-4's harness axis — the harness does the retaining itself, so the real caller's version is exercised by nothing. Corrected in the ADR and the row; the perf:gate role that closes it was BLOCKED BY LL-1 until this range), LL-5 (kernelLoad.proof.mjs's own known-present control fired on its FIRST run: the walk matched `import ... from` and index.js RE-EXPORTS the adapter, so it reported unreachable from the one module that certainly reaches it — a wrong pattern producing that proof's passing answer, caught by the control rather than by luck) and LL-6 (composition.ts's first draft claimed document.execute 'fails with internal' — the CC-2 shape, argued as a cost to accept. Run through the real bridge it returns the DECLARED document-not-open, because DocumentNotOpenError fires before the session is looked up and the missing-session path needs an open document, which is not a channel. Item 5, on my own comment, in the file that argues for measuring). All six closed in-range except LL-4's owed role. Item 2a: three proofs added, none removed, and one amnesty list grew — ACCOUNTED_COMPUTED went from three entries to four for composition.proof.mjs's file:// import of the built budget, carrying a declared site count in both directions as EE-10 requires; and 9a951d6..fac1e4a — II-2's attribution, the annotation wrapper, LL-4's owed perf:gate role, and then THREE COMMITS SPENT ON ONE RED MAIN, which is the part worth reading — findings NN-2 (THE FIRST DEFECT IN THIS PROJECT THAT CI CAUGHT AND THE LOCAL MACHINE DID NOT, and not by luck: the proof's neutralising entries were deduplicated on the FORMATTED STRING, which embeds Math.ceil(ratio), so two roles answering to one budget produced two distinct strings both declaring main and the parser refused the line — correctly, the defect being upstream of the check that caught it. Which integer bucket two measured ratios land in is a property of the machine, so no line of source separates the passing world from the failing one; measured after the fix at main 1.00x and main-service 1.01x on the dense shape, ceilings of 1 and 2, while the gate's own display rounds to two decimals and therefore cannot show which side of the boundary a ratio sits on. Item 2 where the hard shape is not a fixture but a bucket boundary the machine chooses, so the control had to construct it, and item 4's fixture rule decided the numbers — ratios that ceil alike are exactly what the defect handles correctly, and the fixture now carries a case asserting its own ratios diverge), NN-3 (raised by the reviewing seat: Math.max(0, peak - baseline) is verbatim the fourth entry in CLAUDE.md's list of blind instruments, live in the instrument that decides the Stage 0 memory gate — and the comment above it justified the clamp on the grounds that a negative ratio would pass every multiplier silently, which is exactly what a clamped zero also does, while additionally looking like a perfect result rather than an absurd one. The comment named the hazard it was creating and drew the opposite conclusion. No proof here could have caught it: a clamp that fires produces a pass), OO-1 (NN-1 fixed one job; the class is a CI job that runs a proof depending on built output without a step that builds it, and nothing checks for it — instance closed, class open) OO-2 (perfBudget.proof.mjs collects failures and prints at the end, so a throw in the measured section discards findings already recorded; observed during the mutation run, two pure controls recorded and neither printed. Costs diagnosis, not correctness) and OO-3 (check:docs reads docs/JOURNAL.md through readStagedBlob — the INDEX — and then calls auditScope with no watermark, so auditScope falls back to readFileSync and takes the watermark from the WORKING TREE: Z-2's mechanism left standing in the sibling check, one caller converted and the other not, while auditWatermark.mjs's own header states the rule that a gate's inputs all come from the scope its decision is about. MEASURED: with this entry staged and the watermark advance left unstaged, check:docs exits 0 and prints that the watermark is recorded in the journal — so an audit-recording commit can land without advancing the watermark, and nothing catches it afterwards either, since on a clean checkout the OLD sha is what is read and the old sha appears in its own older entry. Found while writing this entry, which is what a range-scoped audit is for. CORRECTED 2026-08-21, the same day and after building the fix: the measurement is right and the ATTRIBUTION IS WRONG. With the split closed the same shape still exits 0, because the check's only property is that the watermark's sha appears somewhere in the journal and the index carries the OLD sha, which appears in its own older entry. Two findings written as one — OO-3a, the scope split, fixed by taking both documents from the index via stagedWatermark, whose own symptom was a false POSITIVE on a pair no commit would contain; and OO-3b, OPEN, that the gate is one-directional and cannot catch a record written without an advance, since every sha the journal has ever named stays in it forever. Item 5, in the sentence I was most confident about: the exit 0 was executed and the chain from it to the split was reasoned, and the reasoned half was written in the executed half's voice); and fac1e4a..f7c74ff — the audit record stops being a search, and TWO CONCLUSIONS DRAWN WIDER THAN THEIR MEASUREMENTS, which is the headline and it is about me. OO-3 attributed a measured exit 0 to a reasoned cause; the withdrawal of Node's permission model was measured on ONE ROUTE being inert and written as the model being unavailable. Neither was a guess, every clause of evidence under both was true, and in both the tell was the same sentence: I had not run the thing I was concluding about against the case my conclusion covered. That is item 5 with a sharper edge than executed-versus-asserted — the dangerous entry is not the asserted column but a claim standing on something executed, one inference further out than the execution reaches. Findings: OO-3b closed (the record requirement was a SEARCH whose reassuring answer any previously recorded sha produces, since the journal names every sha it has ever named forever, so the property was satisfied by history rather than by evidence and could never catch an entry written without an advance; replaced by an exact comparison of the newest heading's range end against the watermark, both read from the index, with the load-bearing case setting the watermark to a sha that IS in the journal and a control asserting that of the fixture — and one of my own cases was VACUOUS, asserting !== null for a branch that decides the message rather than the verdict, found by the mutation that deletes the branch), OO-2 closed (a throw in the measured section ended the process before failures printed, measured live at two recorded controls reaching no output; the section now runs through a wrapper that RETURNS the failure text, which is what makes it testable in both directions, and says the remaining cases are UNRUN so a shortened list cannot read as complete), PP-1 (row 283 asserted THREE of the four properties invariant 25 names, so it could have gone green while its own title was true of three quarters of it — the display-only shape at checklist level; fixed in the commit after the audit, the Y-2 gate having refused to let it share one), PP-2 closed (hostContainment.mjs ran step 4 on step 3's unanswered question: ACCESS_DENIED was recorded correctly as a could-not-look and then conclusions about a Low host were drawn from a process whose Low state was never established; closed by reading the child's token FROM MAIN twice, 0x2000 before and 0x1000 after, since two readings differing across the one action are the only shape separating a working reader from one that always says Low), PP-3 closed and it produced the retraction (the anchor crossed runtimes, so ELECTRON_RUN_AS_NODE=1 separated the two causes: Electron's own Node 24.18.1 HAS the permission model, so what failed was the route — execArgv is applied after Node's bootstrap and NODE_OPTIONS is read during it — and in a utility process so configured, reading a file the host was never handed is refused ERR_ACCESS_DENIED while reading one it was handed returns 3,692 bytes, against three variants where both succeed. THE READ-BACK MISLEADS IN BOTH DIRECTIONS: via execArgv the flag is set, readable back as set, and inert; via NODE_OPTIONS it works and leaves execArgv empty. Invariant 25's property (d) is therefore obtainable for a utilityProcess and (c) no network is now the ONLY property without a mechanism), PP-4 open (ProcessMemoryLimit as a literal would be a second opinion about §9.17's mupdf-host budget, B3a, hiding inside a Windows struct rather than a config object), PP-5 open (a job memory limit and ADR-0007's kill-and-restart are different failure modes and the ADR must say which is primary) and PP-6 open (the window between fork and AssignProcessToJobObject is time under no limits, to be made unrepresentable by a handshake rather than documented). Item 4a: five new instruments, TWO of which shipped without adequate controls and both caught in-range — hostSurface.mjs's permission probe had no positive control while its false was load-bearing, found by applying item 4b to my own instruments because the audit-scope column lists them; and hostContainment.mjs is PP-2. Item 3: CI runs none of the research instruments, correct for scripts that assert nothing and a gap the moment row 283's assertions are built, since those need Electron and Windows. Item 2a: auditScope.proof.mjs 40 cases to 48, perfBudget.proof.mjs 23 to 31 with its deletions being the measured section re-indented into the wrapper, nothing removed and nothing loosened. Also recorded: the unauthenticated board budget cannot survive nine pushes in two and a half hours at forty polls each, so the cadence changes — one unit per commit stays, the board is checked once per group, and a commit CI cannot fail on is not watched. AA-1 is a stated limitation with a trigger rather than an open finding. Item 7 paid again and in the sharpest available form: the comment above the defective block read BUDGET NAMES, not role labels, AND DEDUPLICATED — first clause true and load-bearing, second false, the live clause vouching for the dead one, and it reached review twice in that state. AA-1 fired concretely for the fourth consecutive range and for the first time hid something real: documentCostBytes is a new instrument that arrived as a FUNCTION INSIDE AN EXISTING MODULE and appears in no column of the range's own audit output. MM-1 stays open and correctly — the bound was not raised, since the probe loads in 205 ms against a 15,000 ms bound and a runner that hit 15 s was structurally different, so the load duration is now reported on the success path, which is an instrument added rather than a cause found. Item 2a: perfBudget.proof.mjs went 23 cases to 29 with none removed or loosened, and rendererPolicy.proof.mjs's single deletion is a diagnostic line replaced by four (that clause ends unfinished, and it is left exactly as written, because this field is a RECORD and a record is appended to rather than edited); and f7c74ff..0548ad6 — the containment claim loses a property, and a control that could not fail becomes one that can. ITEM 2a FIRED TWICE IN ONE DOCUMENT AND IN OPPOSITE DIRECTIONS: invariant 25's property (d) went from obtainable through NODE_OPTIONS with enforcement measured to NO MECHANISM, which is a claim being WITHDRAWN, while row 283 went from three assertions to four against a law that has named four since it was written, which is coverage ARRIVING. Neither reads as a change in a diff, because a gap line and a retracted claim render identically, and every gap line reads as rigour including the ones that used to be claims — so the row now says which of the four have a mechanism and which have none, in its own body. The principle that orders all four is that only kernel-enforced mechanisms contain native code, which is why QQ-1 removes (d)'s only candidate while leaving (a) and (b) untouched, and settles (c) with no further measurement. THE MODIFIED PROOF CHANGED MEANING AND IS A STRENGTHENING, stated because a loosened check and a corrected one are indistinguishable outside the diff: rendererPolicy.proof.mjs's crash control gained a conjunct, so it can only fail more often, and the conjunct is not decorative — reverting the harness to a fixed 400 ms, long enough on this machine that the old boolean passed, now fails with resolved by already. THREE FINDINGS RAISED AGAINST MY OWN INSTRUMENTS, ALL CLASSIFIED ROOT-CAUSE by item 1: the outcome classifier moved the decision into the try/catch that already knew the answer rather than guarding the symptom, and its first attempt — classifying the probe's own prose — was a second opinion under B3a that moved the defect one layer instead of closing it; the memory probe's interference was removed rather than subtracted; and the missing-reading case got a fourth terminal state, unreadable, so could-not-look and looked-and-found-containment stop sharing an output. TT-1 IS OPEN: RR-1 added provision:electron to the shim job and nothing in that job consumes it, so the step is a prerequisite for something ADR-0022 has not yet decided, held by nothing — RR-3 either names the consumer or the step is removed in the commit that says the fixture stays research-only. AA-1 fired for the sixth consecutive range — sinkReceives is an instrument that arrived as a FUNCTION INSIDE AN EXISTING MODULE and appears in no column — and the disclosure the report prints at the point of use sent the read to the modified-proofs diff, which surfaced it, so the ruling stands on its own terms. THE LIVE SPECIFICATION AND THE RECORD DISAGREED FOR A WHOLE RANGE: at f7c74ff the journal said (d) was obtained and row 283 said the permission model was withdrawn, written by the same seat a day apart with neither aware of the other; the divergence happened to be in the safe direction and the record happened to move back, which is luck and not a mechanism, and prose agreement is not mechanically checkable — recorded as the reason item 7's document-class rule is written as strongly as it is rather than as a finding owed a guard. CI CAUGHT WHAT NO LOCAL RUN COULD, again: the crash race was green here and on ubuntu and red on windows-latest only. Asserted and named as such: that CRASH_BOUND_MS of 15,000 ms cannot push the proof past its own 120,000 ms timeout — the arithmetic is not close, and no run has exercised the bound because the mechanism has never failed to fire. MM-1 stays open on its original terms; one plausible mechanism is not evidence for another failure. And a correction to my own reading of QQ-5: I wrote that an unwatched commit carries no verdict, which conflates two facts — a run nobody looked at still completes and still records a conclusion, and what removes a verdict is CANCELLATION, which happens only when a later push lands while the run is still going. 77ee5a3 is green; d727843 has no verdict and never will; 0548ad6 was unverified at the time of writing; and 0548ad6..4d04942 — the decision range, audited at its own boundary before feature code joins it. THE LOAD-BEARING COLUMN IS EMPTY AND THAT IS INFORMATION: no proof was modified, so this range added coverage and changed none, which is worth stating because an empty column and an unread one produce the same silence. Item 2a moved four times and TWO OF THEM REMOVED A MECHANISM — PP-6's fork-to-assign handshake, withdrawn for CREATE_SUSPENDED plus assign plus ResumeThread, and the host lowering its own integrity, which does not exist because the LowBox token is Low at creation. A withdrawn mechanism reads in a diff exactly like one that was never there, and both were replacements by construction, which is what B5 asks for and also what leaves the least trace. Neither was removed on an inference: previousSuspendCount 1 beside a running thread reporting 0, and integrityBeforeResume 0x1000 contained against 0x2000 uncontained. The five ACL grants were reclassified from the mechanism to a development accommodation, which is a claim withdrawn about what ships. VV-1 IS THE RANGE'S BEST FINDING AND WAS FOUND BY AUDITING THE PREVIOUS COMMIT: the backtick guard reported 11 templates carry no backtick and meant 11 of the 15 it could see, because four emitted bodies in scripts/research are plain template literals — the more dangerous of the two, since a plain template interpolates as well as terminating. That is the PATTERN axis of a classifier (W-1) reappearing inside a check written to close a different class, one commit after it went green in CI, and fixing it needed the ROOT axis too because the file-level filter held the same opinion and skipped those files before the widened matcher saw them. Widening to every template was tried and REJECTED ON A MEASUREMENT — 36 reports, nearly all openers whose terminator is not a bare backtick-semicolon, because where a template ends cannot be determined textually, which is the same wall the parser hits. VV-2 IS OPEN AND IS NOT AA-1: audit:scope distinguishes proofs added from proofs modified but reports instruments ADDED ONLY, so an instrument file that already existed and whose behaviour changed appears in no column — this range converted four of them to String.raw, altering escape handling in the program each writes to disk, and what caught them was running all four rather than the report. AA-1's compensation cannot reach it, since these are not proofs and appear in no diff the disclosure sends you to; it is a fourth axis beside pattern, root and state, namely added-versus-modified for instruments. Both new guards were blind in the direction they were pointed and CI could not have said so — a search's silence is its most convincing failure — and the backtick scan additionally shipped with the class it guards in its own entry point, printing nothing and exiting 0 because the module-is-main test built a file URL by string concatenation, which is wrong on Windows. Item 7 at cross-reference scale is UU-1, the case check:docs structurally cannot see because a renumber leaves BOTH TARGETS EXISTING; and the compound-claim check found two more in this range's own files, hostFixture's nothing-yet-delivers-them and hostNativeRead's goes-back-to-having-no-mechanism, each true when written and false eight commits later, each with the live half vouching for the dead half beside it; and 1d5e6d6..d3ea661 — WW-1's consolidation, BBB-1 and CCC-1 — findings DDD-1 (the audit report's SOURCE columns carry two states, added and changed, where its PROOFS columns carry three: a 636-line instrument was DELETED in this range and appeared in no column at all, the fifth axis of this classifier to fail after pattern, root, state and added-vs-changed, and the general form is that an asymmetry between two halves of one classifier is itself the finding because nobody audits for a missing column) and the unattributed red at a0d2ec0 (Guards failed on windows-latest with ubuntu green, at a step whose register, checker and proof are BYTE-IDENTICAL to the green d3ea661 on the same platform, so the failure is not in the code under test; the step's only non-deterministic input is 60 live api.osv.dev requests per run per runner and the checker throws on any non-OK status by design, which remains a hypothesis because Actions serves logs only to authenticated callers — CCC-1 wraps all 24 Guards proof steps so the next occurrence prints its own words publicly). Also recorded: (b) memory left the coverage with RR-3 as its trigger, printed at the point of use rather than dropped; the spike's invalid-outcome classifier path has no mutation test; and backtick occurrence 5, the first stopped by the scan rather than by someone remembering; and d3ea661..d55b893 — EEE-1's re-verification, the OSV fix, EEE-2's requirement, EEE-3, GG-1's note and FFF-1 — findings GGG-1 (check:stackowner and proof:stackowner were registered on the Guards job, which runs no npm ci, so both would have failed on every run from the commit that added them; invisible on this machine because node_modules always exists here, found by item 3's second question and fixed in d55b893 before either was pushed, and its registration case had the same defect one level up, asserting the step name appeared in guards.yml by name rather than that a workflow able to run it does), GGG-2 (--recorded-advisories has no case proving it takes effect: a broken parse would make all 31 register cases fetch live and still pass, so the property the whole OSV fix rests on is asserted by a comment and a call-site split rather than checked; open, remedy priced as printing the advisory count and source and asserting a recorded run reports the fixture's own length) and GGG-3 (the checker's Usage block still names two invocations after the range added two flags, item 7 in the shape where the stale half holds the contract position). Also recorded: the OSV change moved 31 live invocations to one, which is a real reduction in what a change to OSV's response shape would redden, and it is NOT evidence about the undiagnosed red at a0d2ec0 even though it was made in its neighbourhood; and FFF-1a, three opinions about the entry-point test under scripts/, one of them written the previous day by the same author; and d55b893..5f07e80 — FFF-2/3/4, GGG-2/3, HHH-1/2/3, III-1 and GG-1's first closed member — findings JJJ-1 (a branch in a new instrument that NOTHING executed: disabling the .catch()-chain handling in nodeModulesPlacement.mjs left all nineteen cases green and the real scan unmoved, because no code here writes that shape, so it was dead from birth rather than merely uncovered; the transferable form is that a new instrument's mutations naturally aim at the branches its fixtures already exercise, and the ones worth checking are the ones no fixture reached), JJJ-2 (a proof CASE was deleted and no column reports that — annotateCoverage.proof.mjs lost the no-paths-throw case because the guard became unreachable under B5, which is correct; it surfaced only through the report's new 'N deletion(s) DO NOT APPEAR in the range diff' line, so AA-1's granularity limitation held and its mandated compensation did real work for the first time) and JJJ-3 (the pre-push hook's BLOCKING path is proven by nothing: fifteen cases cover the decision and none covers what happens when the register fails, because CHECKER is an absolute path into this repository and a fixture cannot supply a failing register without reaching the real one — a stated gap with the fix priced as making that path injectable). Also recorded: the util.inspect swap in rendererHarnessMain.ts stepped around an expiry that fired correctly rather than re-triaging it, recorded as a decision with the refused alternative and the most overturnable call in the range; the placement instrument REFUSED on its first run because its control was unfindable, which is item 4a's ordering actually obtained; three registration cases in three proofs went red at once when FFF-2 landed, reporting a de-registration that had not happened, because each matched an npm script NAME rather than the path that executes; and the escape-resolving-write hook denied three times, every one a reflex composition of mine; and 5f07e80..5889f8a — the host runtime loop, the engine host factory and three documents naming the wrong writer of record — findings NNN-1 (every case in the factory's file held assignToJob at TRUE, so a factory that trusts a NO and never reads membership passed all twenty-one; the claim in the code is that the READ decides, and the fixture set only ever tested half of it — closed with three cases holding assign at false and varying the read), NNN-2 (the runtime loop's terminate-once guard reddened no mutation, and it is reachable: every other caller of stop sits behind an isStopped check and the handler-rejection path does not, so a late rejection would fire terminate twice AND overwrite the reason, reporting a peer's malformed frame as our own unsendable-response — closed, with a control that the rejection alone does terminate), NNN-3 (the isStopped guard at the top of receive also reddens nothing, and that one is NOT a missing case: its only effect is that the frame decoder stops accumulating a refused peer's bytes and nothing on the module's surface can see the decoder — stated in the code with the trigger, which is a real transport, and NOT fixed by widening the surface for a test), and NNN-4 (item 7 is range-scoped like everything else, and a document can be falsified by a commit that does not touch it — MMM-1's three false statements were found by the reviewing seat, not by any audit, because no range ever changed both the claim and the code that refutes it; the compensation named is that a range which STATES a cross-document relationship must sweep every other statement of that relationship, which would have caught it here since budget.ts asserted the correct version inside this very range). Also recorded: the KKK-1 reword is this range's one workaround and is now dated against the Win32 surface rather than left undated; the reviewer's rule that a change stopping a trigger is legitimate when it would have been right anyway and suspect when its only benefit is that the trigger stops, which RETROSPECTIVELY SETTLES the util.inspect swap recorded as overturnable in the previous range — Node owns how a thrown value renders, so calling it is B3a and was right with no register in existence; the mutation harness written for this audit reported '1 failing' for a mutation that reddened 2, because the first match for a failure count in vitest's output is the Test Files line rather than the Tests line — an instrument's own resolution problem caught inside the audit that item 4a exists to prevent, and its RED/GREEN verdict was sound because it came from the exit code; the same harness REFUSED an anchor that matched twice rather than reporting a result, which is 4b's positive control in a mutation tool; and I searched CI for 'proof:composition' by npm script NAME, got nothing, and nearly filed a finding — the workflow invokes it by PATH, which is exactly the recognition rule annotateCoverage.mjs was rewritten around one range earlier; and 5889f8a..167de69 — the Win32 surface, PPP-1, OOO-1, QQQ-1 to QQQ-3, RRR-1 and SSS-1/SSS-2 — findings TTT-1 (the escape guard's redirect-target exclusion covers '>=' and not a bare '>', so a comparison inside a quoted awk or sed program is still read as a redirect and denied; measured, and the proof's own fixture for that case was written with '>=', the one operator the fix had already handled), TTT-2 (the advisory register's symbol-shape rule has a fourth reader inline in ocrDoors.proof.mjs, and the paragraph justifying that spelling was deleted from engineAdvisories.mjs with the code and never added to the file that now has it; neither helper is exported and importing that module runs its main, so the fix is a small shared reader), TTT-3 (five cases passed prose into ask()'s tool-name slot, non-vacuous only because every name that is not PowerShell falls to the shell rules — fixed in range with a throw) and TTT-4 (win32Handle.proof.mjs is the last roster adopter calling process.exit after a write, the shape that double-printed two other proofs; no duplication observed today, so a class fix rather than an observed defect). TTT-1, TTT-2 and TTT-4 are open. Also: the '--require-desktop-copy' hard-failure arm had never executed anywhere and was executed here by moving the built file aside; the range's headline is that both seats carried the escape guard's denial count as six when the transcript says 65, a number neither had ever read; and 167de69..bc8d94b — RR-3's control half, TTT-2 and TTT-4, UUU-1, TTT-1's class B fix and UUU-2 — findings VVV-1 (proof:stagedsyntax is registered in package.json and invoked by NO workflow, and no guard here can see that class: annotateCoverage checks invocations that exist and jobPlacement checks steps that exist, so absence is outside both; found only after a first search matched the npm NAME and reported sixteen false missing, the workflows invoking by PATH), VVV-2 (a filter spelt entry.status where the field is entry.state, so it was always true and staged deletions passed through — harmless only because a second mechanism, the index itself, was doing the work; TypeScript caught it and no case did, the fixture set containing no deletion at all), VVV-3 (two Writes emitted control characters into string literals, 0x01 and two 0x00 where spaces belonged, invisible in every editor — guardFiles.mjs would have blocked both, and the standing rule about escape-resolving tools names shells, not the tool it sends you to) and VVV-4 (two of parseSources.mjs's three parse goals, script and either, are reachable and exercised by no fixture). VVV-1 and VVV-4 are open. The range's pattern: a guard can be right about the wrong thing, and it reads exactly like being right — three instances, and only asking why an answer is right finds the two a mutation test cannot; and bc8d94b..e84538d — UUU-3, UUU-4, the VVV findings, WWW-1, RR-3's migration and its proof conversion — findings XXX-1 (two changes to shipped security code, the inherited stdin handle and --no-stdio-init, guarded by exactly one thing: the containment proof, on one job and one platform, with nothing local failing if either is reverted; recorded rather than papered over with a source-text scan, which would go green on a flag that is present and ignored), XXX-2 (the LowBox-alone process-creation row asserted `same` and now asserts `either`, a deliberate coverage reduction because neither direction is true everywhere — it still asserts the invariant half, that the uncontained cell can spawn, without which two dead cells would satisfy it) and XXX-3 (the tree witness has a caller that must NOT have it: documentScope.proof.mjs deliberately removes a tracked file, so a witness would report THE TREE MOVED on every correct run). XXX-1 is open. The range's headline is that three defects were found by running shipped containment code somewhere other than the developing machine — an undeclared generated-fixture dependency, a contained host that could not start because node opens the NUL device through the CRT and an AppContainer cannot, and the container's refusal of process creation being build-dependent — none of which any local care could have reached, and the first attempt at the second was wrong in a way that was equally consistent with the evidence; and e84538d..52edb0f — YYY-1 to YYY-3, ZZZ-1 and ZZZ-2, XXX-1's discharge onto a second Windows image, XXX-3 and the NUL repair text — findings AAAA-2 (npm run board's verdict was piped through tail, so a run that returned 2 for NO VERDICT reported exit 0; the rule against piping an exit code is already written down and did not reach the moment the command was composed, so the remedy is short output by default rather than a note), AAAA-3 (the unauthenticated API quota is 60 an hour and shared; walking 50 runs for a measurement starved the board reader, which is the caller that must never be starved, so one fetch wrapper with a reserve rather than two careful helpers), AAAA-4 (audit:scope reports a two-point comparison, so a file corrected mid-range shows net churn and the correction vanishes — measured on win32HostSurface.ts, whose 4 hidden deletions are exactly the ZZZ-1 correction of the YYY-2 comment; ruled a stated limitation with one line of output at the point of use) and AAAA-5 (electronBinaryCallers.mjs's first run exited 0 having scanned nothing, because its main guard compared import.meta.url against a hand-built file:// string that never matches on Windows — and NOTHING in the pipeline could have caught it: annotate.mjs re-emits only on failure, check:proofcoverage proves a proof is invoked rather than that a scan ran, and the proof calls report() directly so the CLI path CI uses is exercised by no case; found by the absence of output, which is luck). AAAA-1 was raised in-range and ruled: the either row's contained outcome has no recorder at all, so the one row that exists because the fact varies by Windows build reports that variation where no reader can look. The range's headline is that three of its defects were in the instruments written to close the previous defect and one was CREATED by the correction of the one before it — ZZZ-1 put a false sentence in the contract position inside the fix for YYY-2, which is item 7 arriving inside the fix for the finding item 7 had just been applied to. No check was loosened anywhere in the range; both modified proofs are strictly additive and the only deletion in either is a roster count rising; and 52edb0f..51d3da8 — AAAA-1 to AAAA-7 and the (b) memory probe — finding AAAA-8 (the AppContainer process-creation split was recorded as CLIENT vs SERVER on three points, two of which are GitHub-hosted CI images and the third this machine, so the single client point is also the single not-a-CI-image point and both explanations survive all three readings equally; it does not touch Decision 8, which rests on cannot-rely-on-the-container and which a confounded split supports just as well, but the PREDICTION was recorded as though a routine image bump would settle it. The reviewing ruling's stated basis was itself wrong and was checked rather than accepted: GitHub-hosted Windows runners are not Server SKUs only — windows-11-arm is Windows 11, a client SKU, and the same table no longer lists a 2019 image — so the discriminating test is reachable at a price, namely arm64, which swaps the image-provenance confounder for an architecture one and costs an arm64 MuPDF and an arm64 Electron. Expensive and impossible are different, and only one is safe to leave unfired). The range's headline is that FOUR of its defects were mine and none were in the code under test — a hand-built file:// main guard, a piped exit code, an API budget spent on measurement so the board reader had none left, and one pipe character in a table cell; the same premise as previous ranges at a different level, since three arrived in how the instruments are DRIVEN, which nothing was watching. AAAA-2 and AAAA-3 are recorded as MECHANISED AND NOT FIXED: both were produced by a rule that has to be recalled at the moment a command is composed, which is the root cause this repository has documented seven times and for which there is no repair — reporting them as fixed would report the count as stopping. The pipe character is recorded as not a fix at all: the finding is AAAA-7 one level up, that six local checks read the INDEX so running them before git add inspects the previous content and passes; and 51d3da8..6c2017c — AAAA-8's correction through the hook-probe reshape — reviewer findings AAAA-12 to AAAA-15 and my own AAAA-16 to AAAA-19. AAAA-12: the FEATURES row said the registering commit could not come from a session older than it, reasoning from the OUTCOME'S NAME rather than from the property the age gate is keyed on, which is whether an observation is self-certifying — a PostToolUse report certifies itself exactly as a PreToolUse denial does, so the row was false in the direction that produces a false green, and the vocabulary now names the property (fired, silent, unobserved) instead of the tool call's fate. AAAA-13: the record held one outcome for what became two mechanisms, a widening with no sentence overstating anything because it lived in the DATA SHAPE; the roster of entries that must exist is now derived from the settings file. The range's headline is that THREE mechanisms here derive a requirement from the very set the requirement protects, and all three go quiet when that set shrinks — the hook presence requirement (AAAA-15), the document-rule roster count that AAAA-9 changed from an independent literal 9 to chosen.length so deleting a whole rule is silent today and was caught before (AAAA-16b), and the mechanism key being coarser than the script-and-event pair it identifies, so one script registered on two events yields two roster rows and one certificate — AAAA-13's finding recurring inside AAAA-13's own fix, in the same commit, confirmed by execution rather than reasoning (AAAA-17). The transferable form: a derivation is NOT automatically stronger than the literal it replaced. It removes one failure mode, the list going stale, and adds another, the count agreeing with any size; which one you want depends on whether the danger is the set growing or the set shrinking, and that question was asked in none of the three cases. The remedy already exists in the range as a pattern — an anchor the shrinker must separately touch, which is what registeredHooks refuses without. Also AAAA-18, the untracked-local-hooks half of the new document rule is a correct checker nobody has proven is called, since the missing half is mutation-tested and this one is asserted only at the probeCoverage level; and AAAA-19, registered is not in force, because disableAllHooks leaves the roster reporting hooks that do not run and that is a second module's opinion about one authority. One check was loosened deliberately and is recorded under item 2a: complainsAboutTheGate narrowed so the gate and roster halves stay distinguishable, which makes the induced-failure control stricter and the quiet case laxer. One proof case was repaired that had never reached what it was named for, because it asserted on two alternative refusals and its fixture produced the other one on every run. One count in my own report was recalled rather than measured — 25 files against a measured 24 — and was caught by the reviewing seat; it is the same failure as the denial count, a number carried in the head in the direction that makes the mechanism look wrong. AAAA-14 accepted: the reporter's live-ness is unfalsifiable while its only certifying event is the defect recurring, which is the shape engine-host-containment already paid for, so a reserved probe path supplies a benign trigger on a different axis, with one scan and one repair text, and with invocation and detection kept apart in the record itself; and 6c2017c..71deb64 — AAAA-14 through AAAA-19 closed — finding AAAA-20. The headline is that a figure I reported was wrong on BOTH axes and the correction round caught one: I wrote 8 commits and 26 files and 'the file axis is past a batch', where the instrument printed 7, 24, and 'Within one batch. An audit is not yet owed.' — the opposite conclusion, in words, at the end of its own output. The reviewing seat corrected the file count and carried my commit count forward, because it too was reasoning about my figures rather than reading the instrument's. Second time in two ranges, both in the direction that makes the gate look breached, and the first one I had already written up as the correction to sit with — so this is the class where a resolution to be careful has been tried and has failed. AAAA-20: a report that RESTATES an instrument is a second opinion about it, which is B3a arriving in prose; the finding is the second opinion and not the wrong one, and patching the figure would leave the next report free to compose a third, which is precisely what happened between the two ranges. The remedy is provenance rather than accuracy — paste the instrument's line, because a pasted line cannot be off by two and carries the verdict sentence with it. The audit trigger is recorded rather than carried: the next commit touching a file not already among the 24, or 10 commits, whichever comes first, read from auditWatermark.mjs where both comparisons are strictly greater against BATCH commits 9 files 24. No check was loosened: six proofs moved and the two deletions that read like removed coverage are a case rewritten in place under the same label and a roster count rising for EXPECTED_RULES, which is AAAA-16's anchor arriving. The range's other real find is that prePush.proof.mjs had never executed outside a developer machine — chained inside proof:guards while the workflow step named three of its four scripts and never the fourth — so wiring it in reddened main on its first run, exactly as intended: core.hooksPath is set by prepare, the Guards job runs no npm ci, and execFileSync THROWS on an unset key, so it killed the file rather than failing one case. Made UNVERIFIABLE rather than deleted, with both branches of the provisioning decision given cases on every runner, since an unset key must be null and never the empty string that would compare unequal to .githooks and report a FAILURE where the honest answer is that nobody installed this checkout. The ordering of this audit was decided by the mechanism and not by me: scripts/audit/scope.mjs is not among the 24, so making its verdict quotable would take the range to 25 files and the pre-commit gate blocks exactly that. CORRECTION 2026-08-24, appended rather than edited because this is a claim about evidence: the sentence above saying the reviewing seat carried my commit count forward BECAUSE it was reasoning about my figures rather than reading the instrument's is false about what that round did. It ran the instrument and quoted its block, and the block carried commits: 7; its prose named only the file axis because that was the half that changed the conclusion. I inferred a mechanism from an omission and recorded the inference as an observation, which is this range's own finding aimed at the wrong party. The finding itself rests on my error alone and is unchanged. Also AAAA-21, found by the reviewing seat one commit after the trigger line shipped: the line said 'or on the next commit touching a file outside these N', which is true only when N is exactly BATCH.files — the boundary the range sat on when the sentence was dictated — and false by twenty everywhere else. Naming this run's numbers is not the same as being right, and this was worse than the disclaimer it replaced precisely because AAAA-16's test certifies a line that names them. It now states HEADROOM on both axes, both thresholds BATCH.<axis> + 1 because the comparison is strictly greater on each. Its case was satisfied by the defect twice over: first asserting the line's SHAPE, where a backslash-d-plus matches whatever the formula produced, and then — after that was fixed — running against a fixture that is over budget and never prints the line at all, guarded by an escape clause that made it vacuous. Three fixture-the-defect-also-produces in three commits, all three in cases enforcing the specificity rule, and the pattern is the finding: A CASE ABOUT SPECIFICITY KEEPS CHECKING THAT SPECIFICITY IS PRESENT RATHER THAN CORRECT, because presence survives every mutation that changes a value; and 71deb64..b0a1da4 — AAAA-21 to AAAA-27, with AAAA-28 found by the reviewing seat against the range's last commit. The headline is that ONE PARAGRAPH IN sweepScope.mjs WAS REWRITTEN THREE TIMES IN NINE COMMITS and every version was believed when written: wreckage comes from scripts that COMPLETE, killed by measurement; then sequence DOUBLES wall time and orphans accumulate, killed by proof:shim measuring 1.5s and 9.4s at the SAME position so machine state was never isolated; then four ORPHANED processes prove nothing kills a tree, killed by their command lines showing an MCP server with LIVING parents spawned by the editor. Each correction narrowed the claim, which is the only reassuring thing about it. The useful reading is the other one: A CORRECTION IS NOT A TERMINAL STATE. Two of those three versions were written as the RESULT of an audit finding, reviewed, and passed, and were wrong — so the paragraph a range has just corrected is among the likeliest places for the next range's defect, which is this project's founding premise arriving in the SENTENCE rather than in the proof. No check was loosened; every deletion in that file is prose being corrected. AAAA-23's run log is recorded as a CAPTURE MECHANISM and not a fix, since the 0.0s mechanism remains unknown and the log only ensures the next occurrence keeps its own evidence; AAAA-26's label rule is recorded as a RULE and not a mechanism, its whole claim being that a missing citation is visible in review where be careful is not. Three mutations each reddened exactly their own case, and THREE FIXTURES THE DEFECT ALSO PRODUCES appeared in three consecutive commits, all in cases enforcing the specificity rule. Item 3 answered structurally: none of this range runs in CI at all — checkLocal is a local harness, the run log is under a gitignored .cache, and the clone route is a thing a person types, so every defect here was found by hand or by the reviewing seat reading a file. AAAA-28 is item 7's compound shape: sweepScope.mjs names .cache/checkLocal-lastrun.json, a file deleted one commit earlier FOR BEING THE DEFECT, ten lines above the correct path in the same string, telling an investigator the rows are in the single slot AAAA-25 removed — and the only case on that message asserts two substrings, which also makes that module's header claim about asserting the text a reader will meet broader than the case backing it. Repaired by giving the message no path of its own: the run-log directory is passed in from its only caller; and b0a1da4..7f77999 — AAAA-28 through AAAA-31 and both halves of AAAA-6's residue — the rarest thing this checklist has examined, a WITHDRAWN REQUIREMENT. The job object the FEATURES row had owed since WWW-2 is withdrawn because its premise did not survive measurement: an ordinary grandchild dies with the harness 3 of 3 and a detached one survives 3 of 3, so Windows already tears the tree down for every spawn shape this repository uses, and it uses no detached spawn. Removal came before design, which is the shape to check when a requirement disappears, since a requirement quietly dropped for being expensive reads identically. AAAA-31 is the correction to that withdrawal and the finding worth carrying: a withdrawal REMOVES a check, so its premise is the one that must be asserted, and this premise is a property of the RUNTIME that a node bump would falsify in silence — now a case on both platforms' Guards legs, with the differential as its control, because the grandchild is gone and the grandchild never started are the same observation. The 0.0s signature's CLASS is settled and its ERRNO is not: nothing that started node can render as 0.0s, since the floor is 116ms at its fastest over 15 runs and 0.0s means under 50ms, while a spawn that creates no process returns in about 3ms with status null and no output. Why it stayed unknown was in the instrument — checkLocal.mjs read spawnSync's status, signal, stdout and stderr and never its error, in zero places — so AAAA-23's question about what those 35 printed has the answer nothing, and nothing could have. Seven mutations each reddened their own case, and the ONE THAT DID NOT BITE is the entry worth keeping: AAAA-30's derived fixture lifetime survived its own inversion because a healthy run never waits out the budget, which is item 4a's branch-nothing-reaches inside a guard rather than a fixture, and was answered by asserting the ordering off the file the case writes rather than off the expression. A search's positive control found the search twice in two commits, both times correctly, and was fixed by ceasing to be a hit rather than by an exclusion that would also hide a real one. Item 3 was COMPUTED rather than recalled, which is AAAA-29's own remedy applied to the first range after it: affectedProofs named one proof, it resolves to guards.yml by path, and Guards run 323 shows both legs green. No check was deleted and no assertion weakened — the roster went 22 to 44 monotonically and every one of the twenty deletions invisible in the range diff is a count bump, a literal becoming a derivation, or prose corrected. STATED LIMITATION: five instruments produced this range's load-bearing figures and none is tracked, three having since been superseded by cases; the fifth, node's startup floor, is asserted nowhere and cannot be re-derived, ranked a limitation rather than a defect because it interprets a historical observation and the next occurrence will carry an errno instead — it becomes a defect the first time somebody needs the floor to read a NEW observation. And sweepScope.mjs's prose is now the file to watch: its Unblocked by line was rewritten three times inside this single range, above a paragraph already corrected three times in the nine commits before it, every version believed and each one narrower; and 7f77999..fb9731e — the transport's premise, the expectation vocabulary and a typecheck the local sweep could not reach — findings BBBB-1 (two fixes were instance-level and only one was re-fixed as a class; the tell is a correctness argument that names the specific thing it was written for), BBBB-2 (runInvocations' multi-invocation path is unexercised by either fixture) and BBBB-3 (typecheck.mjs derives its extent from the very script whose shrinking it should notice, which is item 4c's shape with the danger running the wrong way). All three open, all three with the remedy named. This audit was paid because the pre-commit gate REFUSED the tenth commit, not because a number was noticed — check:docs measures against HEAD and would not have seen it until one push later. The range's own headline is that the architecture changed before the feature twice, in the good direction: both ADR-0023 corrections were written before the pipe surface exists, so neither the descriptor nothing can open nor the shape built around handing a stream to node can now be built; and fb9731e..4a0ef5d — the pipe factory, its adapter, the DACL narrowed off GENERIC_ALL and the transport's termination measured — findings CCCC-1 (millisecond figures from the developing machine were written beside a sentence about the CI jobs, which assert only a bound; found in TWO documents, the second by the sweep the first one's correction obliged), CCCC-2 (the teardown probe measures termination and nothing about bytes crossing; the worker's read-completed branch is reached by no cell — a stated scope now, closing when the adapter's proof carries a frame end to end) and CCCC-3 (an instrument that shares a failure mode with its subject meets that failure mode first, in the run where it is trying to report: the teardown probe hung for ten minutes instead of reporting, then would not exit, because process.exit does not end a process whose worker thread is inside a syscall). CCCC-1 and CCCC-3 closed in the range; CCCC-2 open with its trigger. Audited at 8 commits rather than at the gate, because the next unit is more than one commit and being blocked halfway through a module is worse than auditing early; and 4a0ef5d..121c0ff — the write path measured, the write mechanism decided and its ordering built — findings CCCC-4 (the whole-path control compared a byte COUNT, and 64 frames of identical bytes sum the same in any order, so the reorder that would sink the write design produced the reassuring answer; the reviewing seat's), DDDD-1 (the affected-proofs disclosure printed THIS SWEEP DID NOT RUN THEM with nothing passed in that could support the claim, so a run that had just executed the named proof was told it had not), DDDD-2 (two comments state that one thing owns both directions of the pipe, and the decision falsified both — in win32PipeSurface.ts and in packages/kernel, neither named by any scope column, found by NNN-4's cross-document sweep rather than by any check), DDDD-3 (the ordering reading was taken BEFORE the discrimination control was mutated, which is item 4a's order backwards; the number is sound and the order is the mechanism), DDDD-4 (collect()'s first-failure guard survives its own mutation because the detail string is a constant, so nothing can observe which failure won) and DDDD-5 (interleaved inline-and-pending completions are reasoned about rather than measured, and the reasoning is mine). CCCC-4 and DDDD-1 closed in the range; DDDD-2 and DDDD-5 open, DDDD-3 and DDDD-4 recorded rather than fixed; and 121c0ff..484460a — the write adapter, its end-to-end probe, the write teardown and the worker's runtime mode, with one red board — findings DDDD-7 (the local sweep is structurally blind to lint, because checkLocal derives its set from check:/proof: names and npm run lint is neither; it reported 14 of 14 passed on a tree CI then rejected at step Lint on all three jobs, and the compensation being relied on was linting changed files by hand), DDDD-8 (win32PipeSurface's abandon strands every write without waiting when CancelIoEx fails for a reason other than ERROR_NOT_FOUND, and no case reaches that branch — the mutation that removed the cancel exercised the poll timeout instead, and this is the one branch of the teardown that decides whether main hangs) and DDDD-9 (workerModeHarnessWorker.ts runs in a worker thread, which this range MEASURED to be Node mode, and sits in the directory the same finding says Node-mode code must leave; it imports no Electron so nothing is broken, which is why it would have stayed). DDDD-2 and DDDD-6 closed in the range; DDDD-7's fix written and landing next; DDDD-8 open; DDDD-9 resolved by the B4 amendment that follows. Also recorded: two mutations against the write adapter did NOT bite and are kept at their constants rather than acted on — the OVERLAPPED's completion event, which nothing reads because the design only polls, and the payload copy, which the named-pipe file system makes indistinguishable from a view; and 484460a..4880ea0 — the lint gate, the B4 placement amendment and DDDD-8 closed — findings DDDD-10 (the cancel-failed branch's case could not separate it BY COUNT: inverting the test made the polling path strand all 31 writes too, having first spent the full budget, so only the elapsed time distinguishes them — and the reason is that a request whose handle has gone away keeps answering ERROR_IO_INCOMPLETE, so the poll can never settle it) and DDDD-11 (a control pair varying only the LEADING comment marker cannot see the other way to skip too much: a rule keyed on # anywhere passes both and swallows a real step carrying a trailing comment, which these workflows use). Both closed in the range. THE RANGE'S SHAPE IS THAT BOTH OF ITS NEW CONTROLS INITIALLY COULD NOT SEE THE BRANCH THEY WERE WRITTEN FOR, which is item 4's fixture-the-bug-also-handles-correctly arriving inside cases written to close findings. Audited at 4 commits rather than at the gate because the next unit — a new package plus the reader worker — is more than the five files remaining, and being blocked halfway through a package is worse than auditing a short range; and 4880ea0..4f37b51 — packages/nodemode created, the reader thread built, CCCC-2 closed, and main reddened TWICE on one control — findings DDDD-12 (Buffer.poolSize is 8192 on the developing machine and 65536 on the CI runners, so the claim that 4096 is exactly the size Node does not pool was one machine's accident recorded in an ADR as a property; the earlier phases were already writing offset payloads on CI and the blindness the new phase closes existed only locally — B6's rule, that a figure carries the number, the date AND where it was read, broken by a six-row table with no machine named) and DDDD-13 (after the first red I hypothesised a cause, reproduced it locally, found it REAL — a pool refill putting byteOffset at 0, 2 of 40 — fixed that and pushed, and it was not the cause; the annotation, one unauthenticated GET on a wrapped step, named poolSize 65536 the whole time. The reproduction was persuasive BECAUSE it found a genuine defect. Read the failure text before forming the hypothesis, not to confirm one). DDDD-12 closed in the range; DDDD-13 recorded, no fix. THE RANGE'S SHAPE IS THREE FIXTURES WHOSE CONSTANTS CHOSE THE INPUT THE DEFECT SURVIVES — one write batch excluding the read loop's second iteration, FRAME_BYTES excluding a pooled payload, and a single Buffer.from sample excluding every pool refill — each constant picked for a defensible reason, which is what stopped anyone asking what it excluded. Also recorded: two mutations that did NOT bite were turned into cases that do by finding the mechanism instead of recording the non-result, which is the opposite of this checklist's usual finding; and 4f37b51..16dc4da — main's half of the host protocol, CCCC-4's fixture, the OSV retry and ADR-0023 Decision 9 — findings DDDD-14 (the retry is proven in the helper and unproven at the call site: retryTransient.proof.mjs covers the helper with hand-built throws and is registered in guards.yml, while the three branches the range added INSIDE queryOsv, which decide what is transient here, have no case at all — advisoryRegister.proof.mjs spawns the real checker against the live API, so there is no seam a fake fetch could enter, and mutating the network catch to rethrow the cause reddens nothing; NNN-2's category, and it is the half that would have prevented the red board) and DDDD-15 (a fixture's constant was chosen from what the type would ACCEPT and a missing type state hid behind it: client.test.ts settles a connection with code 'frame' for 'the reader thread exited', three times, and HostTermination carries no code that is not a protocol violation — so a host that simply died would render 'the engine host connection ended (frame)' and send its reader to the framing code. Found by the FIRST REAL CALLER, since the composition being built next has to pass something to client.fail when a host dies and there is nothing honest to pass; no check could have found it, because both halves parse). Both open, both fixed in the commit that follows this one. THE RANGE'S SHAPE IS A CHECK PROVEN IN THE WRONG FILE: in DDDD-14 the file that got the new proof is not the file with the unproven branch, and in DDDD-15 the fixture that would have exposed the gap asserted only the outcome it cared about, so the constant it chose for everything else was free to be a lie; and 16dc4da..4859f20 — the composition point, and six commits of corrections to a decision written inside it — findings DDDD-20 (the roster anchor was enforced and correct while the paragraph explaining it was stale by ten: createRoster declared cases: 23 under a comment opening 'THIRTEEN: three for the working-host control, nine property rows, and the absence control'. Nothing was broken, because passRoster.format throws when recorded and declared disagree in EITHER direction, and a run confirmed 23. But that prose is the only part a reader consults when deciding whether their change should raise the number, and it was found at exactly that moment — trusting it would have meant computing the new total from 13. The repair REMOVES the copy rather than correcting it, since restating the number is what failed and an accurate restatement would go stale again in the same place: the rule check:docs already applies to 9.17) and DDDD-21 (a board reading is only available at the time. Two commits here were pushed without their own, and a descendant being green does not certify an ancestor. The retroactive check is impossible by construction: board.mjs fetches actions/runs?per_page=8, the eight most recent runs, which at two workflows per push is a four-push window. boardStatus.mjs classifies an aged-out sha correctly as `blind` and never reports a false green — its own text names 'a page that does not reach far enough' — but board.mjs polls that verdict 40 times at 30s, and for an aged-out sha the lookup cannot succeed because every further push moves it further out. Recorded rather than fixed: it degrades a diagnostic, not a verdict). THE RANGE'S SHAPE IS TWO CORRECT MECHANISMS WHOSE EXPLANATIONS HAD DRIFTED — an enforced count under a stale decomposition, and a sound verdict under a retry that cannot help — neither of which any check could see, because in both cases the part that was wrong is the part no check reads; and 4859f20..3c4f338 — Decision 7's verb split measured on three machines, a reset socket that reddened main on a docs commit, and Decision 10 — findings DDDD-25 (fetchVerified.mjs got its FIRST proof in this range and the proof is shaped like the incident that prompted it: ten cases, five of them controls, all about the retry rule, while four reachable branches of fetchChecked's redirect path have no case anywhere in the repository. The one that matters is the allowlist re-check on each hop, where the code resolves new URL(location, current) BEFORE the host check with a comment saying the ordering exists so a relative hop cannot skip it — a deliberate, security-relevant ordering with nothing holding it, NNN-2's shape. The existing allowlist case covers the INITIAL url, which is exactly the input a redirect defect sails past. The transferable form is not 'write more cases': a module's first proof is written by whoever just had an incident, so a new proof arriving for an old module is the moment to ask what that module does that the incident did not touch) and DDDD-26 (ADR-0023's Decision 10 states its blocker in the present tense — 'commandBus.ts calls spec.capture and spec.apply directly, in main' — and the blocker was removed one commit later. The heading reads as historical to its author and as current to a reader who lands on it. Corrected by an appended dated note rather than an edit, because an ADR is a record). ALSO IN THE RANGE AND OLDER THAN IT: two sentences of ARCHITECTURE.md — §3 and invariant 20 — still said the engines live in utility processes, which §2 of the same document denies and has since ADR-0022 on 2026-08-22. NNN-4's shape, unreachable by any range-scoped sweep because no range has ever changed both the sentence and the decision that refutes it; found by the cross-document sweep Decision 10's B4 question required. And the ordinary grep missed one of them, because the phrase wraps across a line break — the line-wrap axis for the fifth time in this repository, in a hand-run grep during a review; and 3c4f338..2f1444b — Decision 10 built, the concurrency class fixed, the prose sweep given a caller, and Decision 11's B4 amendment — findings DDDD-29 (two security scans run on a developer machine and nowhere else. checkLocal.mjs derives its set from every check:* and proof:* in package.json, so every check runs before a push HERE; CI runs the ones a workflow names, and scripts/security/pathDispatch.mjs and scripts/security/handlerFootprint.mjs appear in none. The first enforces invariant 23, and CI runs its PROOF — so every push proves the scan can see and nothing ever points it at the repository, which is the instrument-that-gates-nothing shape inverted: the gate exists, is proven sound, and is never fired. Run by hand during the audit it reads tracked source only and exits 0, so it is unregistered rather than unregisterable and can run on Guards today; handlerFootprint inspects the shipped DLL and may genuinely need the shim job, which has to be established rather than assumed. The transferable form: a check being DERIVED into the local sweep makes it feel covered, and the fact that one of the two has a proof running in CI is what makes the gap invisible, because the workflow file mentions pathDispatch and a reader stops there) and DDDD-30 (the checkpoint-failure control's INPUTS changed under Decision 10: a bare-lifecycle stub can no longer satisfy the registry type, so it spreads localMupdfWriter and its capture is now the real one. The right call — a lifecycle-only stub would fail at the capture, before reaching the checkpoint the case exists for, and would pass for the wrong reason — but a change to localMupdfWriter now changes this control's fixture, recorded rather than discovered later by whoever changes it). ALSO IN THE RANGE: the first B4 amendment in this project's record to be the whole commit, with the feature it exists for deliberately not in it. CORRECTED the same day, appended in docs/JOURNAL.md: DDDD-29's finding stands and two of its claims do not. pathDispatch.mjs does NOT read tracked source only — it derives the banned set from MuPDF's own is_extension and exits 1 with 'the dispatcher set was NOT derived and nothing was checked' where the source is absent, so it cannot run on Guards; it exited 0 during the audit because this machine has MuPDF provisioned, which is item 3's own second half answered wrongly inside the section that asks it. And the finding overstated by lumping: handlerFootprint.mjs is a reporter with one control, exiting 0 either way, so its absence was a missing measurement rather than a missing enforcement. Both now run in ci.yml's shim job, where the source and the DLL exist; and 2f1444b..5168f3b — the remote execution half, the board reader's derived pacing and the handed directories' grant becoming a security descriptor at creation — finding EEEE-1 (hostPipeDacl moved to hostDacl.ts and its two thorough cases stayed behind, under a describe for a symbol their file no longer imports, while the file that gained the function got a thinner duplicate of one of them; nothing was deleted and no coverage was lost, which is why the tests, the lint and the scope report all read clean — it is visible only by asking where a moved symbol's proof now lives, and it left two files asserting one expected value; closed in the commit that follows, because the gate blocked every further commit until this entry landed) and EEEE-2 (the affected-proofs reporter walks STATIC IMPORTS, so a proof that SCANS the tree appears in no column — electronImports.proof.mjs never imports lowboxSpike.mjs and is what turned 5168f3b red on both platforms, while the local sweep passed 16 of 16 and named only proof:hostcontainment. The reporter's closing sentence discloses this and is a DISCLAIMER by CLAUDE.md's own test: it could have been printed before the change, names nothing and asks for nothing. The class it misses is the worst one to miss, since a tree-scanning proof is what guards a repo-wide invariant and a repo-wide invariant is what a new file trips. OPEN, fix named: enumerate the scanning proofs in the run's own output as the import-reaching ones already are — not derived from anything a missing proof also shrinks, which is item 4c); and 5168f3b..cedec2d — the file-exchanging open/serialise/close, the capability that lets a canonical image leave without anything receiving it, and a measured invariant-20 exposure — findings EEEE-3 (a service outage was reported to the owner from an instrument that looks once: 'GitHub Actions has stopped running for this repository' was false, the sha is green, and the mechanism is that cedec2d was pushed at ~16:11Z while its runs were created at 16:30:18Z — a nineteen-minute lag that both reads fell inside. board.mjs separates pending from absent by WAITING and was bypassed by hand at the moment that distinction was load-bearing; the tool used instead printed a sentence listing the causes it cannot separate, which was read and overridden. The proposed diagnosis — a 403 rendering as an empty list — is a real hole and was already closed in parseRuns and its proof, and is not what bit, since both reads returned populated lists) and EEEE-4 (8215f42, the capability commit, has no CI run and a Guards run whose jobs were cancelled; six of its seven blobs are byte-identical at the green cedec2d and the seventh is a document whose later version passed check:docs, so the content is verified and the commit is not. Both jobs started 15:38:49Z and ended 15:53:50Z to the same second with no timeout-minutes set and nothing newer pushed — a could-not-look, since job logs answer 403 and the runs payload names no cancelling actor); and cedec2d..825c9a2 — Decision 9a built, a B4 amendment to 9.17, and one red commit on main corrected forward — findings FFFF-1 (EngineSessions.release was the mechanism three documents asserted a lifetime on, in the present tense, with no production caller; fixed by REGISTERING it as DocumentService's existing DocumentTeardown rather than by adding a caller, since the composition root was passing nothing for a seam whose own words are 'releases whatever a document holds outside this index — the engine session, above all'), FFFF-2 (9.17's base 96 MB had no recorded derivation, in the section that forbids assuming one; not measured-clean and not fitted-to-the-defect but ARGUED and never measured, per its own commit 752679e — 'the budgets are argued rather than fitted' — a criterion with no number in it, and the value landed within a megabyte of bare Node plus the whole kernel barrel; amended as ADR-0025 to base 80 MB with the rule that a baseline sits below the honest fixed cost plus the smallest thing it must catch) and FFFF-3 (that ADR derives its ceiling from ONE of the three classes 9.17 names and calls it the smallest, and only the native binding has a measured size; not lowered, because the slack also absorbs a >4 MB machine swing and a baseline that reddens on ordinary variance is a check people switch off — recorded as a stated limit, the same move that ADR already made for mupdf-host's undecided ceiling). EEEE-4's cancelled run is explained in this range and no longer owed to the owner: the same GitHub Actions critical outage that produced EEEE-3's lag, read from githubstatus.com's incident feed, so the Actions UI would have shown no cancelling actor because there was none; and 825c9a2..d92737f — Decision 9b and 9c built, the board guard's short-sha hole closed, and the containment probe given a channel — finding IIII-1 (describeEngineHostGone declares its termination as `code: string` while HostTermination.code is a twelve-member union its own caller passes in, so the one comparison deciding whether a host death reads as a fault is a spelling test nothing runs: misspell 'shutdown' and every deliberate close reports the crash message, compiling cleanly. Not fixed in the recording commit, which is docs-only and alone. The transferable form is that a structural parameter standing where a declared union is available turns every literal comparison inside it into an unchecked spelling test, and the tell is a call site that HAS the richer type and hands it to a poorer one). HHHH-1 and HHHH-2 were the reviewing seat's and are restored in this range: an appended correction consumed the heading it landed on, twice — `### 9c` in ADR-0023, and a whole audit entry's `##` heading plus its separator in this file, standing eight commits. No check can see either, since both documents still resolve; the chain that audit-entry ranges form is what surfaced the second, and no mechanism was built for the class because a chain rule cannot reach an ADR heading (4c's own reasoning). One question recorded rather than guessed: Decision 9c rejects get-or-create by name, so sessions are created at open, and DocumentService has teardown with no open-side counterpart — what happens when the engine host is unavailable at open is decided nowhere, and 9c's 4c anchor cannot exist until it is; and d92737f..f75005d — the engine host got a program, IIII-1 was fixed at both ends of its chain, and ADR-0026 was decided and amended ahead of its build — finding JJJJ-1 (hostSessions.ts was added with no proof of its own: three source files arrived and one proof, hostBody.ts is covered and hostEntry.ts is deliberately uncovered and says so, leaving ordinary millisecond-decidable logic pointed at by nothing. It is exercised INDIRECTLY through hostBody.test.ts's engine/close case, which is why nothing looked wrong, and what indirect exercise does not reach is the collision throw — a branch no fixture visits — nor the property behind it, which token.ts records as having been silently lost in this repository once already: a test asserting uniqueness and a 43-character shape is satisfied by a padded counter, and a padded counter substituted for the CSPRNG left the whole suite green. createHostSessions takes its byte source injected FOR that reason and no case uses the injection point. The transferable tell is cheap: the added-source and added-proof columns are both printed, and three against one is a question the report does not ask; two of the three had an answer and asking it found the third). Also recorded this range: the factory wiring cannot be observed in a development checkout at all, measured read-only with icacls against a positive control, and the three-way fork that leaves open is named rather than guessed; and f75005d..cc1e54e — ADR-0026 built and measured, JJJJ-1's cases, KKKK-1/2/3's record corrections — findings LLLL-1 (the declaration move dropped B3's own rationale from WriterOfRecord's doc — 'one writer per concern; two writers is how a codebase acquires sidecar hacks, and for a document it is how one engine's idea of the page tree overwrites another's' — leaving the mechanical half and losing the half that says why anybody should care. commandSpecs.ts is net +27 -64 and a net figure is how a move hides what it loses. The sharp part is not the loss but who lost it: in the same move the same author noticed this exact class on a different comment, deliberately carrying Reproducibility's richer original over the thinner replacement, and said so in the commit line. ATTENTION TO ONE INSTANCE OF A CLASS IS NOT COVERAGE OF THE CLASS. No check proposed, because a moved comment is not decidable — the same words at a new address and words that were dropped look identical to any scan that does not know what a move is; the checkable tell is the net-negative figure this report already prints, and the trigger is to read the deletions of any file whose diff is net-negative after a move against the file the code moved to) and LLLL-2 (a 33-file mechanically-uniform change cannot land as one commit and auditing first does not help: BATCH is 9 commits and 24 files, and the pre-commit figure is committed.files UNION staged, so a commit staging 33 exceeds the budget whatever the range holds. Not filed as a defect in the gate, whose threshold is the median of batches 4-7 and exists for a good reason; filed because the consequence is not obvious from the gate's own message, and because the shape it refuses — whole-class, auto-fixable, uniform — is the shape whose entire value is a diff legible in one piece. Disposition is the owner's: split with an audit between, raise the budget for a uniform diff, or accept that such changes land in parts); and cc1e54e..4eb94a7 — ADR-0027, Track D's baseline finding, MMMM-1's plugin reading and the first part of the emitted-import rewrite — findings NNNN-1 (the emit-diff control ruling LLLL-2 specifies cannot fail: the rule fires only where every specifier is inline-type, so its fix always DELETES a statement, and a removal that erased a load something needed is a removed `import {} from` line too. Reachability is the half that separates, and all seven targets in part 1 retain live load-points; open only in the sense that the same control governs parts 2 to 4, which must carry the reachability half with it), NNNN-2 (two instruments recorded as differing by one with the gap unchased; the gap IS the uncovered export half, since ESLint sees 69 and the grep sees those plus one `export {} from`. Corrected in 4eb94a7 — a discrepancy of one reads as rounding and attracts a sentence choosing which figure to trust) and NNNN-3 (LLLL-1's repair can regenerate and no check is proposed, deliberately: a comment move is not decidable by a scan, and a grep pinning the one paragraph already found would leave the class undecidable while reading as watched); and 4eb94a7..71b299d — ruling LLLL-2's lint unit in four parts with the rule enabled last and alone — findings OOOO-1 (docs/FEATURES.md states the rule is an error and nothing checks it: deleting the line leaves check:lint green, because fixing the class first removed the last violation it could have reported. This is audit finding 31 verbatim, and lintRules.proof.mjs is the mechanism that already exists for it, so the fix is a registration rather than a second check. The transferable half is that FIXING A CLASS REMOVES THE EVIDENCE THE GUARD WORKS, so the proof must be written in the window before adoption or built from a fixture that does not depend on it), OOOO-2 (the export half's only protection is a comment at the site, which this project's record — seven escape-guard occurrences and seven emitted-template ones — says is not a mechanism; expect a recurrence, and the row owes a scan over the emit) and OOOO-3 (nothing in CI covers the general case a bad rewrite would have caused, and the per-part reachability count that did carry the risk is not in CI either; kept as a recorded method rather than made a check, because whether a lost load-point MATTERED is not decidable and a half-check would read as watched); and 71b299d..9bbfbbb — PPPP-1's quantity fix, ADR-0025's base 98 derivation, ADR-0023's open-time correction and its build, PPPP-2, and the UI substrate's token file — findings QQQQ-1 (I turned check:emittedsideeffects from blocking to reporting in the pre-commit set after it blocked every proof:guards case, whose fixture repo has no dist; the reasoning holds and the consequence was understated, because on a machine where nobody builds that half prints one line and passes every time. The row's TWO MECHANISMS is one gate plus one best-effort local copy. The rejected alternative — an index-reading SOURCE scan, since the export spelling is syntactically decidable — was dismissed in one line as a B3a second opinion, and B3a objects to reimplementing an authority where here no rule covers the spelling at all. Raised, not resolved), QQQQ-2 (tokens.css's header documents the role grammar, its illustration began with the real marker, and the parser read the documentation as a declaration — the emitted-template class in a new file type, where prose and machine-read content share a syntax. No scan proposed on 4c grounds; the remedy that generalises is a VALIDATING parser, and the proof now requires every parsed role name to be a valid custom-property name) and QQQQ-3 (onDocumentOpened's post-loop report is reachable by no code in this repository, since the loop bound and the counter read the same constant; kept as JJJ-1's first kind, because deleting it would let open-and-sessionless arrive in silence the day they stop agreeing); and 9bbfbbb..e48b265 — RRRR-1's roster, RRRR-4's row-length rule, ADR-0027's grant built and then measured, and SSSS-2's runtime correction to R — findings TTTT-1 (preCommit.mjs swapped its emit-reading gate for an index-reading one and left the old comment standing eight lines above the new one, asserting IT READS THE BUILD ON DISK WHICH THE INDEX IS NOT, in capitals, with every clause false and one directly contradicted by the paragraph below it; two headers for one gate, and a promise to name staged TypeScript files through a helper the same commit deleted. Item 7's documented shape, and what is new is that the stale half was WHOLLY false rather than half-true and still survived, because it sat above a correction rather than inside one. Found by reading the range's only net-negative file), TTTT-2 (my first case for annotate.mjs --always asserted that a failing run still emits ::error and still exits 3, and dropping the status === 0 guard left both exactly as they were, so the case passed its own mutation. The decision on a failing run is NOT to emit a notice, and the absent notice is the only thing the correct path produces. Third instance in two days, each inside the commit fixing the previous one), TTTT-3 (I ran the three proofs checkLocal derived by import reach, skipped the five it printed by name, and pushed two commits without waiting for the board; both were red on CI and green on Guards at the electronImports step, because containedStart.mjs loads the built surface through a file:// URL and was unlisted in ACCOUNTED_COMPUTED. The printed list is a DISCLAIMER by this project's own test — the same five proofs every run, computed from nothing about the change. The fix is to run them, measured at under a minute against a three-minute sweep; raised, not taken in an audit commit) and TTTT-4 (containedStart.mjs's first verdict said the grant changed nothing observable while its logs showed one cell dying at icu_util.cc:232 and the other in node's module loader; both were in one no-program bucket and the verdict compared buckets, so an absent distinction read exactly like an empty one. Cells carry a stage now); and e48b265..acb2cbb — the hung vitest tree, two temp leaks, UI-GUIDE.md, main's spread, the border scan and Track C's precondition — findings VVVV-1 (checkLocal.mjs appends the scanning roster to a check-only selection and read SCANNING_PROOFS directly, reaching past SCANNING_PROOF_COUNT — the anchor that exists because the failure to fear makes the roster SMALLER. Removing an entry with its count would have left the sweep running eight of nine while affectedProofs.mjs, which also reads the raw array, agreed. The module has a guarded accessor, scanningProofRoster, and the new consumer imported the constant beside it: B3a's helper-beside-a-bare-expression, one step on. Fixed in the following commit), VVVV-2 (the swallowed-error census matched a catch inside a COMMENT — in documentConsistency.mjs, the file documenting that defect — and inside a STRING, 194 becoming 151 once both were masked; then its width metric counted the sentinel in try/return-false/catch/return-true and reported eleven sound sites as defects, 40 becoming 13. An instrument built to find reassuring answers produced two of its own, and the nearly-right metric was the more dangerous because its output looked like real defects) and VVVV-3 (a `*/` written inside a JSDoc block closed the comment early and node refused the file — the emitted-template class in an ordinary comment, where check:emittedtemplates does not look by design, so the variant has no mechanism and will recur); and acb2cbb..36a988b — the WWWW findings, the grant set reshaped to directories, the WWW-2 sweep refusal lifted and ADR-0028 — findings XXXX-1 (checkLocal.mjs's module header still asserts the multi-proof refusal that 0f7f7de deleted, 220 lines above the two sections explaining the lift; a compound claim whose surviving clause vouches for the dead one, and outside sweep:prose's reach because that tool reads .md only. Third occurrence of this shape in a source comment against none the document sweep has caught) and XXXX-2 (documentConsistency.mjs decides whether an ADR is corrected with a pattern matching only the blockquoted banner form, so nine ADRs carrying dated body-level corrections are reported as uncorrected and their index rows all read Accepted — three of them corrected in this very range. The rule was already fixed once for the same blindness, for the EM dash and not for the blockquote marker, which is half a fix reporting found-nothing in the voice of a clean corpus. Which way to repair it is deliberately left open: either the two forms mean different things and the distinction belongs in DECISIONS/README.md, or they do not and nine rows need updating) and XXXX-3 (ADR-0028's Status line says the amendment is the NEXT commit, and that the law therefore states the superseded clause for exactly ONE commit; the commit recording this audit is the next commit and is not the amendment, so the count is wrong the moment it lands. The same compound-claim shape as XXXX-1, written by me in the hour I wrote that finding — and false the day it was written in AAAA-8's sense, since B4 requires the amendment to precede the FEATURE and says nothing about what may sit between it and the ADR. No range-scoped sweep could find it, because nothing changed to falsify it); and 36a988b..c8fa4d0 — YYYY-1 in three parts, the mupdf-host role driving the real host, ADR-0005's library set — findings YYYY-2 and ZZZZ-1, which are ONE finding in two costumes: I ran half of a two-part verification and read its green as covering both halves, twice, and both reddened main. YYYY-2 (the two new reportPeakOf cases call peakWorkingSetOf, whose own header says it is Get-Process and throws off Windows, so on Linux both calls throw, the refusal case passes for the wrong reason and the control fails — the control catching it is the mechanism working; gating the cases was then NOT enough, because proof:peakrss ran on the ubuntu job and nowhere else, so gating alone would have meant they run in CI never, which is ZZ-1's shape and strictly worse than the red being fixed; the proof is now on the Windows job too). ZZZZ-1 (installing 43 production packages left NOTICE stale, and checkLocal.mjs cannot see the check that says so: it derives its set from package.json by NAME, every script starting check: or proof:, and notice:check starts with notice: — as do brand:check, guard:staged, guard:tree, perf:gate, electron:surface, shim:reach and ocr:doors. That is a THIRD limit of a different kind from the two its header documents: those are about reach, this is about the classifier, and the classifier is a naming convention — W-1's 'a file-naming convention is not a check' one layer up, about which SCRIPTS count rather than which FILES. Its failure is silent: a script outside the pattern produces no error and no visible absence, and the sweep reports a smaller number and calls it 29 of 29. Remedy recorded rather than taken — rename into the pattern so the name IS the classifier, or a roster with an anchor per 4c); and c8fa4d0..2091b91 — onColor, the per-document stores, ADR-0029 and the component-test vehicle — findings EEEE-1 (check:domenvironment cannot see the thing it forbids: its walk is rooted at packages/ and apps/ and matches .ts/.tsx only, while vitest.config.mjs sets no include and vitest's default pattern collects ?(c|m)[jt]s?(x) from the whole repository. Measured by writing scripts/auditprobe.test.mjs, which vitest ran with a working DOM while the check reported 'no test outside packages/ui names one', 118 files, exit 0. W-1's pattern axis and X-1's root axis together, in a new instrument, one range after both were fixed in the audit-scope report. Its positive control is sound and tests the MATCHER; the blindness is in the WALK, and its filesScanned>0 anchor sits on a different axis from the danger, which runs toward a smaller set. Remedy: derive the extent from vitest's own collection rule rather than hold a second opinion about where tests live), EEEE-2 (docs/UI-GUIDE.md states that React is not installed; it was installed in 3e25b74, inside the PREVIOUS range, and that audit's item 7 did not reach it. A compound claim with one clause dead and one alive — axe and Playwright genuinely are absent — which is the shape no reader flags. Found by running item 7's NNN-4 compensation for the first time since it was written, keyed on the writer-of-record relationship this range asserts about the WCAG formula) and EEEE-3 (a FEATURES row written in this range claimed docs/UI-GUIDE.md was still owed; the file exists, 192 lines, committed at 8288aac. False at the moment of writing, so no staleness sweep could ever find it — AAAA-8 — and caught two minutes later by the sweep run for EEEE-2 against a different question). One decision flagged for the owner rather than defended: useOnColor reads tokens at the document element rather than at the target element, chosen after happy-dom was measured not to inherit custom properties to a child, which is a workaround with a stated trigger rather than a fix; and 2091b91..af73baa — the four Stage 0 primitives and EEEE-1's fix — finding GGGG-1 (the fix for a second opinion replaced it with two copies, and both sources are importable: COLLECTED transcribes vitest's defaultInclude, which `vitest/config` exports, and SKIPPED transcribes vitest.config.mjs's own test.exclude, which is one import away. Measured by importing them — defaultInclude is **/*.{test,spec}.?(c|m)[jt]s?(x) and the project's exclude is node_modules, dist, .tools, .probe, release — and the two sets ALREADY DIFFER: SKIPPED carries .cache, which the project's exclude does not, so a .test.mjs under .cache is collected by vitest and skipped by the scan. The silent direction again, inside the fix for the silent direction. CLAUDE.md's rule is copy only where the reader cannot reach the source, and the reader reaches both; the trade-off recorded rather than skipped is that importing gives the scan a node_modules dependency it does not have today, which check:jobplacement exists to keep honest and which the build job already satisfies). Also in this range: the owner ruled that useOnColor's read site is settled from ARCHITECTURE section 6 and ADR-0003 rather than from what happy-dom does, and that the CSP question is a Playwright mechanism with a positive control rather than a setting; and af73baa..e234979 — ZZZZ-1's remedy, the reliance sweep, the read site, section 9.17's amendment and unit 7's library-independent half — finding FFFF-1, the largest of this build: perf:gate, electron:surface, shim:reach and ocr:doors appeared ZERO times in either workflow, verified against the committed tree at af73baa, and every one of them has a proof registered in CI that imports the check's own functions (electronSurface.proof.mjs:41, ocrDoors.proof.mjs:57, shimReach.proof.mjs:28, perfBudget.proof.mjs:29). So the machinery executed on every push against fixtures, proving the instrument can see, refuse and separate, and nothing ever asked it about the repository — THE INSTRUMENT CAN SEE standing in for THE INSTRUMENT WAS POINTED AT THE TREE, item 4b's distinction at the highest stakes it has appeared at here: three security checks and the gate enforcing section 9.17. A proof that a check works is not evidence the check ran. Registered at 441ba50 and green on the first real run, read from the run's own step list rather than the board colour because a skipped step and an executed one produce the same colour. Also HHHH-1 (AAAA-1 is blocked on a precondition nobody named: nothing opens a document — DocumentService.open has no shipped caller and the contract declares only app.info and document.execute, so wiring the four surfaces produces a graph whose every edge is unreachable; three shapes recorded for the owner). The sweep's roster moves from 29 to 114, its authority from a naming convention to the workflows, and the danger direction inverts with it — anchored by FLOOR for collapse and by verifiersNotRunByCi, an exception register with written reasons, for shrink. One correction to the reliance sweep itself: its first scope looked only inside item-3 sections, reported 0 of 33, and missed the instance that prompted it, which sits under a 4a/4b heading. GGGG-1 remains unfixed and is carried; and e234979..584362b — the open path — findings KKKK-3 (the channel falsified a premise stated in four places, and in two of them the premise is load-bearing: document.execute against a now-openable document reaches MissingSessionError and answers internal, which composition.ts says by name cannot happen. proof:shell still passes, because its case executes against a DocId that was never opened, so it cannot reach the state its own header calls unreachable — a false premise under a true assertion, which no mutation of the code under test can find. FEATURES.md's row is edited true here; the two code comments are corrected in the commit that gives the engine a session), KKKK-2 (check:typeonlyexports is gated in pre-commit and checkLocal and in no workflow, while its sibling check:emittedtemplates is gated in both — FFFF-1's asymmetry surviving one notch weaker, decided by nobody) and KKKK-1 (IIII-1's batch conversion made an absent staged blob mean skip-this-file, where filesInCommit guarantees every path resolves — so a desynchronised batch parse silently shrinks the scanned set and both scans report no violations, with scanned derived from the same collection and the positive controls proving only that the matcher sees). KKKK-1 and KKKK-2 are fixed in the commit after this one; the range's headline claim was mutation-tested in both directions and separates all three open outcomes. CORRECTION 2026-08-28, appended rather than edited: KKKK-2 is WITHDRAWN, before it was acted on, so only KKKK-1 was fixed. the placement was decided and the reason is written four lines below the line that was read — CI carries check:emittedsideeffects, which walks dist and is the stronger half, since the source cannot tell an inline type export from a type-only one and one of them runs. It was reached by grepping the workflows for the script path, getting no hit, and writing 'decided by nobody' — an absent grep result and an absent decision being the same output. The rule: when a check appears registered nowhere, read what is registered AROUND its proof, because a deliberate placement leaves its reason beside the thing it placed; and 584362b..48ad002 — the composition root creating the engine host, with TWO RED BOARDS inside the range — finding KKKK-7 (composition.ts grew 371 lines of lifecycle and every case calls createShellDependencies with no platform, so connect()'s first line throws and nothing runs the factory call, the containment ask, the terminate-on-not-contained branch, the memoised promise or the death handler. Not a platform limitation: every surface is injected, which is the whole argument of EngineHostPlatform's header, so a fake platform exercises the ordering with no Win32. Closed in the commit after this one, not carried), plus KKKK-6 fixed (affectedProofs walked imports only and the register is read by path: 0 of 85 before, 4 of 85 after), KKKK-3 closed, KKKK-1 fixed, KKKK-2 withdrawn, and KKKK-4 and KKKK-5 escalated to the owner as B4. The two red causes: an em dash after a JSDoc @see tag, TS1127, which eslint and vitest and the scan's own proof all pass over because none compiles JSDoc, and which broke both CI legs plus the containment job that runs typecheck; and a fixture pinned by key to an advisory verdict that had just fired, where Object.values({})[0] is undefined so the mutation landed nowhere and a correct pass read as a failure to refuse. npm run local reported 107 passed, 0 failed, 1 timed out — proof:perfbudget at exactly the 180.0s default, timed alone at 214.2s, so the standard sweep CANNOT complete and leaves typecheck and lint among eight scripts never reached; and 48ad002..e811719 — ADR-0030, the section 2 amendment, and the remote writer reaching the bus — finding LLLL-1: A2 moved open out of remoteMupdfLifecycle and left EngineOpenFailed behind, so the class is declared, exported and thrown by nobody, while composition.ts throws a bare Error with a template string for the same condition. The class it replaced carries an argument the replacement does not — that a document which will never parse must not count as evidence the host is unhealthy — and that distinction is live, because onDocumentOpened counts a failed creation towards Decision 9a's poison bound. Two proof cases moved with it from testing the product to testing the fixture: both assert the proof's own openAsSupervisor helper throws what that helper throws. The move's audit signal was engineHostConnection.test.ts at net +22-158 and THAT file is clean — 16 cases before and after, no deleted line carrying it( or expect( — so the damage is in the file that grew. Also recorded: the gate fired on the commit being COMPOSED rather than on HEAD, which check:docs structurally cannot do, since it measures against a HEAD that is still the parent; and e811719..d8591db — atomicWrite, the coverage report's changed-file scope and the save pipeline — findings BBBBB-1 (the save pipeline landed and a B4 amendment was owed BEFORE it: FEATURES row 288 carries an expiring claim whose trigger is whichever of the save pipeline or checkpoint restore lands first, and ADR-0023 says the amendment is owed before the save pipeline. Reason 1 for deferring is now false, reason 2 still holds, so the amendment is owed and its content is not thereby decided. The claim was parked correctly on a row, since a symbol scan cannot see an event — but a row is read by whoever opens that row, and nothing about writing savePipeline.ts opens it. Found by a cross-document prose sweep aimed at something else. Escalated rather than guessed: one of the three candidates collides with ADR-0009 by construction) and BBBBB-2 (ADR-0023 cites the claim as living on row 284 and it is on row 288 — a reference by ROW NUMBER into a document that grows by INSERTING rows, three of which landed above it in two days. UU-1's shape with no link to check, so nothing can report it; corrected by citing the row's subject rather than its ordinal). Also recorded: two mutations that did not bite and were investigated rather than filed — save's poison-before-session order is unobservable because the two guards are mutually exclusive, and the clean-repository control failed first time because the harness writes .cache into the root it measures; and d8591db..67f38bb — the ICC question, the escape guard's bound and EngineOpenFailed — where every finding was about an INSTRUMENT and three of the four were produced by the commits written to close the previous instrument defect. CCCCC-1 (the positive control the ruling asked for could never have fired: iccUrl is not a fourth BinaryDataFactory kind, the factory serves exactly wasmUrl, cMapUrl and standardFontDataUrl, and iccUrl reaches a synchronous XMLHttpRequest instead — so a control needs a control, and the replacement one fired and named connect-src rather than wasm-unsafe-eval as the deciding directive). CCCCC-2 (the wrong-context instrument reported fixed in 4fad19a was half fixed by the same commit: the repair added two correct new questions and left the per-document columns reading the window, so one line printed wasm=0 while the console from the same run showed the worker refused at wasm-eval. Rule 0's fix-the-class in the shape where the half-fix looks like extra rigour — a correct instrument printing beside the broken one). CCCCC-3 (shellFailure.test.ts's completeness assertion is computed from the roster it polices, so it catches a duplicate and never an omission, and its stated claim is already false — there is a fifth describe function with no row, covered elsewhere by contract.proof.mjs; 4c's shrinkage direction, fixed by an anchor in the commit after the audit). CCCCC-4 (nothing in CI has ever executed the CSP measurement: proof:rendererpolicy proves the derived policy equals section 9.27, which is provenance, and says nothing about what PDF.js does under it — two questions, one covered, owed with A6). Read and not filed: the escape guard's controls were tightened rather than loosened, one mustAllow becoming the ruling's pinned mustBlock; the document.save rows added to three reject fixtures do not double their reasons, since write-failed carries no other field; and item 2a, that the ICC basis moved from one measurement to a derivation plus a diagnostic measurement while the per-document columns' claimed scope narrowed; and 67f38bb..9f51552 — the registries, the coordinate spaces and the escape from A6 — where both findings are in the range's own new code and both are shapes this checklist names. DDDDD-1 (excluding .test.ts from check:secondwiring's walker was right on its own argument and broke an equivalence its refusal branch rested on: that branch fires when the surfaces DIRECTORY is missing, which used to imply an empty input, and a directory holding only test files is now scanned-with-zero-files and reported as a clean pass. My own proof case documents the hole while asserting the behaviour is fine. Transferable: narrowing a search's input set creates a new route to its reassuring answer, and a refusal keyed on something that used to imply a non-empty input does not follow it there). DDDDD-2 (the same defect from the other side and it predates the range: the shipped positive control is a string passed straight to scanModule, deliberately so tidying cannot delete it, so it exercises the matcher and no part of it goes through the walker — checklist 4b's control exists for half of this search, and the half without one is the half I edited). DDDDD-3 (a case computed from the roster it polices: the ribbon's first assertion compares the model's sections against SECTION_IDS, the hand-kept roster the projection derives from, so deleting a section makes both sides agree while section 7 says SectionId is exactly the eight of section 10.3 — written hours after the previous range fixed the same 4c shape in shellFailure.test.ts). Also recorded: npx tsc -b is half of npm run typecheck and the half not run is where the error was, which reddened the board at 9a424b8; and A6's channel is a STOP rather than a build, since perf:gate reads main at 1.00x holding one canonical image and 2.00x holding two against section 9.17's 1.5x; and 9f51552..02b1f65 — the audit's own fixes, B2 and B4 — where the finding is a recurrence rather than an instance. EEEEE-1 (shell.proof.mjs declares its roster as RUNTIME_CASES.length, computed from the list that declares it: passRoster catches a case whose check() is deleted, and cannot see one removed together with its label, which is the same edit anybody deleting a case makes. Every other proof here declares a literal. THE RECURRENCE IS THE FINDING — CCCCC-3 was 4c in a desktop test, DDDDD-3 was 4c in a UI test written hours after that fix, and this is 4c in a proof harness, all three by the seat that wrote the first two corrections. Deriving is the tidy-looking option and 'which direction does the danger run' is not a question anybody asks unprompted). EEEEE-2 (DialogHost mounts a registered body through two `as never` casts — the strongest erasure the language has, in the one file whose subject is validated props. The tie between a schema and the component typed by it is real where an entry is DECLARED and erased by the registry's storage type, so the erasure belongs in a helper that builds the entry, where it is provably safe once, rather than at a mount point where nothing can check it). Also recorded: the npm run local row is 1610 words, six times the target, and its length guard fires on whoever edits it next — the repair is to cut it to a specification with a pointer, since its body is a history of eight findings that each already have a JOURNAL entry; and 02b1f65..7669d69 — the local gate's runner, the dialog builder, ADR-0031 and the section 2 amendment — findings FFFFF-1 (checkLocal's TIMEOUT_MS became a per-STEP bound when the single spawn became a loop, while BUDGET_SECONDS fifty lines up still derives a per-SCRIPT worst case from it and orders the sweep by it, so one number does two jobs and is derived as though it did one; and every timeout fixture is single-step, so a chain timing out on its second step is a branch nothing reaches), FFFFF-2 (the call site that decides which scripts run has no case, while the resolver it calls has a thorough one — and the case that looks like coverage states a rule C2 removed and uses a fixture BOTH versions refuse, so it passes identically before and after the change it now sits under; the shape needing a case is a command resolving through a bin) and FFFFF-3 (ADR-0031's byte figures come from a scratchpad probe, because pdfjs-dist is a dependency of nothing here; the commit that declares it owes those figures as a committed proof). FFFFF-2's stale label is left standing deliberately, with its missing fixture, because repairing the sentence alone would leave a case that reads correct and separates nothing"
}
